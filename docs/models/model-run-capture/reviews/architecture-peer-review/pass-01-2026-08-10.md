# Architecture peer review — pass 1

**Plan folder:** `docs/models/model-run-capture/`  
**Date:** 2026-08-10  
**Scope:** full  
**Reviewer:** architecture-peer-review skill  
**Baseline:** [MODEL_RUN_CAPTURE_FINDINGS.md](../../MODEL_RUN_CAPTURE_FINDINGS.md), [MODEL_RUN_CAPTURE_GENERAL_PLAN.md](../../MODEL_RUN_CAPTURE_GENERAL_PLAN.md), codebase paths cited below  

## Verdict

Review complete: 0 critical, 3 high, 4 medium/low.  
**BLOCK create-execution-plan:** no — findings and phased plan are sound; execution files must resolve high items (snapshot finalize scope, embed capture location, PO-P1.4 ownership) before `phase-execution`.

**Pass 1 incorporated (2026-08-10):** high/medium items merged into findings (MRC-Q8–Q10) and general plan; judge-step claim corrected in findings.

## Prior pass

First pass.

## Verified (docs match codebase)

| Claim | Code evidence |
|:---|:---|
| Index manifest lacks embedding model fields | `github_pipeline_trace.py:511-524` — copies `reused_count`, `new_count`, `embed_batches` only |
| Index step row has no `model_provider`/`model_id` | `github_pipeline_trace.py:489-503` — `_create_completed_step` / update without model args |
| Voyage client has no attempt recorder | `voyage_embeddings.py:109-149` — HTTP only; logs `settings.revy_embedding_model` |
| `github_llm_call_attempts` table + ORM exist (PO-P0) | `alembic/versions/2026_08_10_1200_0031_pipeline_observability.py`; `github_llm_call_attempt.py` |
| Review path writes attempt rows when pipeline run present | `github_review.py:1178-1194` — `LlmAttemptStartContext` + `start_attempt` |
| Judge / publish do **not** write attempt rows today | `start_attempt` only in `github_review.py` + `llm_call_recorder.py` |
| Review step has `model_provider`/`model_id` | `github_pipeline_trace.py:590-591` |
| Judge outcomes store `judge_model_id` | `github_finding_judge.py:381`; `github_finding_judge_outcome.py:40` |
| Judge pipeline step lacks `model_id` on step row | `github_pipeline_trace.py:706-713` — no `model_provider`/`model_id` passed |
| Publish pipeline step lacks model fields | `github_pipeline_trace.py:842-855` |
| No `models_snapshot` on `github_pipeline_runs` | `github_pipeline.py:21-60` — column absent |
| Staging metrics script has no model breakdown | `pipeline_observability_staging_metrics.py:48-102` — `GROUP BY failure_class` only |
| `index_manifest_stats` is in-memory on job; embed fields not included | `github_indexing.py:320-331`; copied in `record_index_pipeline_step` |
| Index step may pre-exist as `pending` | `github_pipeline_trace.py:394-403` — `stash_pipeline_github_check_run_id` |
| `index_embed` step type enum exists | `enums.py:276-282` |
| Retrieve manifest has no query-embed model | `github_review.py:561-594` — `build_retrieval_manifest` |
| Judge runs after review, before publish | `reconcile_tasks.py:129-137` |

## Rollup (critical / high)

| Severity | Section | Title | Resolution |
|:---|:---|:---|:---|
| high | MRC-P2 | `models_snapshot` finalize scope omits **judge** | **Fixed** — MRC-Q9 terminal write includes all four roles; general plan P2 updated |
| high | MRC-P0 | Embedding model captured at embed time in `github_indexing`, not in trace writer | **Fixed** — general plan P0 + findings target catalog |
| high | cross-cutting | MRC-P1 / PO-P1.4 ownership overlap | **Fixed** — MRC-Q10: PO-P1.4 wires recorder; MRC-P1 verifies + judge/publish steps |

## Per-section findings

### Findings — baseline accuracy

| Severity | Area | Finding | Resolution |
|:---|:---|:---|:---|
| medium | findings | **Judge step `model_id` overstated** | **Fixed** — findings table corrected; judge step has tokens only today |
| low | findings | PO-P0 shipped; clarify vs P1.4 Voyage gap | **Fixed** — gap table wording updated |
| low | findings | Locked MRC-Q1–Q6 parity with general plan header | OK |

**Verified OK:** Gap table for index embed, Voyage attempts, staging metrics, chunk column deferral matches code.

### MRC-P0 — Index embedding identity (manifest + step)

| Severity | Area | Finding | Resolution |
|:---|:---|:---|:---|
| high | general plan | Capture model in `github_indexing` at embed HTTP time | **Fixed** in general plan P0 |
| medium | general plan | Update path for pre-existing `pending` index step | **Fixed** in general plan P0 + findings reuse traps |
| medium | clarity | `embed_batches=0` null semantics | **Fixed** — MRC-Q8 locked |

### MRC-P1 — LLM attempt rows with `request_model`

| Severity | Area | Finding | Resolution |
|:---|:---|:---|:---|
| high | cross-cutting | PO-P1.4 / MRC-P1 ownership | **Fixed** — MRC-Q10 + wave order note |
| medium | general plan | Judge step `model_id` in addition to attempts | **Fixed** in general plan P1 |
| medium | codebase | Publish formatter not instrumented | Unchanged — PO-P1 dependency |

### MRC-P2 — Pipeline models snapshot + staging metrics

| Severity | Area | Finding | Resolution |
|:---|:---|:---|:---|
| high | general plan | Finalize must include judge | **Fixed** — MRC-Q9 + P2 scope |
| medium | general plan | `GROUP BY step_type, provider, request_model` | **Fixed** in general plan P2 |
| low | general plan | MRC-Q7 retrieve defer v1.1 | OK |

### MRC-P3 — API exposure + staging sign-off

| Severity | Area | Finding | Resolution |
|:---|:---|:---|:---|
| low | general plan | Flatten manifest `embedding_*` on API | Noted in P3 scope |
| low | clarity | No FE/i18n v1 | OK |

### Cross-cutting

| Severity | Area | Finding | Resolution |
|:---|:---|:---|:---|
| medium | locked decisions | MRC-Q4 four-role snapshot | **Fixed** — MRC-Q9 terminal writer |
| low | dependency | MRC-P1 soft-depends PO-P1 | **Fixed** — general plan P1 depends |

## Open questions (resolved in findings)

1. **`embed_batches=0`:** MRC-Q8 — null / omit keys; `embedding_skipped_reason=reused_chunks_only`.
2. **Snapshot writer:** MRC-Q9 — terminal write after publish (or failure hook); not incremental v1.
3. **PO vs MRC wave order:** MRC-Q10 — PO-P1.4 wires Voyage; MRC-P1 verifies + judge/publish steps.

## Pre-execution checklist

- [x] Fix general plan P2 finalize scope to include **judge** (and terminal snapshot strategy).
- [x] MRC-P0 execution: capture model in `github_indexing`, pass through `index_manifest_stats` → manifest + step columns.
- [x] Name PO-P1.4 / MRC-P1 owner for Voyage `index_embed` attempts (MRC-Q10).
- [x] Correct findings judge-step row claim.
- [x] Document `embed_batches=0` null semantics (MRC-Q8).

**Next:** `create-execution-plan` → `docs/models/model-run-capture/waves/`.
