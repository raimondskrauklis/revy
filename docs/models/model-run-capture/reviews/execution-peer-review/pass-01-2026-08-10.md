# Execution peer review — pass 1

**Plan folder:** `docs/models/model-run-capture/`  
**Date:** 2026-08-10  
**Scope:** full (MRC-P0–P3)  
**Reviewer:** execution-peer-review skill  
**Baseline:** [MODEL_RUN_CAPTURE_FINDINGS.md](../../MODEL_RUN_CAPTURE_FINDINGS.md), [MODEL_RUN_CAPTURE_GENERAL_PLAN.md](../../MODEL_RUN_CAPTURE_GENERAL_PLAN.md), [waves/README.md](../../waves/README.md), [architecture pass-01](../../architecture-peer-review/pass-01-2026-08-10.md), codebase paths cited below  

## Verdict

Review complete: 0 critical, 2 high, 5 medium/low.  
**BLOCK phase-execution:** yes — P1.3 must specify how judge `ModelRef` reaches `record_judge_pipeline_step`; P2.3 must name all MRC-Q9 terminal/failure hook sites (or a single finalize helper).

## Prior pass

First pass.

## Rollup (critical / high)

| Severity | Phase | Title |
|:---|:---|:---|
| high | MRC-P1 | P1.3 omits `model_ref` return path from `record_review_run_judge_status` to `reconcile_tasks` |
| high | MRC-P2 | P2.3 terminal writer hooks incomplete for MRC-Q9 failure paths (index/review/publish workers + central finalize) |

## Per-phase findings

### MRC-P0 — `waves/MODEL_RUN_CAPTURE_P0_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | P0.1 sets embedding fields in `_set_index_manifest_stats`; P0.3 expects `embedding_skipped_reason` but no subphase assigns it. Clarify: `record_index_pipeline_step` derives `embedding_skipped_reason=reused_chunks_only` when `embed_batches==0` in stats (do not set in indexing). |
| low | clarity | P0.1 "first batch only" — `github_indexing` calls `embed_texts` once per job (`github_indexing.py:598-601`); wording is fine but could note single call site today. |

**Verified OK:** Four subphases with What/Files/Deliverable; phase gate runs `ruff` + pytest on cited modules; no migration (manifest JSONB only); aligns with general plan P0 + MRC-Q8; `stash_pipeline_github_check_run_id` pending-step trap cited (`github_pipeline_trace.py:394-403`); out-of-scope defers P1 attempts.

### MRC-P1 — `waves/MODEL_RUN_CAPTURE_P1_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| high | codebase | P1.3 lists `record_judge_pipeline_step` + `github_finding_judge.py`, but `reconcile_tasks` calls `record_judge_pipeline_step` **without** model args (`reconcile_tasks.py:129-137`). `model_ref` is resolved inside `record_review_run_judge_status` (`github_finding_judge.py:480`) and not returned. Execution must add: return `ModelRef` (or provider/model_id tuple) from judge entry and pass into `record_judge_pipeline_step`. |
| medium | cross-cutting | P1.4 depends on PO-P1.2 publish instrumentation (`PIPELINE_OBSERVABILITY_P1_EXECUTION.md` P1.2). Deploy note mentions coordination but no **human gate** if PO-P1 not merged — add explicit "land PO-P1.2 first or implement equivalent recorder in P1.4". |
| medium | execution contract | P1.1 wires `request_model` from settings at Voyage call time; parity with MRC-P0 manifest relies on same read. Prefer passing captured `embedding_model` from `index_manifest_stats` into recorder kwargs to avoid drift if indexing ever threads explicit model. |
| low | clarity | `test_voyage_embeddings_observability.py` is **new** (mirrors PO-P1.4) — OK; not present on `main` yet. |

**Verified OK:** Five subphases; MRC-Q10 PO-P1.4 ownership + P1.2 parity test; judge step gap matches `record_judge_pipeline_step` today (`github_pipeline_trace.py:706-713`); review regression cites existing `test_review_observability_po_v3.py`; phase gate covers all touched modules.

### MRC-P2 — `waves/MODEL_RUN_CAPTURE_P2_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| high | general plan | MRC-Q9 requires terminal snapshot on **publish complete or pipeline failure hook**. P2.3 lists `github_publish.py` + `reconcile_tasks.py` only. Missing explicit hooks: `index_tasks.py` (index failure), `review_tasks.py` (review failure), `publish_tasks.py` (publish failure via `finalize_pipeline_github_check_for_publish_job`), and/or central `_finalize_pipeline_github_check` (`github_pipeline_trace.py:1060+`) to avoid missing a failure path. |
| medium | execution contract | P2.3 wording "after judge, before/after publish" is ambiguous. Successful path: write snapshot after publish step recorded (`github_publish.py` / `publish_tasks.py`), not in `reconcile_tasks` (judge completes there but publish follows async). |
| low | clarity | P2.1 reuses `test_pipeline_observability_schema.py` — exists; new revision must chain after `2026_08_10_1200_0031_pipeline_observability` (`down_revision` head). |

**Verified OK:** Four subphases; handwritten Alembic + LOOP pause on P2.1; `build_models_snapshot` new module; staging metrics extension matches general plan `GROUP BY step_type, provider, request_model`; MRC-Q7 retrieve defer explicit; `test_pipeline_observability_staging_metrics.py` exists.

### MRC-P3 — `waves/MODEL_RUN_CAPTURE_P3_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | P3.1 deliverable is `ruff check` only on schemas — weak alone; mitigated by P3.2/P3.3 pytest in phase gate. |
| low | clarity | P3.4 doc-sync touches staging-validation README — appropriate for final phase; human gate for staging dogfood is non-blocking — OK. |

**Verified OK:** Four subphases; `PipelineRunResponse` / `get_pipeline_trace_for_review_run` paths exist (`schemas/github_pipeline.py`, `github_pipeline_trace.py:882-946`); flatten manifest `embedding_*` matches general plan P3; no FE/i18n scope creep; final phase includes doc-sync table.

## Open questions (max 3)

1. **Snapshot on `neutral` finalize** (review skipped after index): write partial snapshot with embedding only, or defer until explicit terminal?
2. **PO-P1 merge order:** Single PR combining PO-P1 + MRC-P1, or MRC-P1.1 idempotent if PO-P1.4 already landed?
3. **Judge disabled/skipped:** Should judge step row omit `model_id` or record `skipped` in snapshot?

## Execution contract checklist

- [x] Header links general plan + findings (all phase files)
- [x] Goal + decisions locked per phase
- [x] Out of scope lists later phases
- [x] 3–6 subphases per phase (4–5 each)
- [x] Subphases have What / Files / Deliverable
- [ ] **All deliverables** have verify command — P3.1 schema-only ruff (minor)
- [x] Phase gate pytest blocks present
- [x] Migration subphase (P2.1) + handwritten + staging pause
- [x] Human gate on P3 staging dogfood (non-blocking)
- [x] Next → following file / program complete
- [x] No TBD / open options in execution files
- [ ] **MRC-Q9 failure hooks** fully enumerated (P2.3 gap)
- [ ] **Judge model_ref plumbing** specified (P1.3 gap)
- [x] Final phase doc-sync (P3.4)

**Next:** Fix agent updates P1.3 + P2.3 per rollup → re-review (delta) or proceed after human accepts gaps → `phase-execution` from [MODEL_RUN_CAPTURE_P0_EXECUTION.md](../../waves/MODEL_RUN_CAPTURE_P0_EXECUTION.md).
