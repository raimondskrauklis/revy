# Execution peer review — pass 2

**Plan folder:** `docs/github-onboarding/`  
**Date:** 2026-09-19  
**Scope:** full (P0–P4) — fresh-eyes re-review after pass 1 (author’s own plan)  
**Reviewer:** execution-peer-review skill  
**Baseline:** [GITHUB_ONBOARDING_FINDINGS.md](../../GITHUB_ONBOARDING_FINDINGS.md), [GITHUB_ONBOARDING_GENERAL_PLAN.md](../../GITHUB_ONBOARDING_GENERAL_PLAN.md), [README execution table](../../README.md), [architecture pass-02](../architecture-peer-review/pass-02-2026-09-19-delta.md), codebase paths cited below  

## Verdict

Review complete: **0 critical**, **0 high**, **2 medium**, **8 low**.  
**BLOCK phase-execution:** **no** — Q20/Q21 and LOOP order still match findings; remaining items are implement-time Files lists and cookie/TestClient details.

## Prior pass

[pass-01-2026-09-19.md](./pass-01-2026-09-19.md) — **BLOCK phase-execution: no** (0 critical, 0 high). Mediums: keep `GITHUB_OAUTH_PUBLIC_BASE`; update `checklistSteps.test.ts` in P1.5. This pass keeps the first and **widens** the second (more `installationCount` mocks).

## Rollup (critical / high)

| Severity | Phase | Title |
|:---|:---|:---|

None.

## Per-phase findings

## P0 — `docs/github-onboarding/GITHUB_ONBOARDING_P0_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| low | codebase | `APP_PUBLIC_URL` is SPA (`backend/.env.example:59` `http://localhost:5173`; prod example `https://revy.createit.digital`). Local hops are `:8000`. `GITHUB_OAUTH_PUBLIC_BASE` is required locally; prod happens to share the host. Do not collapse onto `APP_PUBLIC_URL`. |
| low | execution | HMAC key = `GITHUB_CLIENT_SECRET` is an execution lock (findings only said server-side). Rotating the GitHub client secret invalidates in-flight install `state` (TTL 1800s) — fail-closed is correct. |

**Verified OK:** Q8/Q12/Q13–Q14/Q20 in locked list; hops + persist out to P1; live paste P4; P0.0 SSOT currently still `judge-thinking-blocks` (`.revy/review-context.json`) so the switch is real work; target-config SPA URLs still at `GITHUB_APP_TARGET_CONFIG.md:26-28,43`; settings have `github_app_id` only (`config.py:86`); client-secret unused comment at `.env.example:73` and deploy example `:55`; 4 subphases; phase gate; Next → P1.

## P1 — `docs/github-onboarding/GITHUB_ONBOARDING_P1_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | codebase | P1.5 lists `checklistSteps.test.ts` but **not** other `ChecklistContext` mocks. `SetupChecklistWidget.test.tsx:85` treats `installationCount: 1` as “all steps complete”; `DashboardPage.test.tsx` and `hooks.ts:41` still derive count from `installations.length`. Changing the predicate without updating those mocks/context leaves a green widget test that still means “row exists”. Add `verified_at` to the context (or replace `installationCount` for checklist only) and list those test files in P1.5. |
| low | execution | Stash cookie: lock **Secure** when the Setup URL is `https` (prod). SameSite=Lax is already locked. |
| low | contract | TestClient should use `follow_redirects=False` so the P1.3 “still 302s” assertion is not eaten by a follow to GitHub. |

**Verified OK:** Alembic head `revision = "2026_09_16_2100_0033_github_pull_request_merged"` matches P1.1 `down_revision`; Q20 no JWT; Q21 column + backfill + checklist in this phase; Q15 new `bind_github_installation` not naive create; Q17 `enqueue_installation_repository_sync` exists (`github_repositories.py:253`); Q18 `workspace_has_feature`; `GET /app/installations/{id}` is new (`test_github_api.py` only covers JWT + list repos); 5 subphases; FE+BE gates; LOOP pause after migration.

## P2 — `docs/github-onboarding/GITHUB_ONBOARDING_P2_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| low | execution | P2.2 marks `InstallationsPage.test.tsx` **(new)** — file already exists (`frontend/src/features/installations/pages/InstallationsPage.test.tsx`). Extend it; mock `connectInstallation` next to `fetchInstallations` / `registerInstallation`. |
| low | execution | `errors.plan_upgrade_required` already exists in EN **and** LV (`en.json:711`, `lv.json:711`); toast already uses `errors.${code}` (`toasts.ts:44`). P2.4 is wiring start-connect 403 to that key, not inventing a new toast mechanism. |

**Verified OK:** start-connect path `POST …/installations/connect` does not collide with `POST ""` create; gates match create (`admin:users` + `installations.create`); Q4 admin CTA / member table; Q6 fallback copy; Q9/Mode B out; Setup not on SPA; 4 subphases; FE+BE gates; Next → P3.

## P3 — `docs/github-onboarding/GITHUB_ONBOARDING_P3_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | P3.4 Configure UI lives in `InstallationsTable.tsx` (no test file today). Gate `npm test -- installations` hits `InstallationsPage` / summary / integration card only — it will not fail if the Configure href is missing. Add `InstallationsTable.test.tsx` (or extend `InstallationsPage.test.tsx`) asserting `verified_at: null` → link `https://github.com/settings/installations/{github_installation_id}`. |
| low | execution | P3.3 PUTS verify on `test_github_installations.py` (service). P2 used a dedicated `test_github_connect_routes.py` for the HTTP surface. Prefer a route test that `POST …/verify` is admin-gated (mirror `test_github_repository_routes.py` for sync). |

**Verified OK:** live `list_installation_repositories` reused (`github_api.py:189`); not local `github_repositories`; no saas-base predicate; checklist not rewired; Q6 verify after fallback; Redirect-on-update re-enters Callback then verify; 4 subphases; FE+BE gates; Next → P4.

## P4 — `docs/github-onboarding/GITHUB_ONBOARDING_P4_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| low | docs | Pipeline trap row still says OAuth install UI **not shipped** (`REVIEW_PIPELINE_FINDINGS.md:88`) in addition to Q10 **reopened** (`:151`). P4.3 table names Q10 shipped; grep “stale type installation id” should also flip that trap row so both cannot stay. |

**Verified OK:** Q12 live paste + human gate; saas-base experiment-only; Q1/Q2 out; `changelog.json` exists; Next none; 3 subphases; LOOP stops on P4.1+P4.2.

---

## Execution contract checklist

- [x] Header links general plan + findings (all five files)
- [x] Goal + locked decisions per phase
- [x] Out of scope lists later phases
- [x] 3–6 subphases (P0:4, P1:5, P2:4, P3:4, P4:3)
- [x] What / Files / Deliverable
- [x] Phase gates copy-paste (P4 human + json.tool)
- [x] Migration P1.1 handwritten + LOOP pause; `down_revision` = current head `0033`
- [x] Human gates on P4 ops/dogfood
- [x] Next chain; P4 Next none + doc-sync
- [x] No TBD / no choose-A-or-B
- [x] FE gates on P1–P3
- [x] Q20 HMAC-only Setup/Callback
- [x] Q21 `verified_at` + backfill + checklist in P1
- [ ] P1.5 Files include all `installationCount` checklist mocks
- [ ] P3.4 has a test that asserts the Configure href

## Open questions

None blocking.

**Next:** `phase-execution` from P0. Fix agent may add the two Files/test lines above; they are not BLOCK.


