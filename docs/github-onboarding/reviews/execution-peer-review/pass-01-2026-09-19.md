# Execution peer review — pass 1

**Plan folder:** `docs/github-onboarding/`  
**Date:** 2026-09-19  
**Scope:** full (P0–P4)  
**Reviewer:** execution-peer-review skill  
**Baseline:** [GITHUB_ONBOARDING_FINDINGS.md](../../GITHUB_ONBOARDING_FINDINGS.md), [GITHUB_ONBOARDING_GENERAL_PLAN.md](../../GITHUB_ONBOARDING_GENERAL_PLAN.md), [README execution table](../../README.md), codebase paths cited below  

## Verdict

Review complete: **0 critical**, **0 high**, **2 medium**, **3 low**.  
**BLOCK phase-execution:** **no** — Q20 HMAC-only hops and Q21 `verified_at` + backfill + checklist switch are in P1; LOOP order matches general-plan depends.

## Prior pass

First execution pass — n/a.

## Rollup (critical / high)

| Severity | Phase | Title |
|:---|:---|:---|

None.

## Per-phase findings

## P0 — `docs/github-onboarding/GITHUB_ONBOARDING_P0_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution | `GITHUB_OAUTH_PUBLIC_BASE` is not a findings Q#; it is the only way to build Q12 URLs because `APP_PUBLIC_URL` is the SPA (`localhost:5173`) while local hops are `:8000`. Keep it; do not substitute `APP_PUBLIC_URL`. |
| low | clarity | HMAC key = `GITHUB_CLIENT_SECRET` is an execution lock (findings only said server-side). Fine; fail-closed when empty. |

**Verified OK:** P0.0 SSOT + Greptile `--check` + `test_generate_greptile_files.py`; settings fields named; target-config SPA URLs exist today (`GITHUB_APP_TARGET_CONFIG.md:26,43`) and P0.3 replaces them; live paste stays P4; 4 subphases; no JWT hops.

---

## P1 — `docs/github-onboarding/GITHUB_ONBOARDING_P1_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | codebase | `checklistSteps.test.ts` still treats `installationCount: 1` as Connect complete. P1.5 files list that test — must change the complete predicate or a P1-only merge still looks green in unit tests. |
| low | contract | Alembic `down_revision` = `0033` matches current head file `2026_09_16_2100_0033_github_pull_request_merged`. Handwritten + LOOP pause on P1.1. |

**Verified OK:** Q20 no `get_current_user`; TestClient without Bearer; SameSite=Lax cookie; Q21 column + backfill + FE/checklist in **this** phase; new inserts `verified_at` null; Q15 idempotent bind not naive create; Q17 enqueue after bind; 5 subphases; FE gate present.

---

## P2 — `docs/github-onboarding/GITHUB_ONBOARDING_P2_EXECUTION.md`

**P2 — no gaps found.**

**Verified OK:** Start-connect is JWT + `admin:users` + `installations.create` (same as `installations.py:51,56` today); Install URL minted from P0 HMAC; Setup not on SPA; Q6 fallback demoted; `errors.plan_upgrade_required` matches `showDomainErrorToast` → `errors.${code}`; FE+BE gates; 4 subphases.

---

## P3 — `docs/github-onboarding/GITHUB_ONBOARDING_P3_EXECUTION.md`

**P3 — no gaps found.**

**Verified OK:** Sets `verified_at` via existing `list_installation_repositories`; does not rewire checklist (P1); no saas-base product predicate; Q6 `POST …/verify`; Configure URL; Redirect-on-update re-enters Callback; 4 subphases.

---

## P4 — `docs/github-onboarding/GITHUB_ONBOARDING_P4_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| low | contract | Human gates on live App paste + experiment memo — LOOP correctly stops. Doc-sync table includes Q10 **shipped** and `changelog.json`. |

**Verified OK:** saas-base is experiment-only; no saas-base codebase; Next none; 3 subphases.

---

## Execution contract checklist

- [x] README execution table matches P0–P4
- [x] One file per phase; 3–6 subphases
- [x] What / Files / Deliverable + phase gates
- [x] Migration P1.1 handwritten + LOOP pause
- [x] Human gates on P4 ops/dogfood
- [x] P0.0 SSOT + Greptile + Bugbot
- [x] Final phase doc-sync
- [x] No TBD / no choose-A-or-B
- [x] FE gates on P1–P3
- [x] Q20 HMAC-only Setup/Callback
- [x] Q21 `verified_at` + backfill + checklist in P1

## Open questions

None blocking.

**Next:** `phase-execution` from P0. Medium items are implement-time (keep `GITHUB_OAUTH_PUBLIC_BASE`; update `checklistSteps.test.ts` predicate in P1.5).
