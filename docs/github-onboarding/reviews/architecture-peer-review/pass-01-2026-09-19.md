# Architecture peer review — pass 1

**Plan folder:** `docs/github-onboarding/`  
**Date:** 2026-09-19  
**Scope:** full  
**Reviewer:** architecture-peer-review skill  
**Baseline:** [GITHUB_ONBOARDING_FINDINGS.md](../../GITHUB_ONBOARDING_FINDINGS.md), [GITHUB_ONBOARDING_GENERAL_PLAN.md](../../GITHUB_ONBOARDING_GENERAL_PLAN.md), codebase paths cited below  

## Verdict

Review complete: **3 critical**, **4 high**, **6 medium/low**.  
**BLOCK create-execution-plan:** **yes** — P0/P1 must lock a public Setup + Callback hop that keeps GitHub query params; persist on Redirect-on-update must be idempotent for the same workspace; product verify is ≥1 granted repo, not a hardcoded `saas-base` check.

## Prior pass

First pass — n/a.

## Verified (docs match codebase)

| Claim | Code evidence |
|:---|:---|
| Manual POST create does **not** call GitHub | `create_github_installation` writes payload fields only — `backend/app/services/github_installations.py:86-120`; API `post_workspace_installation` — `backend/app/api/v1/workspaces/installations.py:40-71` |
| POST requires `admin:users` + plan gate `installations.create` → `plan_upgrade_required` | `installations.py:51,56`; `backend/app/core/plan_gates.py:20-56` |
| Unknown `installation` webhook logs orphan, 200, no row | `apply_installation_webhook_event` — `github_installations.py:159-164`; HTTP 200 in `backend/app/api/v1/webhooks/github.py:21-74` |
| Unknown `installation_repositories` webhook also orphans | `github_repository_orphan_installation` — `backend/app/services/github_repositories.py:155-160` |
| `github_installation_id` globally unique | `GitHubInstallationORM.github_installation_id` unique — `backend/app/models/github_installation.py:24`; conflict copy already splits same-workspace vs other-workspace — `github_installations.py:26-33` |
| App JWT + installation tokens + **list repos** exist; **no** `GET /app/installations/{id}` | Token POST — `github_api.py:163-186`; `list_installation_repositories` — `github_api.py:189-223`; only `/app/installations/{id}/access_tokens` |
| No App slug / OAuth client in settings | `config.py:85-90` (`github_app_id`, PEM path, webhook secret, `revy_bot_login` only); `.env.example:73` “Client secrets … not used by the backend” |
| Checklist `connect_integration` = row count, not GitHub verify | `frontend/src/features/dashboard/checklistSteps.ts:43-47`; count from `fetchInstallations` — `frontend/src/features/dashboard/hooks.ts:34-42` |
| Stale “until webhook phase” i18n | EN `frontend/src/i18n/locales/en.json:581`; LV `lv.json:581` |
| Manual form is admin-only; `/installations` is not `RequirePermission` | `InstallationsPage.tsx:21,77-79`; route `routerInstance.tsx:97-99` |
| Mode B never reaches authenticated shell | `ProtectedRoute` pending_approval → `/pending-approval` — `ProtectedRoute.tsx:41-43` |
| Status enum has no pending-verify | `GitHubInstallationStatus` `active` \| `suspended` \| `removed` — `backend/app/constants/enums.py:72-77`; create always sets `active` — `github_installations.py:108` |
| Same-workspace duplicate create is **409**, not upsert | `_installation_conflict` when `existing.workspace_id == workspace_id` — `github_installations.py:31-32` |
| R1 full sync is an explicit admin enqueue, not automatic on create | `POST …/sync-repositories` — `installation_repositories.py:51-87`; `enqueue_installation_repository_sync` |
| Target config currently wants OAuth-on-install **and** Setup URL | `docs/utils/GITHUB_APP_TARGET_CONFIG.md:26-28,39-44` — GitHub docs confirm those checkboxes cannot be live together |
| Pipeline Q10 already reopened to this folder | `docs/review-pipeline/REVIEW_PIPELINE_FINDINGS.md:151` |
| `review_autostart_enabled` exists (Q11 defer is correct) | `workspaces.review_autostart_enabled`; R8 uses it in `review_pipeline.py` |
| Protected login drop of query string | `Navigate … state={{ from: location.pathname }}` — `ProtectedRoute.tsx:25-26`; `LoginPage` uses `from` as Keycloak `redirectUri` — `LoginPage.tsx:13,36` + `AuthContext.tsx:114-120` |
| Placeholder Callback route does **not** exist | Target config `/settings/integrations/github/callback`; settings routes have no `github/callback` — `routerInstance.tsx:123-144` |
| No backend OAuth/setup routes | no `oauth` / `callback` under `backend/app/api` |

## Rollup (critical / high)

| Severity | Section | Title |
|:---|:---|:---|
| critical | P0 / P1 | Setup/Callback cannot be the protected SPA; GitHub query params are dropped today |
| critical | P1 / P3 | Redirect-on-update will 409 if persist is naive `create_github_installation` |
| critical | P3 / Q5 | Product verify is ≥1 granted repo; `saas-base` is dogfood-only (P4) |
| high | P1 | First `installation` + `installation_repositories` webhooks are expected orphans; enqueue R1 sync after persist |
| high | P1 | Persist (and start-connect) must keep `installations.create` plan gate + plan copy |
| high | P1 / P3 | Shipping P1 `status=active` row completes today’s checklist and violates Q5 until P3 |
| high | P0 | Lock concrete Setup URL + Callback URL (host + path) and the two `state` values (install vs OAuth) |

## Per-section findings

## Cross-cutting

| Severity | Area | Finding |
|:---|:---|:---|
| high | findings ↔ plan | Locked Q1–Q9, Q11 match the general-plan thesis. **Missing locks** that the plan treats as already closed: (1) signed-`state` TTL (findings say “pick an explicit TTL”; CodeRabbit ~30 min is cited, not locked), (2) canonical Setup + Callback URLs, (3) product vs dogfood verify target. General plan “No open calibration” is false until those are in the registry. |
| medium | general plan | Header links findings; program README exists. P0–P4 each have Goal / Scope / Deliverables / Depends on. Next step `create-execution-plan` matches findings. |
| medium | codebase | New GitHub user OAuth is genuinely new (no client id/secret, no routes). Reuse `create_github_installation`, `list_installation_repositories`, unique constraint, plan gate, R1 `sync_installation_repositories` — name them in execution, do not reimplement. |
| low | findings | “Creates the row **before** or **as** GitHub redirects back” cannot be “before”: `github_installation_id` is unknown until GitHub creates the install. Persist on Setup/Callback (as redirect) is the only schema-compatible reading. |
| low | docs | Pipeline catalog still says R8 **active**; Q10 trap copy still says “until later phase”. P4 doc-sync owns that. |

**Verified OK:** Track 2 (one platform App) vs Manifest reject; Q8 Setup URL + OAuth-from-setup vs GitHub OAuth-on-install (confirmed in GitHub “Modifying a GitHub App registration”); Q9 Keycloak stays; Q4 admin start matches POST; Q6 manual form remains; Q11 no Enable UI; no new pipeline tables; EN+LV + `--app-*` called out; secrets stay server-side; saas-base is dogfood repo not app; handwritten Alembic only if a column is added.

---

## P0 — Foundations

| Severity | Area | Finding |
|:---|:---|:---|
| critical | codebase / general plan | Target config placeholders are SPA: Setup `https://revy.createit.digital/installations?setup=1`, Callback `/settings/integrations/github/callback` (route **missing**). `/installations` sits behind `ProtectedRoute`, which saves **`location.pathname` only**. After GitHub SSO (findings: state must survive ~30 min) a Keycloak re-login drops `installation_id`, `setup_action`, and `state`. That is the happy path dying, not an edge. **Lock:** public backend GET Setup + GET Callback (302), cookie or signed blob carrying `installation_id` across the OAuth hop; persist only on authenticated Revy user matching `state.user_id`. Do not copy the current SPA URLs into the live App. |
| high | general plan | P0 deliverable “target config no longer asks for mutually exclusive GitHub checkboxes” is necessary (OAuth-on-install **unchecked**, Setup URL + Redirect on update **on**) but **not sufficient**. Also lock exact URLs per env (API host, path), `GITHUB_APP_SLUG`, `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET` (server), and **two** `state` values: install-URL HMAC (workspace, user, nonce, TTL) vs OAuth CSRF/`redirect_uri`. GitHub: if you need a chosen Callback URL, you must **not** use OAuth-on-install and must pass `redirect_uri` on the web application flow. |
| medium | general plan | “State round-trips without a DB row” is viable (HMAC/JWT) **if** nonce replay and Callback stash of `installation_id` are specified. If nonce consume is required, that is a small table or cache — contradicting “no DB row”. Lock one. |
| medium | general plan | P0 mixes code (settings, signed state, `.env.example`) with live GitHub App dashboard edits. Live slug/URL paste belongs in P4 ops gate (already there). P0 should ship docs + env **shape**; not imply the live App is already updated. |
| low | general plan | P0 should also patch `deploy/env-examples/backend.env.production.example` (today only `GITHUB_APP_ID` / PEM / webhook). |

**Verified OK:** No slug in env today; Install URL cannot be built from App ID; client-secret comment is accurate; Q8 checkbox conflict is real.

---

## P1 — Setup callback + installer OAuth + persist

| Severity | Area | Finding |
|:---|:---|:---|
| critical | codebase / general plan | Findings edge 5 + P3 “Redirect on update re-enters verify” require Setup to run **again** for the **same** `github_installation_id`. Today `create_github_installation` **409s** “already registered for this workspace”. Naive P1 persist makes “add saas-base later” a conflict instead of re-verify. **Lock:** same workspace + same GitHub id → idempotent bind, then verify; other workspace → keep unique conflict + Greptile-style copy. |
| high | codebase / general plan | P1 deliverable “after this path an orphan `installation` webhook is a bug” **contradicts** findings edge 1. GitHub sends `installation` + `installation_repositories` **before** the browser hits Setup. Both handlers already no-op (`github_installation_orphan`, `github_repository_orphan_installation`). That race stays. After persist, **enqueue existing** `sync_installation_repositories` (R1); do not wait for a webhook that already fired. Do **not** “fix” orphans by creating rows from webhooks (reopens spoof). |
| high | general plan | Findings edge 7: plan 403 must not look like GitHub failure. P1 scope omits `require_plan_feature("installations.create")`. Persist without it = GitHub install succeeds, bind 403, row never created, webhooks stay orphan. **Also gate start-connect** so the user is not sent to github.com on free plan. Map `plan_upgrade_required` in wizard copy. |
| high | findings / Q5 | P1 Out: “checklist rule”. Create sets `status=active`. Checklist completes on `installationCount > 0`. A P1-only merge on `main` marks Connect GitHub complete without repo verify — locked Q5. P1 must not look “connected” until P3 verify (separate status/column, or P1+P3 same ship, or checklist ignores unverified rows). `active`/`suspended`/`removed` has no pending-verify today — if you add one, that is a handwritten Alembic subphase. |
| medium | general plan | “User-token check that this GitHub user can admin the installation” — lock the GitHub call (`GET /user/installations` and require the id present). Discard the user token after the check (no persist). App JWT `GET /app/installations/{id}` is **new** (not in `github_api.py`); needed for account metadata, not sufficient alone (findings). |
| medium | general plan | No-`state` / OAuth-without-install / already-linked copy are in scope and match findings 11–13. Name they are **guided HTML + API errors**, not persist. |
| low | clarity | “Setup handler” is two hops (Setup GET → OAuth authorize → Callback GET → persist). Spell that in the phase so execution does not persist on the raw Setup query. |

**Verified OK:** Unique id conflict across workspaces already implemented; webhook will not create the link; POST create is the only persist path today.

---

## P2 — Connect wizard UI

| Severity | Area | Finding |
|:---|:---|:---|
| medium | general plan | P2 depends on P1 because “GitHub return 404s if Setup URL is missing” — that 404 is the **live App** Setup URL (P0 docs / P4 ops), not the P1 persist code. UI can ship pointing at Install URL, but dogfood will bounce until the App field is set. Keep the dependency; don’t treat it as a missing backend route 404 inside Revy. |
| medium | codebase | `/installations` is visible to all members; register form is admin-only. Q4: members must not **start** connect (hide Install Revy + fallback form), but may see verified status. Checklist step `connect_integration` is `available: true` for everyone — fine if href is view-only for non-admins. |
| low | general plan | Mode B “never reaches the wizard” is already `ProtectedRoute`. P2 should not add a second status gate; just don’t put Setup on a public route without the P0 hop. |
| low | findings | Stale webhook i18n is correctly in P2 (EN+LV). Fallback form (Q6) should say admin/dev fallback, not “until webhooks ship”. |

**Verified OK:** Stepper after `me.status === active` matches Track 1; Install URL + cards (org owner, SSO, Only select repositories, authorize ≠ install) match Greptile steal-list; Q9 out of scope.

---

## P3 — Verify granted repo + honest checklist

| Severity | Area | Finding |
|:---|:---|:---|
| critical | general plan / Q5 | Q5: checklist = installation API 200 **and ≥1 granted repo visible**; `(saas-base for dogfood)` is an example. P3 deliverable “missing **saas-base** is Configure-on-GitHub, not success” would fail every tenant that is not `raimondskrauklis/saas-base`. **Product:** ≥1 granted repo (or user-confirmed repo from the GitHub allow-list) + Configure deep-link `/settings/installations/{id}` when the list is empty. **Dogfood:** saas-base visibility is a **P4 experiment gate**, not the product predicate. |
| high | codebase | “Wait/retry on empty sync” must define the source. Local `github_repositories` stays empty until R1 sync (webhooks were orphaned). Prefer **live** `list_installation_repositories` (already shipped) for verify, **and** enqueue R1 sync so reviewer UI is not empty. Empty GitHub list → Configure, not checklist complete (edge 14). |
| medium | general plan | Checklist today is `installationCount > 0` with no verified field. Lock: live check on dashboard vs persisted `verified_at` (migration). Either is fine; execution cannot invent a third table (cross-cutting: no new pipeline tables). |
| medium | general plan | Typed junk completing the checklist is the **manual fallback (Q6)** as well as the happy path. P3 must apply the same verify rule to fallback-created rows, or the fallback remains a lie. |
| low | clarity | Configure URL for user vs org installs differs (`/settings/installations/{id}` vs org path). Vercel-style deep-link can use `https://github.com/settings/installations/{id}` which GitHub redirects. |

**Verified OK:** Q11 correctly out; R8 autostart already covers every granted repo; list-repos API exists.

---

## P4 — Dogfood + Q10 close

| Severity | Area | Finding |
|:---|:---|:---|
| medium | general plan | Human/ops gate is in the right phase (live App Setup/Callback, experiment table, Q10 shipped). Blocked until P0 URL lock is pasteable into the GitHub App form. |
| low | general plan | “Unrelated merges” out of scope is good. Closing pipeline Q10 should flip `REVIEW_PIPELINE_FINDINGS.md` Q10 to **shipped** and the trap row “OAuth install UI not shipped” — don’t leave both “reopened” and “shipped”. |
| low | findings | Experiment table is a solid P4 gate. First row “never types installation id” allows Q6 fallback to remain for admins as long as it is not the documented happy path. |

**Verified OK:** saas-base product code stays out; Actions secrets out; Marketplace out.

---

## Open questions (max 3)

1. **State TTL:** lock a number in the findings registry (recommend **1800s** to match the CodeRabbit bar already cited). Until locked, P0 “survives SSO” is not testable.
2. **Verify persistence:** live GitHub list on each checklist render vs `verified_at` column (handwritten Alembic). Pick one in the registry so P3 execution does not add a pipeline table.

No third blocking product question if criticals above are applied (backend Setup/Callback; product ≥1 repo; idempotent same-workspace persist).

## Pre-execution checklist

- [ ] Lock Setup URL + Callback URL (API host + path) per env; uncheck OAuth-on-install; Redirect on update on
- [ ] Lock public backend hops so GitHub query params survive Keycloak / SSO (do not use `ProtectedRoute` pathname-only)
- [ ] Lock install `state` vs OAuth `state`; TTL (seconds); nonce consume yes/no
- [ ] Persist: idempotent same workspace+id; conflict other workspace; user-token association; discard user token
- [ ] After persist: enqueue R1 `sync_installation_repositories`; do not treat first orphan webhooks as a product bug
- [ ] `installations.create` on start-connect **and** persist; wizard copy for `plan_upgrade_required`
- [ ] Q5 product predicate ≥1 granted repo; saas-base only in P4 dogfood table
- [ ] Checklist (and fallback form) complete only after verify — including intermediate P1 merges
- [ ] Env: `GITHUB_APP_SLUG`, `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET` (server); `.env.example` + deploy example
- [ ] New GitHub helpers: `GET /app/installations/{id}`, user-token `GET /user/installations`; reuse `list_installation_repositories`
- [ ] Tests named in cross-cutting: spoofed id, missing state, OAuth-without-install, already-linked, empty repo list, Redirect-on-update idempotent
- [ ] No `--autogenerate`; FE EN+LV + `--app-*`

**Next:** fix agent updates findings + general plan per this file → architecture-peer-review pass 2 (delta) → **`create-execution-plan`** only when **BLOCK: no**.
