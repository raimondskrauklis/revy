# Architecture peer review — pass 2 (delta)

**Plan folder:** `docs/github-onboarding/`  
**Date:** 2026-09-19  
**Scope:** delta — pass-01 critical/high (Q12–Q19) after fix-agent edits  
**Reviewer:** architecture-peer-review skill  
**Baseline:** [GITHUB_ONBOARDING_FINDINGS.md](../../GITHUB_ONBOARDING_FINDINGS.md), [GITHUB_ONBOARDING_GENERAL_PLAN.md](../../GITHUB_ONBOARDING_GENERAL_PLAN.md) (post pass-01), codebase paths cited below  

## Verdict

Review complete: **0 critical**, **0 high**, **3 medium**, **2 low**.  
**BLOCK create-execution-plan:** **no** — pass-01 blockers are locked as Q12–Q19 and reshaped across P0–P3. Residuals are execution clarifications (no JWT on public hops; checklist/backfill sequencing).

## Prior pass

[pass-01-2026-09-19.md](./pass-01-2026-09-19.md) — **BLOCK create-execution-plan: yes** (3 critical, 4 high). Fix agent applied Q12–Q19.

## Pass-01 items

| ID | Status | Notes |
|:---|:---|:---|
| critical — SPA Setup/Callback drops query | **resolved** | Q12 public API GETs `/api/v1/github/setup` + `/callback`. Prod host matches nginx + `VITE_API_BASE_URL` (`https://revy.createit.digital/api/v1`). Local `localhost:8000`. P0 replaces SPA placeholders; P1 owns the hops; P2 forbids `ProtectedRoute` Setup. |
| critical — Redirect-on-update 409 | **resolved** | Q15 + P1 idempotent same workspace+id. Code today still 409s (`github_installations.py:31-32`) — that is the gap to implement, not a remaining lock hole. |
| critical — saas-base as product verify | **resolved** | Q16 + P3: ≥1 granted repo via live `list_installation_repositories`. saas-base is P4 experiment only. |
| high — first orphans + R1 enqueue | **resolved** | Q17; terminology; P1 Out: do not treat first orphans as a bug; enqueue existing `sync_installation_repositories`. |
| high — plan gate start + persist | **resolved** | Q18; P2 start-connect before leaving Revy; P1 persist. |
| high — P1 `active` row completes checklist | **resolved** (lock) | Q19 `verified_at`; checklist iff set; P1 insert null. Residual sequencing → medium below. |
| high — URLs + two `state` values | **resolved** | Q12–Q14: install HMAC vs OAuth CSRF; TTL **1800s**; no consume table. |
| open Q — TTL 1800s | **resolved** | Q14. |
| open Q — verify persistence | **resolved** | Q19 `verified_at` column (handwritten Alembic), not a pipeline table. |

## Verified (locks still match codebase)

| Claim | Code evidence |
|:---|:---|
| API JWT is `HTTPBearer`, not cookies | `backend/app/core/auth.py:30,192` — GitHub top-level GET to Setup/Callback will have **no** `Authorization` |
| Prod SPA+API same host | `deploy/env-examples/frontend.env.production.example:7`; `deploy/nginx/revy.createit.digital.conf:5-7` |
| Same-workspace create still 409 | `_installation_conflict` — `github_installations.py:31-32` |
| No `verified_at` yet | `GitHubInstallationORM` ends at `permissions_snapshot` — `github_installation.py:16-37`; FE type same — `frontend/src/features/installations/api.ts:5-16` |
| Checklist still row count | `checklistSteps.ts:43-47`; `hooks.ts` `installationCount: installations.length` |
| Plan gate exists for POST create | `plan_gates.py:20-56`; `installations.py:51` |
| List repos exists; `GET /app/installations/{id}` does not | `github_api.py:189-223` vs access-tokens only |
| Orphan both event types | `github_installations.py:159-164`; `github_repositories.py:155-160` |
| Target config SPA placeholders still live in that doc | `GITHUB_APP_TARGET_CONFIG.md:26,43` — P0 must replace (not paste live) |
| No `/api/v1/github` router yet | `backend/app/api/v1/__init__.py` — new include in P1 |

## Rollup (critical / high)

| Severity | Section | Title |
|:---|:---|:---|

None.

## Per-section findings

**Cross-cutting — pass-01 missing locks closed** (Q12–Q19 in registry; general plan “No open calibration” now true for those items).

| Severity | Area | Finding |
|:---|:---|:---|
| medium | findings | Q14 / terminology still say persist is bound to a **Keycloak session** / “authenticated Revy user”. Auth is Bearer-only (`HTTPBearer`). GitHub’s browser GET will not send a JWT. **Execution:** public hops validate HMAC install `state` (and OAuth `state`); bind `workspace_id` + `user_id` from that blob; **do not** `Depends(get_current_user)` on Setup/Callback. Start-connect (SPA) is the JWT + admin + plan gate. Leftover Q14 wording is not a product reversal of Q12. |
| medium | general plan | P1 **Deliverable** (“P1-only merge does not complete the checklist”) vs P1 **Out** (“checklist rule (P3)”). `checklistSteps.ts` still uses `installationCount`. Null `verified_at` does nothing until the FE (and list API) read it. **Execution:** change checklist + `GitHubInstallation` schema when the column lands (P1) **or** land P1+P3 together. Also **backfill** existing manual-register rows (null `verified_at` would mark every current workspace incomplete). |
| low | general plan | Next step still named pass 2; this pass clears it. |

**P0 — no new gaps** (env shape, Q12 URLs, Q8 checkboxes, deploy `.env` example, live App paste stays P4).

**P1 — no new gaps** besides the public-hop auth note above. Two hops, `GET /user/installations`, new App JWT GET, idempotent bind, R1 enqueue, `verified_at` null, plan gate on persist — all locked.

| Severity | Area | Finding |
|:---|:---|:---|
| low | execution | Signed cookie/blob for `installation_id` across OAuth: use **SameSite=Lax** (top-level GET from github.com). Strict would drop the stash. |

**P2 — no new gaps** (start-connect plan gate; members see status; Setup not on SPA; live App paste still P4). HMAC signing implies an authenticated start-connect API (secret stays server-side) — name it in execution; not a baseline hole.

**P3 — no new gaps** besides checklist sequencing/backfill above. Live list, Configure URL, Q6 same verify rule, no saas-base predicate.

**P4 — no new gaps** (ops paste Q12; experiment table includes Redirect-on-update + ≥1 repo; Q10 shipped here).

## Open questions

None blocking.

## Pre-execution checklist

- [x] Q12 Setup/Callback public API URLs (prod + local)
- [x] Public hops; not `ProtectedRoute` pathname-only
- [x] Install `state` vs OAuth `state`; TTL 1800s; no consume table
- [x] Idempotent same workspace+id; other workspace conflict; user-token association; discard user token
- [x] Enqueue R1 after persist; first orphans expected
- [x] `installations.create` on start-connect and persist
- [x] Product verify ≥1 granted repo; saas-base P4 only
- [x] `verified_at`; checklist not row count (end state)
- [x] Env slug + OAuth client id/secret; `.env.example` + deploy example
- [x] New helpers named; reuse `list_installation_repositories`
- [ ] Execution: no JWT on Setup/Callback (HMAC bind)
- [ ] Execution: checklist + API `verified_at` + backfill vs P1-only merge
- [ ] Tests listed in general-plan cross-cutting
- [ ] Handwritten Alembic for `verified_at`; EN+LV; `--app-*`

**Next:** **`create-execution-plan`** for P0–P4 (then `execution-peer-review`).
