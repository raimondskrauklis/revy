# Architecture peer review — pass 2

**Plan folder:** `docs/review-pipeline/pipeline-observability/`  
**Date:** 2026-08-10  
**Scope:** delta — pass-1 checklist resolutions + full re-verify of codebase citations  
**Reviewer:** architecture-peer-review skill  
**Baseline:** [PIPELINE_OBSERVABILITY_FINDINGS.md](../../PIPELINE_OBSERVABILITY_FINDINGS.md), [PIPELINE_OBSERVABILITY_GENERAL_PLAN.md](../../PIPELINE_OBSERVABILITY_GENERAL_PLAN.md), [pass-01-2026-08-10.md](./pass-01-2026-08-10.md)

## Verdict

Review complete: **0 critical**, **1 high**, **4 medium**, **2 low**.  
**BLOCK create-execution-plan:** **no** — pass-1 blockers are locked in findings + general plan; remaining gaps are execution clarifications (Bedrock module path, manual-index embed scope, `trigger_source` write site).

## Prior pass

[pass-01-2026-08-10.md](./pass-01-2026-08-10.md) — **BLOCK create-execution-plan: yes** (2 critical, 5 high). Fix agent updated findings + general plan per pre-execution checklist.

### Pass-1 resolution status

| Pass-1 finding | Status |
|:---|:---|
| P0 commit graph underspecified | **resolved** — findings §Commit graph + general plan cross-cutting |
| Attempt rows must survive Celery retry | **resolved** — boundaries #2–3 + retry note in findings |
| HTTP timeout re-ship in P0 | **resolved** — prerequisite PR #90; out of P0 scope |
| Price table no phase owner | **resolved** — `llm_pricing` in P0 (PO-Q14) |
| Attempts retention / O8 purge | **resolved** — FK cascade + purge extension in P0 |
| Bedrock in-scope | **resolved** — PO-Q13 locked |
| Nullable `review_run_id` on attempts | **resolved** — schema + reuse-trap table |
| `trigger_source` write path | **resolved** — PO-Q15 copy at enqueue |
| PO-Q9 log aliases | **resolved** — dual-emit through P3, remove P4 |
| Success-only steps overstated | **resolved** — findings summary + P2 framing |
| `error_class` vs `failure_class` | **resolved** — unified `failure_class` in terminology |
| P5 depends on P4 | **resolved** — depends on P3 |

## Verified (docs match codebase)

| Claim | Code evidence |
|:---|:---|
| No `timing_stats` / `token_rollup` / `failure_stage` / `failure_class` / `trigger_source` on review runs | `GitHubReviewRunORM` — `backend/app/models/github_review_run.py` (ends at `context_stats`) |
| No `github_llm_call_attempts` table/model | No matches under `backend/` |
| `context_stats` flushed pre-LLM, single worker commit | `github_review.py` ~1057–1071 `flush`; `review_tasks.py` ~89 `commit` |
| `_mark_failed` commits run failure + check only — no retrieve/review trace | `review_tasks.py` ~100–117 |
| HTTP timeout headroom on `main` | `config.py` ~324–328; `test_review_timeout_config.py` |
| `httpx.TimeoutException` → Celery retry | `worker_retries.py` ~42–43 |
| Bedrock is separate module, not `anthropic_review` | `llm_dispatch.py` ~37–70 → `bedrock_review.py` |
| Pipeline run exists for pipeline-trigger index only | `index_tasks.py` ~57–78 `ensure_pipeline_run_for_index_job` when `trigger_source in _PIPELINE_TRIGGERS` |
| `create_review_run` already loads latest completed index job | `github_review.py` ~635–639 — viable PO-Q15 copy site |
| Judge legacy log taxonomy ≠ planned enum | `anthropic_review._judge_profile_fallback_failure_class` → `parse`/`http`/`empty_body` (~480–485) |
| Purge deletes pipeline runs (cascade steps/artifacts) | `github_pipeline_trace.purge_old_pipeline_artifacts` ~966–977 |
| `PipelineStepStatus.processing` unused by step writers | `enums.py` ~139–145; writers use terminal `_create_completed_step` |
| API omits `model_id` | `GitHubReviewRunResponse` — `schemas/github_review.py` ~31–45 |

## Rollup (critical / high)

| Severity | Section | Title |
|:---|:---|:---|
| high | P1 | Bedrock is `bedrock_review.py`, not anthropic completion path — separate instrumentation |

## Per-section findings

## Cross-cutting

| Severity | Area | Finding |
|:---|:---|:---|
| medium | P0 / scope | **Manual index jobs** have no `pipeline_run_id` (`index_tasks.py` only creates runs for `_PIPELINE_TRIGGERS`). Required FK on attempts excludes Voyage embed rows for manual-index paths. Lock in execution: embed attempts **only when `pipeline_run_id` is set**, or document manual index as out of pipeline-observability scope. |
| medium | P0 | PO-Q15 “copy at enqueue” — two call sites (`review_pipeline.prepare_review_after_index`, `installation_review.post_review_pull_request_revision`). Both funnel through `create_review_run`, which already fetches `get_latest_completed_index_job`; set `trigger_source` there (not only pipeline module). |
| low | findings | Line 7 cites pass-01 as evidence before pass-02 existed — harmless; optional doc-sync to “post pass-1 fixes”. |

**Verified OK:** Locked PO-Q1–Q15 parity across findings ↔ general plan; commit graph boundaries #1–6; DB-SSOT-first; handwritten Alembic; `failure_class` taxonomy listed; judge log mapping called out in P0.

---

## P0 — Foundations

**Pass-1 — no new gaps** (verified unchanged). Commit graph, `llm_pricing`, O8 purge extension, `LlmCallRecorder`, nullable `review_run_id`, PO-V3 HTTP-timeout case all present in general plan.

| Severity | Area | Finding |
|:---|:---|:---|
| medium | deliverables | PO-V3 needs attempt-row insert/update on timeout — success-path token population remains P1; execution should scope P0 recorder tests to checkpoint + timeout attempt durability only. |
| low | clarity | Add `GitHubReviewRunFailureClass` (or equivalent) to `enums.py` in P0 migration subphase — implied by taxonomy but not named in plan. |

---

## P1 — Unified LLM instrumentation

| Severity | Area | Finding |
|:---|:---|:---|
| high | general plan | “Bedrock uses same completion path when enabled” is **incorrect** vs codebase. `llm_dispatch` routes to `bedrock_review.complete_review` / `judge_finding` — separate boto Converse client. PO-Q13 “same `LlmCallRecorder`” is right; **instrument `bedrock_review.py` explicitly** in execution (shared recorder, not shared HTTP helper). |
| medium | general plan | Voyage scope correctly aligned with PO-Q11 (null tokens + batch count) — verified `voyage_embeddings.py` parses embeddings only. |

**Verified OK:** `_complete_chat` covers review + publish when callers pass `step_type`; judge Anthropic path has usage + `judge_llm_*` logs for dual-emit; Moonshot has `finish_reason` but no persisted usage.

---

## P2 — Incremental pipeline trace

**Pass-1 — no new gaps** (verified unchanged). Live `processing` → terminal helpers; depends on P0; abort visibility framing correct.

---

## P3 — Run-level rollups

**Pass-1 — no new gaps** (verified unchanged). `estimated_usd` consumes P0 `llm_pricing`; expose `model_id` on API noted; partial rollups + `_mark_failed` in scope.

---

## P4 — Staging metrics

**Pass-1 — no new gaps** (verified unchanged). Script patterns exist (`moonshot_staging_spend_metrics.py`, `judge_staging_spend_metrics.py`); PO-V5 shifts reconcile to attempt rows.

---

## P5 — OTel OTLP export

**Pass-1 — no new gaps** (verified unchanged). Depends on P3 only.

---

## Open questions (max 3)

1. ~~**Manual-index embed attempts**~~ — **locked PO-Q16:** omit when no `pipeline_run_id`.

## Pre-execution checklist

- [x] Pass-1 critical/high items locked in findings + general plan
- [x] Bedrock: not dropped; **deferred from P1** — separate `bedrock_review.py` follow-on (PO-Q13)
- [x] `trigger_source` set in `create_review_run` from fetched index job (PO-Q15)
- [x] Index-embed attempts gated on `pipeline_run_id` presence (PO-Q16)
- [x] P0 PO-V3 scoped to timeout/checkpoint durability (not full token capture)
- [x] `GitHubReviewRunFailureClass` enum named in P0 scope

**Next:** `create-execution-plan`.
