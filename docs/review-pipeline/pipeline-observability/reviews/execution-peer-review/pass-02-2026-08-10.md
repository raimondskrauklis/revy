# Execution peer review — pass 2

**Plan folder:** `docs/review-pipeline/pipeline-observability/`  
**Date:** 2026-08-10  
**Scope:** delta — pass-1 high fixes + full re-verify P0–P5  
**Reviewer:** execution-peer-review skill  
**Baseline:** [PIPELINE_OBSERVABILITY_FINDINGS.md](../../PIPELINE_OBSERVABILITY_FINDINGS.md), [PIPELINE_OBSERVABILITY_GENERAL_PLAN.md](../../PIPELINE_OBSERVABILITY_GENERAL_PLAN.md), [PIPELINE_OBSERVABILITY_EXECUTION.md](../../waves/PIPELINE_OBSERVABILITY_EXECUTION.md), [pass-01-2026-08-10.md](./pass-01-2026-08-10.md)

## Verdict

Review complete: **0 critical**, **0 high**, **2 medium**, **2 low**.  
**BLOCK phase-execution:** **no** — pass-1 highs resolved; remaining items are P3.2 clarity (non-blocking for P0 start).

## Prior pass

[pass-01-2026-08-10.md](./pass-01-2026-08-10.md) — **BLOCK phase-execution: yes** (4 high). Fix agent updated execution files + findings (PO-Q17–Q19, PO-V5 deferred).

### Pass-1 resolution status

| Pass-1 finding | Status |
|:---|:---|
| P0 recorder session strategy | **resolved** — PO-Q17: dedicated `get_db_context()` #2–#3; shared session + `refresh(run)` #1; dedicated session #6 |
| P0 extend P0.5 / P1.1 duplicate hooks | **resolved** — P0.5 + P1 locked decisions |
| P0 checkpoint field list | **resolved** — P0.4 lists `context_stats`, partial `timing_stats`, `failure_stage=retrieve` |
| P0 `trigger_source` enum | **resolved** — `GitHubIndexJobTriggerSource` in P0 locked |
| P1 Voyage `pipeline_run_id` | **resolved** — `get_pipeline_run_for_index_job` in P1.4 |
| P1 publish path files | **resolved** — `github_publish.py` + formatter in P1.2 |
| P1 PO-V2 persist vs helper | **resolved** — in-memory P1.5; persist P3 |
| P2/P3/P4 `review_llm_wait_ms` | **resolved** — PO-Q18: attempts primary; P3.2 mirror; P4.1 uses attempts |
| P2 publish incremental site | **resolved** — `github_publish.py` in P2.4 |
| P2 phase gate ruff | **resolved** — ruff in P2 gate |
| P3 context_stats vs manifest | **resolved** — documented in P2/P3 locked |
| P4 PO-V5 CSV reconcile | **resolved** — deferred per findings; P4 optional cost flag only |
| P5 OTLP review-only hook | **resolved** — PO-Q19: review + reconcile + publish hooks |

## Rollup (critical / high)

None open.

## Per-phase findings

## P0 — `waves/PIPELINE_OBSERVABILITY_P0_EXECUTION.md`

**P0 — no new gaps** (verified unchanged).

| Severity | Area | Finding |
|:---|:---|:---|
| low | clarity | P0.1 subphase label skips “P0.2” in old drafts — current file is P0.0–P0.5 contiguous; OK. |

**Verified OK:** PO-Q17 session strategy; boundary #1 field list; migration `0031` after `0030`; LOOP pause; phase gate; PO-V3 scope.

---

## P1 — `waves/PIPELINE_OBSERVABILITY_P1_EXECUTION.md`

**P1 — no new gaps** (verified unchanged).

**Verified OK:** `get_pipeline_run_for_index_job` in P1.4; publish via `github_publish.py`; extend P0.5 wiring; PO-Q16 skip; Bedrock deferred; `_call_llm` at `github_review.py:969`.

---

## P2 — `waves/PIPELINE_OBSERVABILITY_P2_EXECUTION.md`

**P2 — no new gaps** (verified unchanged).

**Verified OK:** `review_llm_wait_ms` deferred to P3.2 per out-of-scope; manifest vs `context_stats` split; `github_publish.py` in P2.4; ruff in gate.

---

## P3 — `waves/PIPELINE_OBSERVABILITY_P3_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | Locked `timing_stats` keys include `index_ms`, `retrieve_ms`, `reconcile_ms`, `judge_ms`, `publish_ms` but P3.2 only names **`review_llm_wait_ms`** writer. Map `retrieve_duration_ms` → `retrieve_ms` in P3.2; document post-review stage ms updates from `reconcile_tasks` / `github_publish.py` (or mark `index_ms`/post-review keys nullable until those workers land). Non-blocking for P0–P2 LOOP. |
| low | clarity | `estimated_usd` optional in P3 — aligned with general plan P4 cost deferral. |

**Verified OK:** PO-Q18 mirror + attempts SSOT; `stats_version: 1`; API fields; partial rollups + `_mark_failed`.

---

## P4 — `waves/PIPELINE_OBSERVABILITY_P4_EXECUTION.md`

**P4 — no new gaps** (verified unchanged).

**Verified OK:** PO-V4 from `attempts.wait_ms`; truncation fallback; PO-V5 deferred; `--po-gate` without cost; PO-Q9 sunset; human staging gate.

---

## P5 — `waves/PIPELINE_OBSERVABILITY_P5_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | cross-phase | Execution index authority line still says PO-Q1–**Q16**; findings registry includes **Q17–Q19** — sync index header on next doc touch (P5.3). |
| low | clarity | Idempotent full re-export at each stage is correct; tests should assert judge + publish hook sites call export when OTLP enabled. |

**Verified OK:** PO-Q19 three hook sites + files listed; depends on P3; doc-sync subphase; human OTLP gate.

---

## Open questions (max 3)

None blocking.

## Execution contract checklist

- [x] Pass-1 critical/high items resolved in execution files
- [x] PO-Q17–Q19 in findings + execution index locked decisions
- [x] One execution file per phase; subphases + gates present
- [x] P0 migration LOOP pause; P5 doc-sync
- [ ] P3.2 name writers for all `timing_stats` keys (optional clarity during P3 LOOP)

**Next:** `phase-execution` from [PIPELINE_OBSERVABILITY_P0_EXECUTION.md](../../waves/PIPELINE_OBSERVABILITY_P0_EXECUTION.md).
