# Execution peer review — pass 1

**Plan folder:** `docs/review-pipeline/pipeline-observability/`  
**Date:** 2026-08-10  
**Scope:** full (P0–P5 execution files)  
**Reviewer:** execution-peer-review skill  
**Baseline:** [PIPELINE_OBSERVABILITY_FINDINGS.md](../../PIPELINE_OBSERVABILITY_FINDINGS.md), [PIPELINE_OBSERVABILITY_GENERAL_PLAN.md](../../PIPELINE_OBSERVABILITY_GENERAL_PLAN.md), [PIPELINE_OBSERVABILITY_EXECUTION.md](../../waves/PIPELINE_OBSERVABILITY_EXECUTION.md), [architecture pass-02](../architecture-peer-review/pass-02-2026-08-10.md)

## Verdict

Review complete: **0 critical**, **4 high**, **9 medium**, **5 low**.  
**BLOCK phase-execution:** **no** (after fix pass) — was yes before execution file updates.

## Prior pass

First pass — n/a.

## Rollup (critical / high)

| Severity | Phase | Title |
|:---|:---|:---|
| high | P0 | `LlmCallRecorder` commit on shared worker session needs isolated session or explicit `refresh` strategy |
| high | P1 | Voyage `pipeline_run_id` plumbing unspecified — `run_index_job` has no param; use `get_pipeline_run_for_index_job` |
| high | P3/P4 | `timing_stats.review_llm_wait_ms` never written — P4 PO-V4 percentiles will be empty |
| high | P5 | OTLP export hook on `review_tasks` only — misses judge/publish spans |

## Per-phase findings

## P0 — `waves/PIPELINE_OBSERVABILITY_P0_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| high | execution contract | Locked: “each public method ends with `session.commit()`” on passed session. Mid-task commit inside `run_review_run` can expire ORM state or commit unintended flushed work. Require **dedicated `get_db_context()`** for attempt rows (boundaries #2–3) and document `session.refresh(run)` after boundary #1 checkpoint on shared session. |
| medium | execution contract | P0.5 wires recorder on timeout path; P1.1 extends same `_call_llm` path — note “extend P0.5 wiring” to avoid duplicate hooks. |
| medium | clarity | P0.4 checkpoint should list boundary #1 fields explicitly: partial `timing_stats`, `failure_stage=retrieve`, not only `context_stats`. |
| low | clarity | `trigger_source` column should reuse `GitHubIndexJobTriggerSource` (`enums.py:113`). |

**Verified OK:** 6 subphases (P0.0–P0.5); migration `0031` follows `0030`; LOOP pause + human staging gate; handwritten Alembic; PO-V3 HTTP-timeout scope; phase gate with ruff + pytest; `test_generate_greptile_files.py` exists; architecture pass-02 locks (PO-Q15, PO-Q16, Bedrock deferred) reflected.

---

## P1 — `waves/PIPELINE_OBSERVABILITY_P1_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| high | codebase | P1.4 lists `github_indexing.py` but `run_index_job(session, index_job_id=…)` has no `pipeline_run_id`. Worker holds it only in `index_tasks.py`. Execution must specify `get_pipeline_run_for_index_job(session, index_job_id=job.id)` inside embed path or pass param from worker. |
| medium | codebase | P1.2 primary publish LLM site is `github_publish_formatter.py` → `moonshot_review.complete_issue_comment_markdown` (called from `github_publish.py` ~1657), not `publish_tasks.py`. |
| medium | execution contract | P1.5 PO-V2 rollup helper is in-memory/test helper only; persisting `run.token_rollup` stays P3 — clarify in P1.5. |
| low | clarity | `_complete_chat` needs new optional kwargs (`recorder`, `pipeline_run_id`, `step_type`, `attempt_no`). |

**Verified OK:** 5 subphases; Bedrock deferred (PO-Q13); PO-Q16 skip manual index; `_call_llm` at `github_review.py:969`; dual-emit PO-Q9; phase gate covers all P1 test modules.

---

## P2 — `waves/PIPELINE_OBSERVABILITY_P2_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| high | cross-phase | P2.3 writes `compare_ms`/`supplemental_ms` to `timing_stats`; P4.1 reads `review_llm_wait_ms` — **no phase writes `review_llm_wait_ms`**. Add to P3.2 finalize (`review_duration_ms` from `ReviewRunOutcome`) or change P4 to percentile on `github_llm_call_attempts.wait_ms` where `step_type=review`. |
| medium | codebase | P2.4 lists `publish_tasks.py`; publish LLM + `record_publish_pipeline_step` live in **`github_publish.py`** (~2178). Judge orchestration in `reconcile_tasks.py` + `github_finding_judge.py` is correct. |
| low | execution contract | Phase gate has pytest only — no `ruff check` (minor vs other phases). |

**Verified OK:** 5 subphases; `PipelineStepStatus.processing` exists (`enums.py:139`); depends on P0; PO-V6 `finish_reason=length` path in `moonshot_review.py`; does not duplicate end-of-task terminal steps on clean `_run` failures.

---

## P3 — `waves/PIPELINE_OBSERVABILITY_P3_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| high | execution contract | P3.2 must define `timing_stats` schema keys including **`review_llm_wait_ms`** (and map from `review_duration_ms`) — required by P4 / PO-V4. |
| medium | cross-phase | P3.3 `context_stats` retrieval fields overlap P2.3 manifest `retrieval.*` — intentional if documented (manifest = artifact, `context_stats` = run summary). |
| low | clarity | `build_context_stats` in `engineering_context/stats.py:64` — path verified. |

**Verified OK:** 4 subphases; `stats_version: 1`; partial rollups + `_mark_failed`; API fields match general plan; `model_id` on ORM, missing from `GitHubReviewRunResponse` today.

---

## P4 — `waves/PIPELINE_OBSERVABILITY_P4_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| high | execution contract | P4.1 percentiles from `timing_stats.review_llm_wait_ms` blocked until P3.2 defines writer (see P2/P3 finding). |
| medium | execution contract | Truncation rate from attempt `finish_reason=length` is sound alternative data source; consider documenting fallback. |
| low | clarity | `moonshot_staging_spend_metrics.py`, `judge_staging_spend_metrics.py`, `BACKEND_SCRIPTS_RUNBOOK.md`, `staging-validation/README.md` exist. |

**Verified OK:** 4 subphases; `--po-gate` + PO-V5 reconcile; PO-Q9 sunset in P4.4; human staging gate; phase gate with ruff on script.

---

## P5 — `waves/PIPELINE_OBSERVABILITY_P5_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| high | execution contract | P5.2 hooks export **after review run terminal commit** only — pipeline continues reconcile → judge → publish. Export must run on **pipeline terminal** (publish check finalize or idempotent re-export per stage). |
| medium | execution contract | P5.3 doc-sync subphase present (final-phase checklist ✓). |

**Verified OK:** 3 subphases; OTLP from DB SSOT; depends on P3 not P4; `app/observability/` correctly named as new; human OTLP validation gate.

---

## Open questions (max 3)

1. ~~**Recorder sessions**~~ — **locked PO-Q17:** dedicated `get_db_context()` for #2–#3; shared session + `refresh` for #1.
2. ~~**P4 percentiles**~~ — **locked PO-Q18:** `attempts.wait_ms` primary; `timing_stats.review_llm_wait_ms` mirror.

## Execution contract checklist

- [x] Execution index matches general plan P0–P5
- [x] One execution file per phase; 3–6 subphases each (P0 has 6 + P0.0 meta)
- [x] Each subphase: What / Files / Deliverable (with test command)
- [x] Out of scope + locked decisions per phase
- [x] Phase gate blocks present
- [x] P0 migration LOOP pause + human staging gate
- [x] P0.0 SSOT + Greptile + Bugbot
- [x] P5 doc-sync subphase
- [x] **High items fixed in execution files** (recorder sessions, Voyage plumbing, `review_llm_wait_ms`, OTLP hook sites)

**Next:** `execution-peer-review` pass 2 (delta) → `phase-execution` from P0.
