# Execution peer review — pass 1

**Plan folder:** `docs/review-pipeline/pr-summary-rollup/`  
**Date:** 2026-08-08  
**Scope:** P0–P3 execution files vs findings, general plan, codebase  
**Reviewer:** execution-peer-review skill

## Verdict

Review complete: **0 critical**, **0 high**, **2 medium**, **1 low** (all fixed in-place during review).  
**BLOCK phase-execution:** **no**.

---

## P0 — `waves/PR_SUMMARY_ROLLUP_P0_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | Initial draft listed two call sites without naming authoritative final ctx — **fixed:** flush uses post-collapse ctx; collapse path refreshes mid-flush `summary_json`. |
| low | execution contract | Phase gate pytest `-k` placement — **fixed.** |

**Verified OK:** 6 subphases (P0.0–P0.5); migration LOOP pause; pass-01 locks (`_load_pr_groups_for_rollup`, dedicated review_count, post-collapse); SSOT/Greptile/Bugbot P0.0; cited paths exist (`github_publish.py:1313–1325`, `apply_publish_summary_thread_collapse`).

---

## P1 — `waves/PR_SUMMARY_ROLLUP_P1_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | clarity | P1 `_build_summary_json` parity vs P0 persist — acceptable split: P0 writes manifest; P1 ensures formatter rebuild paths include key. |

**Verified OK:** 5 subphases; section order matches findings catalog; PSR-Q8 spike scoped to audit doc; phase gate covers formatter tests; `splice_deterministic_findings_tables` precedent exists.

---

## P2 — `waves/PR_SUMMARY_ROLLUP_P2_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | Wrong test module name (`test_installation_review_publish.py`) — **fixed** to `test_github_publish_routes.py`; dogfood test path named explicitly. |

**Verified OK:** 5 subphases; PSR-Q8 ship on `GitHubPublishJobResponse`; Moonshot extension points verified (`moonshot_review.py`, `build_pr_review_comment`); `test_revy_review_dogfood_staging_validation.py` exists.

---

## P3 — `waves/PR_SUMMARY_ROLLUP_P3_EXECUTION.md`

**P3 — no gaps found.**

**Verified OK:** 3 subphases; human gate on staging sign-off; doc-sync table; changelog explicitly out of scope; staging template pattern matches repo convention.

---

## Checklist

- [x] README execution table matches general plan phases
- [x] One execution file per phase; 3–6 subphases each
- [x] Each subphase: What / Files / Deliverable (with test command)
- [x] Out of scope + locked decisions
- [x] Phase gate block(s)
- [x] Migration subphase flagged for LOOP pause
- [x] Human gate on P3 staging
- [x] P0.0 SSOT + Greptile + Bugbot
- [x] P3 doc-sync subphase
- [x] No critical/high open

**Next:** `phase-execution` on `feat/pr-summary-rollup` from `main`, starting P0.
