# Execution peer review — pass 2 (P4)

**Plan folder:** `docs/review-pipeline/pr-summary-rollup/`  
**Date:** 2026-08-09  
**Scope:** P4 readability findings + execution vs codebase  
**Reviewer:** execution-peer-review skill

## Verdict

**0 critical** (splice fix scoped to P4.0 before LOOP). **0 high** open after plan updates.  
**BLOCK phase-execution:** **no** — if P4.0 lands before P4.1 tests.  
**BLOCK execution-peer-review:** **no**.

P4 is well-scoped and formatter-only. Safe to proceed after splice boundary fix.

---

## Verified (plan matches codebase)

| Claim | Evidence |
|-------|----------|
| P0–P3 shipped; formatter reads persisted rollup | `format_pr_resolution_rollup_block` ~477:516 `github_publish_formatter.py` |
| `filter_snapshot` v1 ready | `compute_filter_snapshot` ~162:216 `github_pr_resolution_rollup.py` |
| #501 reconciliation | `raised = resolved + still_open_display + hidden` → 29 = 17 + 0 + 12 |
| Deterministic splice after Moonshot | `splice_deterministic_pr_summary_block`, `apply_rollup_to_publish_surfaces` |
| Moonshot told not to invent lifetime numbers | `ISSUE_COMMENT_FORMAT_SYSTEM_PROMPT`; user prompt JSON subset |
| Product bar anchors `### PR summary` only | `_issue_comment_meets_product_bar` ~1241:1244 |
| PSR-Q4 rate unchanged | `build_pr_resolution_rollup` — P4 reformats only |

---

## Findings → plan fixes applied

| Sev | Finding | Resolution |
|:---|:---|:---|
| **critical** | `<details>` inside rollup breaks `_replace_markdown_section` (`\n<details>` end marker) | **P4.0** subphase + PSR-Q17 + double-splice test |
| high | Reconciliation `raised = resolved + display + hidden` not universal | **PSR-Q15** guard + `raw_active_before_filters` fallback |
| high | Moonshot `-k "pr_summary or splice"` tests missing | **P4.3** — add tests; PSR-Q18 stub-only LLM prose |
| high | Push-block scope drift (findings vs execution) | **PSR-Q16** — cross-ref in `<details>` only; push rewrite out |
| high | Check run unchanged | Documented in findings + P4.4 staging note |
| medium | `lifetime_disclosure` placement | After scan table, before `<details>` (PSR-Q12) |
| medium | PSR-Q13 “All clear” vs hidden > 0 | Qualified copy: “Nothing to act on in the tables below” |
| medium | Existing formatter tests need updates | Called out in P4.1 |
| low | PSR-Q14 / P4.5 split | Unchanged — good |

---

## Checklist

- [x] P4.0 splice fix before scan/details implementation
- [x] Reconciliation guard locked (PSR-Q15)
- [x] Moonshot must not emit table/details (PSR-Q18)
- [x] Push cross-ref scope aligned (PSR-Q16)
- [x] Phase gate includes moonshot tests
- [x] No rollup math / filter rule changes

**Next:** `phase-execution` on `feat/psr-p4-readability` from `main`, starting P4.0.
