# Architecture peer review — pass 1

**Plan folder:** `docs/review-pipeline/pr-summary-rollup/`  
**Date:** 2026-08-08  
**Scope:** full — [PR_SUMMARY_ROLLUP_FINDINGS.md](../../PR_SUMMARY_ROLLUP_FINDINGS.md) + [PR_SUMMARY_ROLLUP_GENERAL_PLAN.md](../../PR_SUMMARY_ROLLUP_GENERAL_PLAN.md) cross-checked against backend publish/formatter stack  
**Reviewer:** architecture-peer-review skill  
**Baseline:** [FINDINGS](../../PR_SUMMARY_ROLLUP_FINDINGS.md) · [GENERAL_PLAN](../../PR_SUMMARY_ROLLUP_GENERAL_PLAN.md) · [program README](../../README.md)
§
## Verdict

Review complete: **2 critical**, **4 high**, **6 medium**, **2 low**.  
**BLOCK create-execution-plan:** **no** — findings + general plan are baseline-ready; pass-01 locks copied into findings (2026-08-08).  
**BLOCK phase-execution:** **no** — SOS-5 / [#84](https://github.com/raimondskrauklis/revy/pull/84) merged to `main` (`2d7462b`).

> **Post-review (2026-08-08):** [PR_SUMMARY_ROLLUP_FINDINGS.md](../../PR_SUMMARY_ROLLUP_FINDINGS.md) and [PR_SUMMARY_ROLLUP_GENERAL_PLAN.md](../../PR_SUMMARY_ROLLUP_GENERAL_PLAN.md) updated with PSR-Q8–Q10, implementation locks, and open-question resolutions below. Historical findings in this file remain the audit record.

## Prior pass

First pass — n/a.

## Verified (docs match codebase)

| Claim | Code evidence |
|:---|:---|
| Two-block summary tables | `format_summary_comment` — `backend/app/services/github_publish_formatter.py:663–691` |
| Push FR-Q12 metrics block + G9 prose | `format_resolution_metrics_block` (197–254); `_g9_resolution_prose_for_ctx` (409–416) |
| SOS-5 display filters (orphan + collapse) | `filter_pr_active_groups_for_summary` (423–460); `display_still_open_prior_count` (463–470) — on `main` via #84 (`2d7462b`) |
| Push manifest only | `build_resolution_pass_manifest` — `backend/app/services/github_resolution_metrics.py:697+` |
| `summary_json` per publish, no rollup | `_build_summary_json` (1193–1209); no `pr_resolution_rollup` / `build_pr_resolution_rollup` in `backend/` |
| PR row has no rollup column | `GitHubPullRequestORM` — `backend/app/models/github_pull_request.py` (no JSONB fields) |
| Check run omits G9 + resolution metrics | `build_check_run_summary` (694–707) — confidence + two-block tables only |
| Moonshot lacks PR summary step | `ISSUE_COMMENT_FORMAT_SYSTEM_PROMPT` — `backend/app/integrations/moonshot_review.py:40–57` |
| Deterministic findings splice exists | `splice_deterministic_findings_tables` (1032–1049) |
| Pipeline trace = push manifest only | `get_resolution_metrics_for_review_run` — `backend/app/services/github_pipeline_trace.py:525–542` |
| Dogfood reads publish `summary_json`, not rollup | `revy_review_dogfood_staging_validation.py` (109–124, 415–431) |
| Publish flush sets `completed` after surface flush | `run_publish_job` — `backend/app/services/github_publish.py:2056–2071` |
| PSA #83 on `main` | `main` at `a5bb273` (merge PR #83) |

## Rollup (critical / high)

| Severity | Section | Title |
|:---|:---|:---|
| critical | P0 | Rollup must be computed **after** thread-collapse refresh in flush, not only at `_build_publish_surface` |
| critical | P0 | Lifetime counts need **all non-superseded PR groups** — not `_load_pr_active_groups` (active-only) |
| high | P0 | SOS-5 / #84 **merged to `main`** (`2d7462b`) — prerequisite satisfied |
| high | P0 / findings | `still_open_display` = full block-2 row count — **not** `display_still_open_prior_count()` (prior-only) |
| high | P1 / PSR-Q8 | API — **P1 spike, P2 ship** on `GitHubPublishJobResponse` (PSR-Q8 locked) |
| high | findings | Legacy PR first publish — **PSR-Q9** locked (full DB + disclosure footer) |

## Per-section findings

## P0 — Rollup contract, migration, compute, persist

| Severity | Area | Finding |
|:---|:---|:---|
| critical | codebase | `apply_publish_summary_thread_collapse` recomputes filtered `pr_active_groups` and `_build_summary_json` during `_flush_publish_surface` (`github_publish.py:1652–1662`, `github_publish_formatter.py:598–640`). Rollup computed only at `_build_publish_surface` will drift from published comment when inline threads collapse mid-flush. **Lock:** single `build_pr_resolution_rollup(ctx, all_pr_groups, review_count)` called from final format paths — including collapse refresh — immediately before `job.status = completed`. |
| critical | codebase | `_load_pr_active_groups` selects `state == active` only (`github_publish.py:1313–1325`). Lifetime `raised_count` / `resolved_count` / `resolved_by_method` require all groups on PR where `state != superseded`. Add dedicated loader (e.g. `_load_pr_groups_for_rollup`). |
| high | general plan | P0 depends on “SOS-5 + #84 on `main`”. SOS-5 code is on `feat/still-open-summary-sos5` (~76-line formatter delta vs `main`); [SOS README](../../still-open-summary-dogfood/README.md) still shows deploy + #491 dogfood pending. Gate `phase-execution` on SOS merge. |
| high | findings | Verification gate `rollup.still_open_display == len(filtered block 2 rows)` means `len(_active_groups(verdict_groups(ctx)))` after filters. `display_still_open_prior_count()` excludes generation fingerprints (463–470) — push/G9 metric only. Document in execution to prevent conflation. |
| medium | general plan | P0 scope cites `_build_publish_surface` / successful flush but not **post-collapse** `job.summary_json` merges (`github_publish.py:1674–1683`, 1944–1947). Execution subphase should name both call sites. |
| medium | findings | `filter_snapshot` manifest field has no v1 key list. Edge cases mention `compare_failed_hidden`; SOS implies collapsed/orphan hidden counts. Lock keys in P0 execution. |
| medium | codebase | PR row update on publish is **new** — today only `job.summary_json` is written. P0 needs atomic PR JSONB write when `status → completed` only (PSR-Q5). |
| medium | findings | `review_count` = completed publish jobs — must not reuse `_fetch_prior_reusable_publish_jobs` mix (completed + failed for inline reuse). Dedicated count query on `pull_request_id` + `status == completed`. |

**Verified OK:** PSR-Q1–Q7 locked in general plan match findings registry; migration target (`github_pull_requests.pr_resolution_rollup` JSONB) is net-new and plausible; PSR-Q5 aligns with `run_publish_job` terminal status write.

---

## P1 — Formatter surfaces (GitHub comment + check)

| Severity | Area | Finding |
|:---|:---|:---|
| high | codebase | `GitHubPublishJobResponse` (`backend/app/schemas/github_publish.py`) exposes job metadata only — no `summary_json`. PSR-Q8 open item: findings say “P2; else P3”, general plan says “P1 spike; P2 fallback” — **lock one timeline** in execution P1. |
| medium | codebase | `build_check_run_summary` (694–707) lacks G9, resolution metrics, and PR summary — plan correctly scopes check parity here. |
| medium | codebase | Moonshot parity needs new splice/insert beyond `splice_deterministic_findings_tables`: `_refresh_issue_pr_verdict_sections` does not patch resolution or PR summary blocks; `_insert_resolution_metrics_block` markers (1074–1091) lack `### PR summary`. |
| medium | general plan | `_build_summary_json` rollup inclusion listed in P1 while P0 persists rollup — execution should clarify: P0 persists manifest; P1 ensures formatter/summary_json parity on display paths. |
| low | clarity | `build_pr_review_comment_fallback` section order (928–989) inserts G9 + resolution metrics before files/tables — target layout adds `### PR summary` **before** G9 per findings catalog. |

**Verified OK:** Greptile-shape fallback and Moonshot system prompt are the right extension points; `format_resolution_metrics_block` display override pattern (SOS-D8) is the precedent for display-aligned counts.

---

## P2 — Moonshot prompt + pipeline trace + dogfood script

| Severity | Area | Finding |
|:---|:---|:---|
| medium | findings | Dogfood gate text says `rollup.still_open == pr_active_count` — `summary_json.pr_active_count` today uses filtered `verdict_groups` (`_build_summary_json` 1205–1207). Assert `still_open_display` against block-2 parity, not raw `pr_active_count` alias. |
| medium | general plan | Optional pipeline trace artifact for rollup — no reader exists yet; subphase should name artifact kind + step manifest key. |
| low | findings | Terminology table still says review count “Publishes or review runs” while PSR-Q2 locked to **completed publish jobs** — fix in findings on next edit pass. |

**Verified OK:** `_build_issue_comment_user_prompt` (1111–1139) is the right injection point for rollup JSON; Moonshot tests exist in `test_moonshot_review.py`.

---

## P3 — Staging sign-off

| Severity | Area | Finding |
|:---|:---|:---|
| medium | general plan | P3 dogfood chain depends on SOS P2 sign-off ([SOS README](../../still-open-summary-dogfood/README.md)) — note in execution or accept staging risk for orphan filter regressions. |
| low | clarity | Staging memo filename `PR_SUMMARY_ROLLUP_STAGING_VALIDATION.md` not yet in program folder — expected new in P3. |

**Verified OK:** `revy_review_dogfood_staging_validation.py` exists and is the right script to extend; human gate appropriately isolated in P3.

---

## Cross-cutting

| Severity | Area | Finding |
|:---|:---|:---|
| medium | findings | Reuse trap “`pr_active_count` pre-filter in some paths” may be **stale** — current `_build_summary_json` uses filtered verdict (1205–1207). Rollup must still use `still_open_display` semantics from findings gate, not assume `pr_active_count` equivalence forever. |
| medium | findings | “No misleading pre-history” vs data scope “all non-superseded groups on PR”: first post-deploy publish on a **legacy PR** will include full DB history in lifetime raised/resolved. Lock product rule in findings before execution (full history vs deploy-forward cohort). |
| low | general plan | Consider `github_pr_resolution_rollup.py` instead of `github_resolution_metrics` to avoid FR-Q12 push-pair confusion — optional; not blocking. |

**General-plan contract checklist**

| Check | Status |
|:---|:---|
| Header links findings | ✓ |
| Locked decisions match findings | ✓ (PSR-Q1–Q7) |
| Each phase: goal / scope / deliverables / depends | ✓ |
| SOS prerequisite merged on `main` | ✓ (#84 `2d7462b`) |
| Migration implied + handwritten Alembic | ✓ (P0) |
| APIs/services named exist or marked new | ✓ |
| Human gate in P3 only | ✓ |
| PSR-Q8–Q10 locked in findings | ✓ (2026-08-08) |
| Next step = `create-execution-plan` | ✓ |

## Open questions (resolved 2026-08-08)

1. **Legacy PR, first post-deploy publish:** **PSR-Q9** — full DB non-superseded groups; disclosure footer when `review_count == 1` and pre-existing groups.
2. **PSR-Q8:** **P1 spike** route audit; **P2 ship** `pr_resolution_rollup` on `GitHubPublishJobResponse`.
3. **`filter_snapshot` v1 keys:** **PSR-Q10** — `raw_active_before_filters`, `collapsed_hidden`, `orphan_never_inlined_hidden`, `compare_failed_hidden`.

## Pre-execution checklist

- [x] Merge SOS-5 / #84 to `main` before `phase-execution` P0
- [x] Lock rollup compute call sites (in findings — implementation locks)
- [x] Lock `still_open_display` vs `display_still_open_prior_count` (PSR terminology + tests)
- [x] Lock `filter_snapshot` key schema v1 (PSR-Q10)
- [x] Resolve PSR-Q8 timeline and legacy-PR lifetime scope (PSR-Q8, PSR-Q9)
- [x] Moonshot splice helper name (`splice_deterministic_pr_summary_block` in general plan P1)

**Next:** `create-execution-plan` → weave pass-01 locks into P0–P3 execution files → `execution-peer-review` → `phase-execution` on `feat/pr-summary-rollup` from `main`.
