# docs/review-pipeline/resolution-honesty/reviews/execution-peer-review/pass-02-2026-09-19.md

# Execution peer review — pass 2

**Plan folder:** `docs/review-pipeline/resolution-honesty/`  
**Date:** 2026-09-19  
**Scope:** full (P0–P5) — independent re-review; pass-01 not used as evidence  
**Reviewer:** execution-peer-review skill  
**Baseline:** [RESOLUTION_HONESTY_FINDINGS.md](../../RESOLUTION_HONESTY_FINDINGS.md), [RESOLUTION_HONESTY_GENERAL_PLAN.md](../../RESOLUTION_HONESTY_GENERAL_PLAN.md), [README.md](../../README.md), architecture [pass-02](../architecture-peer-review/pass-02-2026-09-19.md), codebase paths cited below  

## Verdict

Review complete: 1 critical, 1 high, 15 medium/low.  
**BLOCK phase-execution:** yes — P0.4 UUID-keys `github_inline_threads` without switching formatter/rollup/metrics/Option A comparisons would hide every still-open finding.

## Prior pass

[pass-01-2026-09-19.md](./pass-01-2026-09-19.md) — BLOCK phase-execution: no. This pass does not inherit that verdict.

## Rollup (critical / high)

| Severity | Phase | Title |
|:---|:---|:---|
| critical | P0 | P0.4 re-keys inline threads to `group.id` but consumers still match `group.fingerprint` |
| high | P1 | P1.1 “≠ 1” close rule vs P1.2 “count == 0”; implement P1.2 (findings count), not the bind rule |

## Per-phase findings

## P0 — `docs/review-pipeline/resolution-honesty/RESOLUTION_HONESTY_P0_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| critical | codebase | P0.4 re-keys `github_inline_threads` to `str(group.id)` but **Files** are only `github_publish.py` + `test_github_publish.py`. Live consumers still treat map keys as `group.fingerprint`: `filter_pr_active_groups_for_summary` (`github_publish_formatter.py:453–464`), rollup orphan filter (`github_pr_resolution_rollup.py:193–201`), `_fingerprints_to_resolve_inline_threads` (`github_publish.py:728`, `:759` — `group.fingerprint in inline_threads`), `InlinePostSpec.group_fingerprint=group.fingerprint` (`:1593`), `_fingerprints_from_publish_summary` (`github_resolution_metrics.py:357–361`). After the first cutover publish, `ever_inlined` is UUIDs and `group.fingerprint not in ever_inlined` is true for every group → **all still-open rows look never-inlined / hidden**. Dual-read of 64-hex keys does not fix **writes** that are UUID-only. **Lock:** either (a) expand P0.4 Files to formatter + rollup + resolution_metrics + `InlinePostSpec` and switch every comparison to `group.id` in the same subphase, or (b) keep map keys = `group.fingerprint` in P0 (fingerprint column is not rewritten in Alembic) and only migrate to `group.id` when a subphase also rewrites stored fingerprints. |
| medium | execution contract | P0.4 `-k "inline_thread"` misses `test_close_active_groups_for_fingerprints_*` (no `inline_thread` in the name) even though P0.4 changes that helper. Phase gate later runs the whole `test_github_publish.py` — keep the close tests in the **subphase** command. |
| medium | LOOP | P0.2 pytest includes all of `test_github_finding_closure.py` before P1. That is OK only for the `compute_fingerprint(claim_slot=)` signature in `_fingerprints_in_review_run`. Do not change `should_close_absent_and_addressed` in P0.2. |
| low | clarity | `claim_slot` is 32 hex chars; ORM column is `String(64)`. Fine. `test_compute_fingerprint_differs_by_start_line` must be rewritten in P0.2 (new hash must **not** change with line). |

**Verified OK:** Header/goal/out-of-scope/6 subphases; Alembic `0035` after `0034` (`2026_09_19_1800_0034_github_installation_verified_at`); no fingerprint rewrite in migration; unique constraint kept; `_mark_superseded_peers` retired in P0.2; bind order matches general-plan cross-cutting; H2 skip deferred to P1; nearby = same `file_path`; RH-Q11 intent; P0.0 SSOT + Bugbot; LOOP pause after P0.1; `compute_fingerprint` call sites are reconcile + closure + tests only.

---

## P1 — `docs/review-pipeline/resolution-honesty/RESOLUTION_HONESTY_P1_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| high | execution contract | P1.1 says skip H2 when candidate count **≠ 1**. P1.2 says close iff `path_gone` or count **== 0**, skip if count **≥ 1**. Those are not the same: **≠ 1** would **refuse** to close two same-file fixes (count==2). P1.2’s **findings** count == 0 is the rule that can close PAT+innerHTML in one file. **Lock P1.2:** close unbound groups only when this-run **findings** in that `file_path`+`category` == 0 or the path is gone; skip H2 when ≥ 1 finding remains (leftovers / unmatched). Do not implement P1.1’s “≠ 1” as the close predicate — that is the **P0 bind** rule. |
| medium | general plan | P0 candidate set = unbound **groups**; P1.1 counts this-run **findings**. Keep both, but name them: bind uses leftover groups; H2 uses finding count so two real fixes in one file still close. |
| low | P1.3 | Path-removed **bucket** is rollup (`_is_lifetime_path_removed_resolution`). P1.3 should assert method `absent_and_addressed`; bucket check can wait for P3. |

**Verified OK:** FR-Q3 supersede named; method stays `absent_and_addressed`; both call sites listed (`github_finding_closure.py:252`, `github_publish.py:1433`); compare/HEAD block kept; Pass 1 line-region tests exist (`test_patch_touches_line_region_*`); FR-Q11 volume note; 4 subphases; no new enum; `test_github_finding_closure.py` already has `should_close_absent_and_addressed` cases to rewrite.

---

## P2 — `docs/review-pipeline/resolution-honesty/RESOLUTION_HONESTY_P2_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | codebase | `format_resolution_metrics_block` **display override** still does `rate = … if denominator else 0.0` (`github_publish_formatter.py:223–229`). P2.2 must N/A that path too (`test_format_resolution_metrics_block_display_still_open_override`), not only the raw empty-denom fixture. |
| medium | execution contract | Phase gate `-k "build_resolution_pass_manifest or format_resolution_metrics_block"` will skip P2.3 unless the new test name contains one of those strings. Name it accordingly or drop `-k` on the phase gate. |
| low | P0.4 coupling | If P0.4 writes UUID keys into `github_inline_threads`, `_fingerprints_from_publish_summary` (`github_resolution_metrics.py:357–361`) must treat them as group ids. P2.1 “keeps group-id identity” should say that explicitly. |

**Verified OK:** Empty denom is `0.0` today (`:792`) and formatter paints `0% (0/0)` (`:245`); N/A in **both** manifest and formatter is locked; historical superseded stay out; `get_last_published_prior_revision` exists; 3 subphases; no second persist path.

---

## P3 — `docs/review-pipeline/resolution-honesty/RESOLUTION_HONESTY_P3_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | codebase | PSR-Q15 / orphan filters still compare `group.fingerprint` to `ever_inlined_fingerprints` / publishable keys (`github_pr_resolution_rollup.py:193–201`, `github_publish_formatter.py:453–464`). If P0.4 UUID-keys land without expanding those Files, P3.2’s four-term identity is **false** (everything hidden). Same fix as P0 critical — do not “fix raised math” in P3 as a workaround. |
| low | P3.2 | `-k "rollup or pr_resolution or hidden_total"` matches most formatter tests (`rollup` is in helpers). Prefer the phase gate’s full-file pytest (already listed). |

**Verified OK:** `raised_count` stays non-superseded (`:231–234`); no remap of #1 (P3.3 fixture 13/10); `path_removed` bucket exists; PSR-Q15 copy already in details; 3 subphases; RH-Q2 forward-only.

---

## P4 — `docs/review-pipeline/resolution-honesty/RESOLUTION_HONESTY_P4_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | P4.1–P4.2 assume inline keys are `group.id` (P0.4 option a). If LOOP keeps fingerprint keys to avoid the P0 critical, rewrite these tests to that key type — do not mix UUID maps with `group.fingerprint in inline_threads` (`github_publish.py:759`). |
| low | P4.1 `-k` | `fingerprints_to_resolve` is not in current test names; `outdated` and `inline_thread` are. Phase gate runs the full publish file. |

**Verified OK:** GH-Q9 keep is explicit (`:730–733`); `_close_active_groups_for_fingerprints` stays on P1 predicate (`:1411–1438`); Outdated ≠ DB resolved; no App settings; 3 subphases; GitHub markdown EN; scan vs details split matches RH-Q5/Q12.

---

## P5 — `docs/review-pipeline/resolution-honesty/RESOLUTION_HONESTY_P5_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | human gate | P5.1 is deploy + new PR + memo. LOOP must not treat it as a same-commit code phase; human gate already says so. |
| low | P5.2 | Optional gap pass is allowed. P5.3 still greps FR-Q3 / D10 / BLOCK create-execution-plan. |
| low | P5.3 | `changelog.json` is English-only today (existing pattern). No new `t()` keys required unless the app later i18n’s that file. |

**Verified OK:** RH-Q10 equivalent new PR; experiment gates include eval insert + ambiguous leftovers + Pass 2 without `addressed`; doc-sync table + sibling FR-Q3/R5-Q1; **Next:** none; `frontend/src/data/changelog.json` exists.

## Open questions (max 3)

1. P0.4: expand UUID-key consumers in the same subphase, or keep `github_inline_threads` keyed by `group.fingerprint` until fingerprints are rewritten?

## Execution contract checklist

- [x] Header links general plan + findings
- [x] Goal + decisions locked per phase
- [x] Out of scope lists later phases
- [x] 3–6 subphases each (none >8)
- [x] What / Files / Deliverable
- [x] Deliverable has verify command or assertion
- [x] Phase gate copy-paste pytest / json.tool
- [x] Migration subphase flagged handwritten + LOOP pause (P0.1)
- [x] Human gate on P5.1
- [x] Next chain; P5 none
- [x] No TBD / no A-or-B
- [ ] P0.4 Files list covers every inline-map key consumer **or** keys stay fingerprints
- [ ] P1 close predicate is P1.2 (findings count == 0 / path gone), not P1.1 “≠ 1”
- [x] Final phase doc-sync

**Next:** fix agent edits P0.4 Files (or fingerprint-key fallback) and P1.1/P1.2 predicate wording. Then execution-peer-review pass 3 (delta). Do **not** run `phase-execution` while BLOCK is yes.
