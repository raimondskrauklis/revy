# docs/review-pipeline/resolution-honesty/reviews/architecture-peer-review/pass-02-2026-09-19.md

# Architecture peer review — pass 2

**Plan folder:** `docs/review-pipeline/resolution-honesty/`  
**Date:** 2026-09-19  
**Scope:** full (re-review after pass-01 locks landed in findings + general plan)  
**Reviewer:** architecture-peer-review skill  
**Baseline:** [RESOLUTION_HONESTY_FINDINGS.md](../../RESOLUTION_HONESTY_FINDINGS.md), [RESOLUTION_HONESTY_GENERAL_PLAN.md](../../RESOLUTION_HONESTY_GENERAL_PLAN.md), codebase paths cited below  

## Verdict

Review complete: 0 critical, 1 high, 4 medium/low.  
**BLOCK create-execution-plan:** no — pass-01 critical/high locks are in RH-Q1–Q12 and the phase blocks; remaining high is an execution matching rule, not a locked-decision contradiction.

## Prior pass

[pass-01-2026-09-19.md](./pass-01-2026-09-19.md) — BLOCK create-execution-plan: yes. Fix agent applied identity (claim_slot, continuation, start_line attribute), FR-Q3 supersede (RH-Q9), GH-Q9 keep, PSR raised forward-only, P0 cutover, P5 new PR, inline-thread keys.

## Verified (docs match codebase)

| Claim | Code evidence |
|:---|:---|
| Today’s D10 still includes title + start_line | `compute_fingerprint` — `github_finding_reconcile.py:49–70` (target identity is **new**) |
| File+category peer supersede still ships | `_mark_superseded_peers` — `github_finding_reconcile.py:73–98` (P0 retires) |
| Pass 2 still requires `addressed` | `should_close_absent_and_addressed` — `github_finding_closure_rules.py:18–32` (P1 replaces; method stays `absent_and_addressed` `:35–44`) |
| GH-Q9 outdated union still ships | `_fingerprints_to_resolve_inline_threads` — `github_publish.py:730–733` (P4 **keeps**) |
| `path_removed` is a display bucket | `ResolutionMethod` has no `path_removed`; rollup `_is_lifetime_path_removed_resolution` |
| Group has no `start_line` / `claim_slot` yet | `GitHubFindingGroupORM` — P0 handwritten Alembic (head `0034`) |
| This-push still paints `0%` on empty denom | `build_resolution_pass_manifest` `:792`; `format_resolution_metrics_block` `:245` (P2) |
| RH-Q1–Q12 findings ↔ general plan | findings `:190–205` ↔ general plan `:15–32` |

## Rollup (critical / high)

| Severity | Section | Title | Status vs pass-01 |
|:---|:---|:---|:---|
| high | P0 / P1 | Ambiguous continuation + H2-unbound would false-resolve leftovers | **new** |

Pass-01 critical/high: all **resolved** (see per-section).

## Per-section findings

## P0 — Persistent identity (cutover)

| Severity | Area | Finding |
|:---|:---|:---|
| high | general plan | **New.** Bind is exact line, then continuation only if **exactly one** candidate. H2 then closes every group this run **did not bind**. On a file with two leftover same-category claims, continuation is skipped; both old groups are unbound → P1 would `resolved` them and the new titles look newly raised. That fails the leftover gate even though the eval **single-claim** case is saved (`claim_slot` from frozen title hash matches when the title is stable; continuation covers line insert). **Execution lock (do not reopen RH-Q4/Q8/Q9):** when candidate count ≠ 1, do **not** H2-close those unbound leftovers this run (leave `still_open`); only close when there are **zero** plausible candidates or the path is gone. Name “nearby” and the candidate set (unbound, same path+category, not already bound). |
| — | pass-01 | Start_line-in-hash vs eval: **resolved** (RH-Q4 — start_line attribute; unique key excludes it; continuation before H2). |
| — | pass-01 | Same-line collision: **resolved** (RH-Q8 — `claim_slot`). |
| — | pass-01 | Cutover parked as calibration: **resolved** (P0 **is** D10 cutover: group `start_line`, handwritten unique constraint, dual lookup, RH-Q11). |

**Verified OK:** P0 scope lists continuation, retire `_mark_superseded_peers`, Alembic, dual lookup, inline-thread migrate. Deliverable “INFO→CRITICAL and eval insert keep one open group” matches the north star.

---

## P1 — Honest closure (H2)

| Severity | Area | Finding |
|:---|:---|:---|
| — | pass-01 | FR-Q3 unnamed: **resolved** (RH-Q9; findings “This program supersedes” table). Predicate: `closure_blocked_reason is None` + unbound; method `absent_and_addressed`. |
| — | pass-01 | `path_removed` as enum: **resolved** (display bucket). |
| medium | codebase | Pass-01 medium **open:** widening Pass 2 eats FR-Q11 verification candidates (`still_open` + fingerprint absent). Execution: Pass 3 volume drop is expected for missed-line **fixes**; do not also H2-close leftovers held open by the unmatched-continuation rule above. |

**Verified OK:** Four-fix deliverable now names Pass 2 without `addressed`. Cross-cutting still blocks silent close on compare/HEAD failure. `should_reopen_absent_and_addressed` still keys off method `absent_and_addressed` (FR-Q13) — keep that after P1.

---

## P2 — This-push metrics

**P2 — no new gaps** (verified unchanged vs pass-01 medium: N/A must be in the manifest **and** `format_resolution_metrics_block`; after P0, empty denom from peer supersede stops on new PRs). Not blocking.

---

## P3 — Lifetime rollup

| Severity | Area | Finding |
|:---|:---|:---|
| — | pass-01 | PSR raised vs RH-Q2: **resolved** (supersede **forward** only; historical superseded stay out; RH-Q10). |
| — | pass-01 | Three-term vs PSR-Q15: **resolved** (RH-Q12). |

**Verified OK:** P3 out-of-scope remaps #1. New-PR raised = unique claims after stopping file+category supersede.

---

## P4 — Comment + GitHub threads

| Severity | Area | Finding |
|:---|:---|:---|
| — | pass-01 | Fingerprint vs `github_inline_threads`: **resolved** (RH-Q11; P0 migrates with rewrite; prefer `group.id`). |
| — | pass-01 | Collapse vs GH-Q9: **resolved** (P4 deliverable keeps Outdated **UI** collapse; numbers follow H2). |

**Verified OK:** RH-Q5 N/A copy; RH-Q12 four-term details. Do not rip `github_publish.py:730–733`.

---

## P5 — Dogfood gates

| Severity | Area | Finding |
|:---|:---|:---|
| — | pass-01 | Replay #1’s 13 rows: **resolved** (RH-Q10 — equivalent **new** PR). |

**Verified OK:** Six gates include same-group-open for eval insert and Pass 2 without `addressed`. Human/ops last.

---

## Cross-cutting

| Severity | Area | Finding |
|:---|:---|:---|
| — | pass-01 | Unnamed FR-Q3 / PSR / GH-Q9: **resolved**. |
| medium | findings | “This program supersedes **FR-Q1** D10 fingerprint includes title” is the **wrong ID**. FR-Q1 is product vocabulary (Addressed / Dismissed / Still open / Superseded). D10 title+line is **R5-Q1** / review-quality **D10**. Execution should cite those, not FR-Q1. Does not reopen RH-Q4. |
| medium | general-plan contract | Files / test module names still belong in execution (expected). P0 now implies handwritten Alembic after `0034`. General plan **Next** still says wait for pass 2 — update to `create-execution-plan` in a doc-sync, not a new architecture lock. |
| low | findings | H3 still says “last published **fingerprints**”; RH-Q3/Q11 prefer identities / `group.id`. P2 execution should persist prior set as group ids after rewrite. |
| low | findings | Edge row “addressed or absent” for line-miss is leftover wording; target is unbound after continuation (RH-Q9). |

**Verified OK:** Locked tables match. No TBD. Continuation-before-H2 is in cross-cutting. GitHub markdown stays EN; reviewer EN+LV only if labels change (no new `ResolutionMethod`).

## Open questions (max 3)

1. Execution only: when continuation sees 0 vs 2+ candidates, confirm H2 **skips** those leftovers (pass-02 high) — not a new RH-Q.

## Pre-execution checklist

- [x] Identity vs line drift (start_line attribute + continuation; unique key excludes start_line).
- [x] FR-Q3 superseded; Pass 2 predicate in one sentence (RH-Q9).
- [x] Two claims same path+category+line stay two groups (`claim_slot`).
- [x] P0 is D10 cutover (Alembic, dual lookup, group `start_line`).
- [x] `github_inline_threads` migrates with fingerprint (prefer `group.id`).
- [x] PSR raised formula: forward-only; historical superseded stay out.
- [x] Keep GH-Q9 thread collapse; numbers follow H2.
- [x] P5 = equivalent new PR.
- [x] `path_removed` stays a display bucket.
- [ ] Execution P0/P1: candidate set + “≠ 1 candidate ⇒ do not H2-close those leftovers.”
- [ ] Execution P2: empty denom → N/A in manifest and formatter.
- [ ] Unit tests: continuation/eval insert, `claim_slot` collision, Pass 2 without `addressed`, compare-fail does not close, PSR-Q15, no duplicate inlines.

**Next:** `create-execution-plan` from this folder. Include the unmatched-continuation H2 skip as a P0/P1 subphase. Then `execution-peer-review`.
