Reviewing RQ9 backend changes and cross-checking docs against the implementation.

Checking whether a neutral conclusion is valid in _finalize_pipeline_github_check and update_check_run within github_pipeline_trace.py.

Confirmed index failures do not double-finalize. The prepare_review_after_index call exits early when the job status is incomplete. A race where a PR becomes draft during indexing is handled correctly by the neutral finalize path.

Pending reviews return empty outcomes without neutral finalize. This may leave checks stuck in_progress until completion. Conflict errors also return empty outcomes. Need to verify whether another process should finalize those. Also checking if _finalize_pipeline_github_check sets the status to completed.

Need to inspect update_check_run to confirm it marks the check as completed.

The transaction split test may call finalize twice: once directly at lines 89-94, and again through record_index_pipeline_step. The test assertions need closer inspection.

There is a possible contradiction in the recovery checklist: it lists migrations 0001-0026 on main, but 0026 might not actually be present.

A stray "Not to forget;" line in my-commands could be accidentally committed code.

The 0026 migration was not found. Searching for the review_quality migration instead.

Checking whether update_check_run sets the check run status to completed.

Checking whether draft pull requests can enqueue index jobs.

maybe_enqueue_pipeline_for_revision appears to skip draft PRs entirely. This raises the question of how a draft PR could ever run an index job and need neutral finalize.

The recovery checklist expects draft PR autostart to trigger neutral finalize. Yet if maybe_enqueue_pipeline skips drafts at enqueue time, no index job would be created. This creates a contradiction between the expected workflow and the current implementation.

Investigating whether GitHub webhooks for synchronize or opened events on draft PRs might bypass the enqueue filter and create index jobs anyway. Will examine the webhook flow for these scenarios.

A potential bug was spotted: the staging smoke test expects draft PR autostart to check neutral, but maybe_enqueue_pipeline appears to skip draft PRs entirely.

The smoke test scenario is likely invalid because enqueue skips draft PRs. The real fix addresses a race condition where a ready-for-review PR becomes draft before review. The text may be misleading but the code change remains valid. Now scanning for other bugs.

Confirmed that a failed index correctly triggers single-failure finalization. The elif ordering places neutral before failure, so neutral would win if both flags were somehow set. This is unlikely but noted.

Command triggers on closed PRs are safe; enqueue skips them and neutral finalize handles closure during indexing.

The index-failure transaction-split test lacks a verification that finalize receives the stashed check ID. This is a test gap, not a code bug.

No new tests cover finalize_pipeline_github_check_neutral in test_github_pipeline_trace.py. This is a test coverage gap rather than a runtime bug.

A potential double-finalize bug is being investigated. The failed-index finalize at lines 89-94 might overlap with a finalize call inside record_index_pipeline_step.

Checking whether record_index_pipeline_step finalizes on failure.

A stray "Not to forget;" appears in my-commands. This looks like accidentally committed code.

The agents/prompts/ directory discrepancy between BUGBOT and Greptile seems like an intentional gap rather than a bug.

The recovery checklist incorrectly claims migrations 0001–0026 are on main. Migration 0026 is missing locally, so the documentation is wrong.

The staging smoke gate documentation is misleading. It claims draft PRs trigger the pipeline automatically, but the enqueue logic skips drafts. This is a documentation issue introduced by the diff.

The staging smoke gate wording is actually accurate. Draft PRs correctly skip indexing entirely. The human gate item 9 in RQ8 execution describes the intended behavior when a PR transitions to draft or closed, not an automatic autostart trigger.

The "Draft PR autostart" smoke table entry directly contradicts the enqueue logic, which skips drafts. This risks operator confusion during smoke tests.

The @revy review command creates jobs for open PRs. If a user closes the PR during indexing, the neutral path handles it correctly. The command is skipped for closed PRs.

It is unclear whether the ready_for_review webhook action actually triggers an enqueue.

ready_for_review does not enqueue the pipeline. Only opened and synchronize trigger it in github_tasks.

Draft-to-ready transitions do not trigger pipeline enqueuing unless a push occurs. The neutral finalize path handles pipelines started before the PR was closed or drafted. A potential bug exists where a finalized pipeline run could leave orphaned state without a linked review_run. There is also uncertainty about whether get_db_context auto-commits; if finalize succeeds but the session rolls back, the database and GitHub would become inconsistent.

The pipeline check ID is stashed during the first session commit. Finalize resolves it from the database in a second session. This two-session flow appears correct.

Checking get_db_context for commit behavior.

Transaction splitting is working correctly. Session 1 commits the stash, and session 2 resolves the check_run_id from the database.

Finalize ordering is correct. Index failures trigger finalize before review preparation. Neutral outcomes finalize correctly. Pending reviews return empty outcomes and stay in progress.

A ConflictError edge case could leave the check stuck in_progress if another review path does not complete the pipeline. The elif chain priority logic is being reviewed to confirm benign handling for pending reviews, conflicts, and incomplete jobs.

Manual triggers and missing pipeline entries are handled correctly. A converted-to-draft job that finishes indexing during synchronization still triggers neutral finalization. The index failure path is being checked for duplicate finalize calls.

The index failure path is confirmed safe. The finalize call is guarded by a pipeline run ID check, so duplicate finalization cannot occur.

The first test uses a single database context and does not exercise transaction splitting. A command trigger closed during indexing follows the same neutral finalization path.

When GitHub is disabled, the neutral finalize path is a no-op because the check was never created. Stash skipping is also skipped when no GitHub check run ID exists. The refactor of finalize_pipeline_github_check_failure into a wrapper preserves existing callers.

The old finalize function had a full implementation that is now shared with the wrapper. This removes duplicate code. Failure behavior remains identical. Tests are being checked for regressions.

The RQ9 deliverable requires test_github_pipeline_trace.py in the pytest command, yet no tests were added for neutral finalize.

Missing test coverage is not a production bug. The finalize flow correctly uses the database session: record_index_pipeline_step flushes, finalize_neutral reads within that session, and the context manager commits at exit.

The logic paths for fail_pipeline_check and neutral_finalize_check are mutually exclusive, so no conflict exists. Enqueueing happens outside the database session, which is safe. The next concern is whether maybe_enqueue skips closed PRs when triggered by a user comment.

Evaluating the realistic path for neutral finalize.

Identified the core bug fix: if a PR converts to draft or closes during indexing, the index finishes against a stale revision and prepare_review falls back to neutral finalize. The smoke test wording "Draft PR autostart" is misleading but not a logic error. Confirmed no logic bug exists where index failure finalization collides with prepare_review.

Confirmed that prepare_review_after_index exits early when the job status is not completed. A failed index finalizes using job.error_message, yet prepare_review_after_index still runs afterward; this is wasteful but harmless. Enqueue_review_run correctly does not execute after neutral finalize. Neutral finalize runs within the same transaction as record_index.

A database commit failure after a successful GitHub API call could leave the PR neutral on GitHub while the job retries. This follows a pre-existing pattern across all finalize calls. The transaction split test is weak because both mocked database contexts share the same session object. The split test using separate setup and index sessions is stronger.

Confirmed the index failure test avoids duplicate finalization. The _mark_failed path only handles full-run failures, not index job failures.

The refactor did not break existing import paths. The review_tasks finalize module still uses the failure wrapper. The update_check_run helper correctly sets the completed status. Migration files 0001 through 0026 exist on main. Documentation contradictions between AGENTS.md and the waves README remain to be checked.

Migration 0026 likely already exists on main from PR #50. The waves README table contains duplicate rows for Review quality and RQ9 hardening, but this is not a documentation contradiction. BUGBOT references agents/prompts/ for VERB and OUTPUT_FORMAT, yet Greptile only includes ROLES.md. This creates a scope contradiction between BUGBOT and Greptile. The RQ9 execution item 5 aligns with Greptile because it only lists CURSOR_AGENT_WORKFLOW and ROLES.

The BUGBOT prompt inconsistency is a low-severity documentation issue rather than a production bug. The human gate execution aligns with RQ9 requirements. The index revision test correctly enqueues the review run when a review_run_id exists even without a pipeline trigger source.

Before RQ9, draft skips returned empty outcomes and checks remained stuck in_progress. RQ9 now finalizes drafts to neutral instead. The draft check runs after loading the revision and pull_request, so a missing revision triggers a failure rather than a neutral outcome. This preserves data integrity.

Manual admin indexes exit early without finalization. Command triggers with a closed PR during indexing still neutralize correctly. The elif branch for a failed job with no review_run_id is unreachable because the completed status guard already filters those cases.

Confirmed that duplicate neutral and publish finalization cannot occur together, since no review run is created for neutral outcomes. A ServiceUnavailable error during review run creation correctly triggers failure finalization. A scenario where the review run succeeds but linking to the pipeline fails is unlikely, as it would throw and cause the entire run to fail.

Confirmed that exceptions during _run roll back the review run in session 2, and the enqueue happens only after a successful run. The index session rollback scenario is pre-existing, not introduced by this change. Now beginning to list newly introduced bugs.

A stray "Not to forget;" comment was accidentally committed alongside the changeset. It is unrelated to RQ9 and carries low-to-medium severity. The RQ9 deliverable lists a new test file, yet no corresponding test was added, leaving the feature partially untested.

The new split-context test mocks a failed index job rather than exercising an actual transaction rollback. It therefore does not prove the G10 check survives rollback, despite its name suggesting otherwise.

The execution item 4 test asserts two database contexts and one finalize call, yet it never verifies that the stashed check ID resolves correctly. This is a test gap, not a production bug. When the index job fails, the failure finalize runs before prepare_review_after_index; when the index succeeds, neutral finalize runs instead.

Confirmed that pipeline_run_id is only set for pipeline triggers, so non-pipeline triggers with a pipeline_run_id are impossible. When a command trigger completes indexing while a pending review exists, the outcome stays empty and the check remains in_progress; a stale pending review would leave the check orphaned, but this is a pre-existing issue. Now checking whether admin indexing offers an alternative path for draft PRs.

Checking whether a neutral finalize run might skip enqueueing a review.

A race condition could leave the PR state stale if a webhook updates it after indexing begins. Reopening a closed PR during indexing is another edge case that might skip the review.

The finalize_pipeline_github_check_failure refactor appears safe. No remaining references to the old function name were found. The my-commands diff contains an accidental note and a stray newline. This qualifies as accidentally committed code.

The unchecked recovery checklist items are expected WIP and not a bug. The migration status in waves/README.md is slightly inconsistent, but clear enough.

prompts/README.md contains an incorrect relative path to CURSOR_AGENT_WORKFLOW.md. From the prompts directory, the link should use four parent-directory levels (../../../utils/) rather than three.

A broken relative path to CURSOR_AGENT_WORKFLOW.md appears on line 18.

The same broken relative path appears on line 83. The ../../utils/ reference resolves to a non-existent directory. PHASES.md is being checked next.

Verifying paths in the review-pipeline agents prompts README.

Found a path bug in docs/review-pipeline/agents/prompts/README.md. It incorrectly uses ../../utils instead of ../../../utils/ to reach docs/utils. Other README files correctly resolve this path.

The prompts/README.md path bug appears at lines 83 and 18. A documentation issue is confirmed. Backend logic is being reviewed for production bugs. A scenario where a successful review sets neutral while enqueuing seems impossible. The index_tasks first test shows a job with completed status and no autostart leaves pipeline_run_id as None because trigger_source is unset. This may be an ORM default.

Review complete. Now considering one final scenario.

A race condition exists between the index job and the prepare_review step. Another worker could update the draft flag via webhook after the index ends but before prepare_review reads the pull request state.

The race condition is unlikely in practice because the index job is slow. A separate issue remains: a review could be enqueued for a PR that later becomes a draft. This is not a regression.

There is also an incomplete acceptance criterion. The function finalize_pipeline_github_check_neutral is missing from test_github_pipeline_trace, though this is not a runtime bug.

The staging smoke documentation contradicts itself. It claims draft PRs trigger autostart, yet the enqueue logic skips them. This is a new documentation bug.

The recovery checklist references undefined requirements. The "Human gate S4" and "AS2" entries are missing from the diff.

Broken links were introduced in the prompts README.

No logic bug was found. The neutral finalize path correctly covers both draft and closed pull requests. The index failure finalization uses the correct database session.

A worker dying between session commits is a pre-existing orphan risk. The neutral finalize flow records the index step as completed before the GitHub neutral check, keeping the pipeline trace consistent. The publish path will not run. The backend implementation appears sound.

RQ9 H2 claims a test proves rollback survival, but the test only mocks a failed job without verifying that the stashed check ID is reused from the first session commit. This is a documentation mismatch rather than a code bug. The elif ordering in the finalize logic was also reviewed.

If create_review_run succeeds but the subsequent Celery enqueue crashes, the check stays orphaned in_progress. This is a pre-existing issue. Calling prepare_review after a failed job is harmless.

The XML format from the system prompt takes precedence over the user's table request. I am now checking whether a None github_check_run_id paired with a set pipeline_run_id introduces a logic bug.

The None github_check_run_id case is safe. If the GitHub check start fails, the stash is skipped and the check is never created, so the neutral finalize path no-ops without leaving an orphan. If the start succeeds, the check ID is stashed in session 1 and resolved from the database in session 2.

A session-1 stash failure would roll back the pipeline run, leaving no persisted identifier. The waves README duplicates RQ9 in both the post-v1 table and the active row. The R9 tag review-quality-v1.1 is an optional versus execution fold into v1. The chain_of_thoughts export is likely intentional evidence rather than an accidental commit.

run_index_job returns a failed job without throwing. Finalization happens in _run at lines 89–94.

_mark_failed resolves the pipeline via index_job_id, so session 1 committing the run is safe. A mid-failure exception is unlikely because finalization happens right after the return.

Checking whether _mark_failed could double-mark the index job as failed. Also verifying if finalize_pipeline_github_check_for_index_job is safe to call from that path.

The diff contains no new bugs. Only pre-existing patterns are present.

Neutral finalize cannot run after failure finalize. The pipeline trace test changes were omitted. The autostart test expects literal draft behavior. This is a documentation bug. .cursor/BUGBOT.md omits RQ9 phase scope. It only states RQ9 hardening is active.

Greptile correctly covers RQ0 through RQ9.

agents/README incorrectly claims the wiring layer is RC0 shipped despite RQ9 expanding it. This is a minor documentation stalecy.

The staging smoke contradiction is actionable for the AS2 gate.

Manual triggers cannot reach neutral finalize. They skip pipeline setup entirely because their trigger is excluded from _PIPELINE_TRIGGERS, leaving pipeline_run_id as None.

The test test_prepare_review_after_index_skips_closed_pull_request does not assert pipeline_check_summary text like the draft test does. This is not a bug.

A conflict exists between the recovery checklist migration count and git status. Uncommitted work remains on docs/agent-work even though the checklist indicates migrations 0001–0026 are on main. This suggests the documentation is ahead of reality.

Migration 0026 is assumed to be on main from PR #50. The staging smoke contradiction is specific to the draft case and does not affect the neutral path. Documentation contradictions will be flagged in XML.

The RQ9 deliverable lists test_github_pipeline_trace.py but omits the corresponding tests. This is a process compliance issue under BUGBOT_RULES.

No unit test covers finalize_pipeline_github_check_neutral. The neutral finalize path shares code with failure finalize, so the missing test is low risk.

The conclusion parameter accepts "neutral" correctly. GitHub API usage confirms this.

A manual index job cannot trigger autostart. These are mutually exclusive triggers.

Draft pull requests are skipped by maybe_enqueue on the opened action. Ready-for-review status does not enqueue either. Commits on synchronize trigger enqueue only when the PR is ready. This is expected behavior.

The neutral path occurs when a PR becomes draft or closed after enqueue while indexing is still running. The staging smoke should state this.

The waves/README claim that RQ9 hardening precedes the human gate is consistent with BUGBOT and execution requirements. RQ9 must merge before the human gate for draft checks. No contradiction exists.

The agents/README states RC0 shipped in the wiring column, yet RQ9 expanded wiring. This is a low-severity documentation issue.

A broken link was introduced in prompts/README. The href points to a missing file. The my-commands file is in the diff and will be reported. Chain of thought exports are documentation evidence and will be skipped. No backend logic bugs were found.

A pre-existing data bug could leave a pending_review_id from a prior run unresolved. That would cause prepare_review_after_i to return empty and stall the check at lines 220–229.

Staging smoke draft autostart impossible
docs/review-pipeline/REVIEW_PIPELINE_STAGING_SMOKE_VALIDATION.md:173 · Severity: medium

Description
Broken workflow doc links
docs/review-pipeline/agents/prompts/README.md:18 · Severity: low

Description
Accidental note in my-commands
my-commands:33-34 · Severity: low

Description
RQ9 trace tests not added
docs/review-pipeline/waves/REVIEW_QUALITY_EXECUTION.md:340-345 · Severity: low

Description