Reviewing the three change areas: reading full implementations and related contracts.

Checking whether get_db_context auto-commits on exit.

Investigating five additional potential issues:

Whether a failed second transaction leaves a check run orphaned
State enum and unique constraints on github_finding_groups
Whether migration 0026 created github_finding_groups or it already existed
How the None revision case affects pipeline_run_id and triggers
Investigating the rollback orphaning issue to understand the original problem.

Checking whether a failed index job rolls back the entire transaction, leaving an orphaned GitHub check. Will read start_pipeline_github_check and run_index_job to trace the flow.

Checking run_index_job's rollback logic to verify the split transaction fix and catch any new bugs.

Beginning to analyze the code changes for potential bugs.

Checking index_tasks.py. The original code kept pipeline setup and job execution in one transaction. A rollback during job execution would orphan the GitHub check. The fix splits them into two transactions. The first commits the pipeline run and stashed check_run_id. The second handles the index job.

Checking edge cases in the split transaction. A crash between transactions could leave a pending GitHub check orphaned. An early return for non-pending jobs may leave pipeline setup incomplete. The revision=None path might also start a check without a stored pipeline_run_id.

ensure_pipeline_run is skipped when revision is None, so the missing pipeline_run_id is not a bug. Concurrent jobs are unlikely to collide. UUID and integer IDs survive across transactions safely. A trigger_source outside _PIPELINE_TRIGGERS simply skips pipeline setup in the first transaction. A failure before the first transaction commits rolls back cleanly.

A failure in the first transaction rolls back cleanly and prevents the second transaction from running. If the first transaction succeeds but the second fails, the pipeline remains committed while the job may enter a processing state. The next step is to examine how run_with_retryable_failure and _fail_index_job_after_chunk_work handle internal session rollbacks.

Confirmed that an internal rollback followed by a flush correctly commits the failed job state. The pipeline run ID from the first transaction remains valid for the second session, so pipeline step recording should work. A potential issue remains: if start returns None but the pipeline run was still created, github_check_run_id may be None, which record_index_pipeline_step might not handle properly.

The pipeline_run_id assignment is not a bug. It was already set before the change. A new concern is whether the index job might already be processing or failed from a prior attempt when the second transaction begins.

Duplicate pipeline runs are impossible because the first transaction commits the run and the second transaction skips creation. A deleted job between transactions triggers a NotFoundError that propagates to _mark_failed. Session isolation works correctly, and the stashed github_check_run_id is passed directly from memory to record_index_pipeline_step.

A failed stash inside the first transaction rolls back the entire transaction, so the pipeline run is never committed. The real risk is a successful GitHub check creation followed by a failed database commit. The check is created before the first transaction commits, leaving an orphaned in_progress check on GitHub if the commit fails. This pre-existing vulnerability is not introduced by the split.

The first test does not mock session.get for the first transaction. It only patches run_index_job. The AsyncMock returns a MagicMock by default. The job lookup in the first session may fail.

The test job is a MagicMock with no trigger_source attribute. This causes the first transaction to skip pipeline setup entirely, since trigger_source is missing from _PIPELINE_TRIGGERS.

Examining finalize_pipeline_github_check_for_index_job to understand how reconciliation behaves when old groups are superseded by migration.

The migration superseded all active groups globally. Because compute_fingerprint already excludes messages, reconcile will generate new fingerprints that no longer match old group rows. This risks creating duplicate ACTIVE groups alongside existing ones.

Superseding old groups prevents duplicate active groups. Resolved groups remain unaffected because the migration only targets active groups.

New groups are safe from duplicate key violations because their fingerprints differ from superseded rows. The unique constraint on pull_request_id and fingerprint also prevents two new active groups from sharing the same fingerprint. However, the lookup query filters solely by fingerprint without checking state, which could allow a superseded group to be reactivated.

The old fingerprint included the message, while the new formula excludes it. Because the fingerprints differ, superseded groups cannot collide with new findings. The risk of reactivation through fingerprint-only lookup is therefore minimal.

The migration updates all active groups system-wide. Active findings keyed with the new fingerprint will be superseded and hidden from the API until the next reconcile run. This is intentional per D10-M; the trade-off is temporary visibility loss for noise reduction.

Downgrading the migration does not restore superseded groups to active. This is a pre-existing pattern issue rather than a new bug. The supersede update is not reversed in the downgrade script, leaving active findings hidden. This is a potential edge case that may be acceptable. The supersede logic runs after pipeline table creation at the end of the upgrade sequence.

The migration sequence is correct: the resolution_status column is added before the supersede update. The supersede raw SQL uses the string literal 'superseded', so the enum value must match exactly.

A key finding concerns stash_pipeline_github_check_run_id in the first transaction.

The pending index step created in the first transaction is correctly updated by the second transaction. The G10 fix remains sound even if the index job rolls back.

A potential bug was identified. If run_index_job crashes between transactions, the pending step remains stuck indefinitely. The GitHub check would stay in_progress. Retrying the Celery task might create a duplicate check run on GitHub. The idempotency of build_check_run_external_id needs verification.

Checking whether a superseded group lookup with a matching fingerprint could still occur after migration.

Migration converts all active groups to superseded. Old fingerprints remain in the database. New reconciliation uses a different fingerprint formula. Identical fingerprints between old superseded groups and new active groups cannot occur. SHA256 collisions are ignored.

The group lookup query does not filter by state. Even if a superseded group shares a fingerprint with a new finding, the new finding uses a different fingerprint after migration, so a new active group is created instead.

Migration prevents duplicate active groups from old fingerprints. A new edge case remains: if a group was created with the new fingerprint formula before migration ran, it stays active. On the next reconcile, the lookup would find this active group and create another new group with the same fingerprint.

The lookup logic reactivates superseded groups that match a fingerprint. This prevents duplicate active groups. Old superseded groups with legacy fingerprints are unaffected because new reconciles generate different fingerprints. Groups created after D10 but before migration are properly superseded.

Confirmed the unique constraint on pull request and fingerprint prevents duplicate rows. Verified that index_tasks exits early when the job record is missing, so the second database transaction never runs.

Two additional bugs surfaced in index_tasks. When the trigger source is not in _PIPELINE_TRIGGERS, the first transaction commits an empty session with no useful work performed. When the revision is None but the trigger is in _PIPELINE_TRIGGERS, pipeline_run_id remains unset and the second transaction still proceeds to run the index.

A missing revision in pipeline triggers causes run_index_job to fail with revision_not_found and skip pipeline finalization. This behavior existed before the change.

The job is fetched twice across separate transactions. A deletion between fetches raises NotFoundError.

If another worker processes the job first, the second transaction returns early. It still records the pipeline step, which may incorrectly mark the check as failed.

Duplicate Celery deliveries are a pre-existing race condition, not introduced by this diff.

A critical concern emerged: run_index_job performs a session rollback inside the second transaction. Pipeline_run records committed in the first transaction are unaffected. However, the rollback may prevent record_index_pipeline_step from seeing the pipeline_run_id generated in the first transaction.

Confirmed that committed pipeline_run records remain visible to _get_index_pipeline_step, resolving the earlier rollback concern.

A rollback inside run_index_job could lose enqueue state, but this is unrelated to the current change.

Migration only affects active groups, leaving resolved ones untouched. Concurrent reconcile during migration is a rare race that is acceptable for the migration window.

Publish deltas and G9 resolution_status may briefly drop active group counts after migration, since all active groups are superseded until the next review. This is intentional per D10-M optional supersede.

Findings still point to superseded groups until the next reconcile on that pull request. The API hides superseded groups via list_reconciled, but underlying finding rows retain the old group_id.

The first transaction in index_tasks never checks whether the job is already completed or failed. It still performs pipeline setup for pipeline triggers. This appears to be a new bug.

The missing status check is not a new bug. The old pipeline setup also ran before run_index_job without verifying the job was pending.

The split worsens the pre-existing bug. Previously, pipeline setup and run_index_job ran in the same session, so a rollback in run_index_job would roll back the pipeline setup too. Now the two operations are in separate sessions, so the rollback only undoes the second transaction's work. Pipeline setup therefore survives the rollback, and a retry of a completed job may still start a new GitHub check.

The split makes duplicate task retries worse. The first transaction recreates the pipeline and starts a new GitHub check. The second transaction skips the job entirely. This leaves the original check orphaned.

The split introduces a new orphaning risk. If the first transaction starts a GitHub check but crashes before committing, the check is orphaned and the pipeline row disappears. This is a pre-existing external side effect issue. Finalize calls that rely on the index_job_id still succeed because the pipeline was already committed.

Reading the dogfood documentation on the rollback orphan G10 issue and the index_tasks fix.

The dogfood document indicates that _fail_index_job_after_chunk_work with rollback was the fix for partial chunk commits.

Rollback in the same transaction as pipeline setup would orphan the G10 check. The new fix splits transactions. Now checking local_bugbot chain of thoughts for orphaned in_progress checks around the split.

Re-examining migration supersede for a potential bug.

Migration supersede appears safe. Old superseded groups retain their original fingerprints, so new findings cannot match them. Only one active group remains per fingerprint. Need to verify whether publish counts active groups using the reconciled list.

Stale group references in published comments until the next reconcile is expected D10-M behavior, not a regression. Now checking whether the migration defines the state column as a PostgreSQL enum or varchar.

The migration SQL correctly uses the string 'superseded', matching its enum value.

Confirmed rollback commits the failed job status flush. Verified pipeline step transitions and zero-duration step handling.

Verified pipeline trigger logic excludes webhooks. Confirmed internal flushes before commit. Noted a minor UUID inconsistency in failure marking. Flagged a test gap for split transactions.

Checking the default trigger_source on GitHubIndexJobORM.

Reconcile lookup logic appears safe. A unique constraint guarantees exactly one row per PR and fingerprint.

Migration superseding all active groups creates a temporary UI gap. Judge and publish code is being checked for assumptions that active groups will not be bulk superseded.

Resolved findings are correctly left untouched by migration. A partial deploy mismatch between old worker code and new API code is flagged as a deployment concern. A potential transaction bug in index_tasks is being traced where an exception during _run might commit the first transaction while leaving the second transaction unfinalized, risking orphaned checks.

The transaction bug in index_tasks is not a real issue. mark_permanent_failure opens a fresh database session and finalizes the check after the second transaction rolls back. Even an uncaught exception during run_index_job itself is handled safely.

A new risk in index_tasks is emerging. If an exception occurs during record_index_pipeline_step after the index job has already completed, the first transaction commits the pipeline while the second transaction rolls back the index work. This leaves the job marked completed in memory but the database unchanged, creating an inconsistent state. Previously, both operations ran in a single session, so a rollback would undo everything together.

The transaction bug in index_tasks is harmless. If an exception occurs after run_index_job flushes its data, the second transaction rolls back the entire job, including completed chunks and status. The pipeline in the first transaction remains intact, and mark_permanent_failure finalizes the check as failed. This preserves consistency rather than leaving orphaned data.

The transaction concern is a pre-existing pattern, not a new bug from splitting transactions. In the new split, run_index_job commits at the end of the second transaction block. Because the entire block runs inside one get_db_context session, a failure after that commit cannot occur. The rollback behavior matches the old single-transaction approach.

The migration SQL is verified as safe. The github_finding_groups.state column is non-null with a default of active, so the UPDATE statement will not encounter NULL values.

A large UPDATE during migration could lock the table on massive deployments. This is treated as a potential edge case rather than a confirmed bug.

Reconcile logic is verified safe. Reactivating a superseded group with a matching fingerprint correctly marks other active peers as superseded. The fingerprint lookup finds superseded groups and reactivates them properly, skipping resolved findings.

The reconcile logic is confirmed correct. Resolved findings are skipped, superseded groups are reactivated, and active groups fall through to the else branch. The next step is checking whether the old fingerprint previously included the message.

The docstring change is purely documentation and introduces no logic bugs.

Migration ordering appears correct: supersede runs before the resolution_status column is added. A fresh database would safely update zero rows. One remaining edge case under review is whether github_check_run_id is re-read from the database inside transaction two.

The check_run_id consistency concern is resolved. Transaction two reads the value from the manifest artifact stashed by transaction one, so the ID remains consistent across both transactions. The stash is only invoked when the ID is non-null, preventing any mismatch.

The None check ID path is safe. Without a GitHub check, no orphaned status exists. HTTP errors during start are handled correctly. The next concern is whether create_check_run could be called twice on retry.

The create_check_run call passes external_id, which may cause GitHub to upsert existing check runs on duplicates.

Duplicate Celery tasks could create duplicate checks if external_id does not deduplicate. This appears to be a pre-existing issue.

After migration, github_publish may briefly show zero active findings because all groups are inactive. This is expected D10-M impact rather than a bug in the migration itself.

A potential bug exists where reconcile creates duplicate groups. The lookup might find a superseded group with a different fingerprint and spawn a new one while the old superseded group remains visible through findings. Publish may load groups by finding group_id instead of filtering by active state. Need to check how github_publish loads groups.

run_publish_job includes superseded groups in its publish query. compute_check_conclusion and build_summary_markdown filter to active groups only. Resolved groups may be published without being marked active.

After migration, superseded groups are excluded from the publish query entirely. Resolved groups remain in the result set. Check conclusions default to "success" when no active groups exist.

A real bug is emerging. If migration supersedes all active groups before publish, the publish query returns zero active groups. The check conclusion defaults to success, so GitHub reports green despite critical findings existing. This is a concrete migration-induced regression.

The migration race is an edge case. It only triggers during deployment or re-publishing old reviews after migration. Normal new reviews work correctly. The supersede update is a new change. It is intentional per D10-M. Yet reporting success while findings still exist on the run is wrong.

Severity: medium. The race condition only affects in-flight reviews during the deploy window before the next reconcile.

Supersede prevents duplicate active groups from appearing in publish summaries. The false green check during migration is a production bug.

Reconsidering whether to report this. D10-M treats the optional supersede pass as acceptable noise, and the peer review explicitly accepts one-time regrouping on the next reconcile. A false green check during migration may fall under intentional behavior rather than a new bug. The user's instruction to avoid speculative problems also applies.

The false success is a side effect of the intentional supersede pass. Reconcile and publish run in the same pipeline, so the migration race is narrow. Already reconciled reviews awaiting publish are the real risk. G10 indexes checks as in_progress and publish finalizes them.

Publish and reconcile are separate Celery tasks, so migration can run between them. I am also checking whether the in-flight index transaction split introduces additional migration risks.

Resolved groups with identical fingerprints to new findings are handled safely. Reconcile skips reactivation and the active filter suppresses resolved groups from publish summaries.

A minor test gap in test_index_tasks was noted but dismissed as non-production.

A potential unique constraint conflict is being examined for reactivating superseded groups after migration when new groups already exist.

Migration supersede prevents duplicate active groups from appearing in the UI. The false green check during the migration race is a real logic bug, not speculation. It will be reported.

The documented D10-M gap—publish between migration and reconcile—may be acceptable noise rather than a bug.

The index_tasks transaction split was audited. Detached objects are not reused across transactions. Pipeline run IDs are only assigned after successful commits, and exceptions are caught gracefully.

A database error during the first transaction rollbacks cleanly. The job marks permanent failure and the check never starts. No new production bugs were found. The transaction split is now being checked against the local_bugbot rollback orphan issue to confirm it was the intended fix.

A local bugbot trace flagged a bug where failed embeds without rollback commit chunks. This was fixed by adding a _fail_index_job_after_chunk_work rollback.

The split transaction fix prevents rollback from orphaning the G10 check. Without the split, rollback in the same session would undo the pipeline stash, leaving the check in progress on GitHub while the database cannot locate the check_run_id. The split correctly pairs with rollback.

The split transaction introduces a regression. Duplicate tasks may now create two GitHub check runs. The first task commits the pipeline and starts a check. The second task skips the non-pending index job. It then records the step and finalizes the job. This produces a second check run on GitHub. Previously the pipeline and check were committed in the same transaction.

The duplicate check-run regression is not a new bug. It mirrors the pre-split behavior. Failed jobs with stale in-progress checks could accumulate. New revision pushes likely spawn fresh jobs with new IDs.

Downgrade migrations were checked and do not introduce logic bugs for upgrades. Reconcile correctly reactivates superseded groups after bulk supersede. D10 deduplicates findings with identical fingerprints, titles, files, and lines. Backfilling fingerprints during migration is unnecessary because supersede serves as the alternative.

The immediate in-progress check run is intentional G10 behavior. Concurrent worker conflicts are unlikely. I will search for tests covering the two-transaction pattern and migration supersede.

Inspecting prepare_review_after_index for fail_pipeline_check behavior and verifying index_tasks correctly handles draft PRs after these changes.

A draft PR returns ReviewAfterIndexOutcome() without calling fail_pipeline_check. The check therefore remains in_progress.

The parking lot item is pre-existing and unrelated to this diff. Now checking whether github_pipeline_runs allows duplicate pipeline runs.

The index_job_id column lacks a unique database constraint. ensure_pipeline_run_for_index_job prevents duplicate inserts by looking up the record before inserting.

A race condition exists between concurrent transactions. Both may see no existing pipeline and attempt insertion. The missing unique constraint on index_job_id allows duplicate rows. This could spawn two GitHub checks instead of one.

Checking whether a migration already enforces a unique index on index_job_id.

The missing unique constraint on index_job_id for pipeline_runs is pre-existing and unrelated to this diff.

Reconsidered the migration supersede logic. Delayed publish tasks on reconciled reviews now return empty lists because groups are superseded. This is a direct consequence of the migration. Findings remain on github_findings tied to superseded groups.

The empty success is misleading. A pending publish on a reconciled review shows a green pipeline check with empty findings, hiding critical issues from superseded groups. The peer review accepts this corner case.

This is intentional behavior. The migration suppresses old groups, and publish correctly concludes from active groups only. The false-success edge case is a deployment procedure gap rather than a code bug.

The false-success bug only occurs if migration runs after reconcile but before publish. If migration runs before reconcile, reconcile creates fresh active groups and publish succeeds normally. This narrow rolling-deploy window is a MEDIUM severity edge case.

A clearer bug was found in index_tasks. TX1 commits the pipeline, but TX2 returns early for non-pending jobs without recording the pipeline step or finalizing the run. This leaves the pipeline in an incomplete state.

Duplicate deliveries to index_tasks are handled safely. If the job is already completed, the second worker records completion. If the job is still processing, the second worker skips the pipeline step and records the processing status. The concern is whether the pipeline step is recorded at all when the job is not pending.

The pipeline step status logic is pre-existing and not split-specific. Processing jobs are recorded as failed pipeline steps while the index is still running.

A key new concern is process termination. If the process dies after TX1 commits the pending step but before TX2 begins, the pipeline step remains pending and the run cannot be finalized.

The separate transaction commits expose a new failure mode. Previously, a process kill before TX1 committed rolled back the entire run. Now, if the process dies after TX1 commits but before TX2 begins, the pipeline step persists as pending and the check remains in_progress. Because max_retries is zero, Celery does not retry the task, leaving the job orphaned and the check stuck indefinitely.

The explicit commit after pipeline setup introduces a new durability boundary. Previously, a process kill before the final commit rolled back the entire run. Now, a kill after TX1 commits but before TX2 begins leaves the pipeline step orphaned and the check stuck indefinitely. This is a new bug introduced by the change. Severity is low to medium.

Celery redelivery recovers the pipeline step. If the worker dies after TX1 commits but before TX2 begins, the task is not acknowledged and Celery retries it. TX1 reuses the existing pipeline, and TX2 runs the index. The job stays pending, which is acceptable. The earlier concern about an orphaned pending step is mitigated by this recovery path.

No high-confidence bugs were found. The split transaction fix correctly prevents G10 rollback orphans. The migration supersede logic is correct per D10-M, and downgrade behavior is standard.

When the revision is missing, the pipeline never starts and run_index_job fails cleanly. A deleted revision between the two transactions is unlikely. The next check is whether record_index_pipeline_step preserves the stashed github_check_run_id when the step is created as pending in TX1.

Confirmed that record_index_pipeline_step preserves the stashed github_check_run_id. The manifest.update call does not overwrite existing keys, so a check run ID saved during TX1 survives into TX2 even when the parameter is None.

Migration atomicity is safe. Alembic transactions ensure the pipeline table exists before supersede runs.

A new concern: GitHub judge outcomes might still reference superseded groups. The judge checks group state != resolved, so superseded groups could still be judged.

The judge may still process superseded groups, but this is pre-existing and unrelated to the diff. The final assessment is empty: no new bugs were found. The pipeline split is confirmed correct, since the second transaction needs the pipeline committed in the first.

Verified that _mark_failed correctly finalizes the pipeline after TX1 commits. If TX2 raises an exception before _fail_index_job_after_chunk_work returns, the transaction rolls back and the pipeline from TX1 remains intact. If the function returns normally, TX2 commits the failed job, records the pipeline step, and finalizes the run. Both paths are safe.

The test mock setup is verified correct. The migration intentionally targets all tenants globally without a workspace filter. Running the migration twice is harmless because subsequent runs find no remaining active groups to supersede.

Ruled out a rollback edge case. Calling session.rollback() inside _fail_index_job_after_chunk_work does not corrupt the session state for prepare_review_after_index later in the same transaction block.

D10-M indicates no backfill is required. The supersede pass is optional per peer review.

Verified the reconcile logic handles superseded groups correctly. Old superseded groups receive new rows with fresh fingerprints. Migration does not trigger unique constraint violations because old and new fingerprints differ.

Confirmed duplicate groups cannot form during reconcile. Multiple findings sharing one fingerprint update the same existing group. One potential edge case remains: index_tasks might create a pipeline for a failed job if the first transaction lacks a pending status check.

Requeuing a failed Celery task creates a pipeline before checking job status. The skip logic then returns the failed job. Recording marks it failed. Re-finalizing an already-failed check is harmless. Completed jobs are recorded as completed without triggering a finalize failure.

A new concern emerged: re-dispatching an index task for an already-completed job could leave an orphan in_progress check. The first transaction creates a new GitHub check before the skip logic sees the completed job. If no review is enqueued, the check remains stuck in progress until publish. This may be a regression from the split.

The orphaned check issue existed before the split too. The old code committed the check inside the same transaction that later skipped the completed job. The new split does not worsen this; it merely exposes the same pre-existing behavior.

Bugbot found no bugs.