# Architecture peer review — pass 2 (delta)

**Plan folder:** `docs/review-pipeline/judge-thinking-blocks/`  
**Date:** 2026-08-23  
**Scope:** delta — pass-01 highs + mediums after fix-agent locks  
**Reviewer:** architecture-peer-review skill  
**Baseline:** [JUDGE_THINKING_BLOCKS_FINDINGS.md](../../JUDGE_THINKING_BLOCKS_FINDINGS.md), [JUDGE_THINKING_BLOCKS_GENERAL_PLAN.md](../../JUDGE_THINKING_BLOCKS_GENERAL_PLAN.md) (post pass-01 edits)

## Verdict

Review complete: **0 critical**, **1 high**, **0 medium**, **1 low**.  
**BLOCK create-execution-plan:** **yes** — D6 still names the existing JSON-invalid `parse_error` `judge_json_invalid`; the tree and tests use `judge_json_invalid`. One-line lock in findings + general plan; do not rename the live code.

## Prior pass

[pass-01-2026-08-22.md](./pass-01-2026-08-22.md) — BLOCK create-execution-plan: yes (D2/D4 raise-site, P3 94.3% ceiling).

## Verified (locks vs codebase)

| Claim | Code evidence |
|:---|:---|
| Helper is still first-block-only (P0 not started) | `_extract_message_text` — `anthropic_review.py:208–230` |
| Review run does not catch `ValueError` / `JudgeParseError` | `run_review_run` `:1331` — `(httpx.HTTPError, ServiceUnavailableError)` only |
| `JudgeParseError` is `ValueError` | `judge_llm_errors.py:16–22` |
| Judge HTTP except is HTTP + SUE only (wrap must record extra) | `_post_judge_anthropic_messages` `:331–333` |
| Profile fallback already catches `JudgeParseError` | `_post_judge_with_profile_fallback` `:513` |
| Retry fires only on `JudgeParseError` / `judge_outcome_invalid` | `call_judge_with_optional_retry` `:92–109` |
| Empty `content: []` is already SUE in helper | `:210–215` — matches D5 |
| Live JSON-invalid code is `judge_json_invalid` | `judge_llm_errors.py:39–51`; tests `test_anthropic_review.py:206`, `test_github_finding_judge.py:171` |

## Rollup (critical / high)

| Severity | Section | Title |
|:---|:---|:---|
| high | D6 / P2 | Existing parse code is `judge_json_invalid`, not `judge_json_invalid` |

## Pass-01 items

| ID | Status | Notes |
|:---|:---|:---|
| high — D2/D4 raise-site | **resolved** | D2: helper never raises `JudgeParseError`. D4: helper returns `""` on non-empty content with no text; **`_post_judge_anthropic_messages` only** maps `""` → `JudgeParseError` (`judge_empty_text`). Review maps `""` → SUE. Q6 locked. P0/P1 split matches. |
| high — P3 94.3% ceiling | **resolved** | D9: 88+45=133/141=94.3%. P3 Goal + scope-out: pass at ≥95% **or** classified residual; do not reopen JSON-contract / add retries for 0.7pt (D7). Experiment Fail column updated. |
| medium — empty `content: []` | **resolved** | D5 + Q7: stays SUE in helper; 0 live. |
| medium — D8 INFO vs manifest | **resolved** | D8 + Q8: worker INFO never dumps `signature`; manifest truncated dump stays; no strip pass. |
| medium — P1 transport extra / fallback | **resolved** | P1 in: record transport extra on empty-text wrap (`content_block_types` home); keep `_post_judge_with_profile_fallback`. Out: do not raise `JudgeParseError` from helper; do not remove gateway→direct. |
| medium — P3 split evidence | **resolved** | P3 in: metrics re-run **plus** text-later vs thinking-only SQL/script; today’s printer does not split. |
| low — line cites | **resolved** | Retry helper and extract cites still match. |

**P0 — no new gaps** (raise-site and `[]` SUE locked; walk vs wrap split is executable).  
**P1 — no new gaps** except D6 string (shared with P2).  
**P3 — no new gaps** (D9 + split evidence locked).

## Per-section findings (delta)

## D6 / P2 — parse_error strings

| Severity | Area | Finding |
|:---|:---|:---|
| high | findings / general plan | **JTB-D6** and P2 Goal say operators distinguish `judge_empty_text` vs **`judge_json_invalid`**. Same D6 cell also says “Do not rename `judge_json_invalid`.” Those two names are not the same. Code raises `ValueError("judge_json_invalid")` then `JudgeParseError` with that code (`judge_llm_errors.py:39–51`). Manifests and unit tests already persist `judge_json_invalid`. **Lock:** additive code is `judge_empty_text` only; keep **`judge_json_invalid`** unchanged. Do not introduce `judge_json_invalid`. |

## Cross-cutting

| Severity | Area | Finding |
|:---|:---|:---|
| low | execution sequencing | P0 makes the helper return `""` for thinking-only; P1 maps review `""` → SUE. If P0 ships alone, Anthropic `complete_review` would return `""` and `run_review_run` would fail as JSON `ValueError` (`:1201`) instead of SUE. Live review is Moonshot (cold path). Execution: same PR for P0+P1, or include review `""`→SUE in P0 as behavior-preserving. Not a baseline hole. |

### General-plan contract checklist

| Check | |
|:---|:---|
| Header links findings | pass |
| Locked decisions match findings (D1–D9, Q5–Q8) | **fail — D6 code token vs tree** |
| Each phase Goal / Scope / Deliverables / Depends on | pass |
| D2/D4 raise-site | pass |
| D5 empty `[]` | pass |
| D8 INFO vs manifest | pass |
| D9 94.3% + classified residual | pass |
| P1 transport extra + keep profile fallback | pass |
| P3 split evidence | pass |
| Next step | still “pass 2 then create-execution-plan” — after D6 one-liner, next is **`create-execution-plan`** |

## Open questions

None remaining from pass-01 (Q6–Q8 locked). JTB-Q5 stays calibration in P3.

## Pre-execution checklist

- [x] Helper taxonomy-neutral; D4 wrap in `_post_judge_anthropic_messages` only.
- [x] Empty `[]` stays SUE (D5).
- [x] D8: INFO never dumps signature; manifest truncated dump stays.
- [x] P1 records transport extra on wrap; do not remove gateway→direct fallback.
- [x] P3: ≥95% **or** classified residual; ceiling 94.3%; split text-later vs thinking-only.
- [ ] **D6:** write `judge_json_invalid` (existing) everywhere; drop `judge_json_invalid`.
- [ ] Then `create-execution-plan` for P0–P3 under `docs/review-pipeline/judge-thinking-blocks/waves/`.

**Next:** fix agent one-line D6 in findings + general plan → architecture-peer-review pass 3 (spot/delta) **or** human ACK that waves will use `judge_json_invalid` → **`create-execution-plan`**.
