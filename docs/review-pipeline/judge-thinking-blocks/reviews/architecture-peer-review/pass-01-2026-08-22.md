# Architecture peer review — pass 1

**Plan folder:** `docs/review-pipeline/judge-thinking-blocks/`  
**Date:** 2026-08-22  
**Scope:** full  
**Reviewer:** architecture-peer-review skill  
**Baseline:** [JUDGE_THINKING_BLOCKS_FINDINGS.md](../../JUDGE_THINKING_BLOCKS_FINDINGS.md), [JUDGE_THINKING_BLOCKS_GENERAL_PLAN.md](../../JUDGE_THINKING_BLOCKS_GENERAL_PLAN.md), [program README](../../README.md), [LIVE_TRAFFIC_FINDINGS.md](../../../staging-validation/LIVE_TRAFFIC_FINDINGS.md) LT-1  

## Verdict

Review complete: **0 critical**, **2 high**, **4 medium**, **2 low**.  
**BLOCK create-execution-plan:** **yes** — lock where `JudgeParseError` is raised (D2 vs D4) and that P3 may pass below 95% with documented residual (extract-only ceiling on the baseline window is 94.3%).

## Prior pass

First pass — n/a.

## Verified (docs match codebase)

| Claim | Code evidence |
|:---|:---|
| `_extract_message_text` reads only `content[0].text` | `backend/app/integrations/anthropic_review.py:208–230` — empty / non-dict / missing text → `ServiceUnavailableError("Anthropic response invalid")` |
| Same helper on judge HTTP and Anthropic review | `_post_judge_anthropic_messages` `:317`; `_post_anthropic_messages` `:368` (`complete_review` `:541`) |
| Retry only `JudgeParseError` / `judge_outcome_invalid` | `call_judge_with_optional_retry` — `github_finding_judge.py:92–109`; SUE extract miss never retries |
| Profile fallback catches SUE **and** `JudgeParseError` | `_post_judge_with_profile_fallback` `:513–524`; gateway `allow_judge_profile_fallback=True` |
| Manifest stores `raw_response_text` + `parse_error` | `JudgeCandidateArtifact` `:70–71`; `record_judge_pipeline_step` `_candidate_payload` — `github_pipeline_trace.py:771–785` |
| Empty-content unit test only | `test_extract_message_text_empty_content_includes_preview` — `test_anthropic_review.py:362–368`; no thinking-first fixture |
| Bedrock extract is separate (`content[0]`) | `bedrock_review.py:_extract_text` `:35–66` then `parse_judge_payload` — correctly out of scope |
| RG-6 withhold | `github_publish.py:1305–1317` — judge candidate without outcome row is not publishable |
| Metrics script groups by `outcome` / any `parse_error` | `judge_json_contract_staging_metrics.py:48–50`, `:61–78` — no hardcoded `"Anthropic response invalid"` filter |
| JSON-contract observability on this tree | `JudgeParseError`, transport context, retry helper present (prerequisite usable; JC staging sign-off still FAIL at 62.4%) |

## Rollup (critical / high)

| Severity | Section | Title |
|:---|:---|:---|
| high | P0 / P1 / D2–D4 | Raise `JudgeParseError` on the **judge caller**, not from shared `_extract_message_text` |
| high | P3 | Extract-only ceiling on the baseline window is **94.3%** — lock P3 pass = ≥95% **or** documented residual |

## Per-section findings

## P0 — Content-block extract (foundations)

| Severity | Area | Finding |
|:---|:---|:---|
| high | general plan / codebase | **D2** (one helper for judge + `complete_review`) and **D4** (no-text → `JudgeParseError`) conflict if D4 is implemented inside `_extract_message_text`. `JudgeParseError` is a `ValueError` (`judge_llm_errors.py:16–22`). `complete_review` → `_post_anthropic_messages` does not catch it. `run_review_run` only maps `(httpx.HTTPError, ServiceUnavailableError)` (`github_review.py:1331`); `_call_llm` `:982–1031` does not catch `ValueError`. A thinking-only Anthropic **review** body would become an unhandled exception instead of a failed run with `ServiceUnavailableError`. Live review is Moonshot, but D2’s edge case (“Review path via Anthropic must keep working”) fails if the helper grows a judge-only type. **Lock:** helper walks `content` and returns concatenated non-empty `text` (D1–D3); HTTP / non-JSON / non-dict stay SUE (D5); **no-text stays SUE in the helper** (or a neutral empty result). `_post_judge_anthropic_messages` (only) maps no-text → `JudgeParseError` so `call_judge_with_optional_retry` can fire. |
| medium | codebase | `_post_judge_anthropic_messages` `:331` catches `(httpx.HTTPError, ServiceUnavailableError)` only. A `JudgeParseError` raised at `:317` skips `_record_judge_transport_failure` (`:257–279`) and leaves transport context as `{}` from `judge_finding` `:573`. P2’s optional `content_block_types` on transport extra cannot land unless this except tuple (or a wrap-after-extract) is in P1. Same lock as the row above. |
| low | findings | Line cites `github_finding_judge.py` ~90–136 match retry helper `:82–136`; anthropic extract cites ~208–230, ~317, ~368 still match. |

**Verified OK:** P0 in/out split (walk vs retry taxonomy) matches the 45 text-later cases; legacy `content[0].text` test (`test_complete_review_returns_content` `:62–77`) is the right regression; verification judge (`github_finding_closure.py` via `call_judge_with_optional_retry`) inherits the shared helper with no extra phase.

---

## P1 — Empty text is a parse miss

| Severity | Area | Finding |
|:---|:---|:---|
| high | general plan | Same D2/D4 split as P0 — P1 must name the **judge-only wrap**, not “extract raises `JudgeParseError`”. Existing retry tests already prove `JudgeParseError` from `call_judge_llm` retries once (`test_call_judge_with_optional_retry_retries_parse_failure` `:1088–1115`) and that SUE does not (`except` in retry helper `:108–109`). |
| medium | findings | Empty `content: []` is SUE today (`:210–215`) and was **0/53** live. D4 “walk with no text” vs D5 “non-dict body stay SUE” does not say whether `[]` is `judge_empty_text` or remains transport. Lock one; 0 live so either is fine. |
| low | codebase | After D4, thinking-only with gateway+direct is: profile fallback (already catches `JudgeParseError` `:513`) **then** the one schema-reminder retry (up to 4 HTTP calls). D7 “no second model” means do not **add** a model — do not disable existing `_post_judge_with_profile_fallback`. Document in execution so P1 tests do not “fix” fallback away. |

**Verified OK:** `judge_retry_count = 1` on double parse fail (`:127–136`, artifact `:368`) matches P1 “retry_count visible on the second miss”; no extra retry count or second model in this phase.

---

## P2 — Trace codes

| Severity | Area | Finding |
|:---|:---|:---|
| medium | general plan / D8 | `JudgeParseError.response_text` is persisted as manifest `raw_response_text` (`judge_failure_trace_fields` `:68–69`). Live previews already include truncated `signature` blobs. D8 “do not log thinking `signature` blobs” vs P2 “failure artifacts distinguishable in DB” needs an explicit rule: **worker INFO** never dumps signature (keep JT-Q4); **manifest** either (a) keep truncated dump as today, or (b) strip `signature` / `thinking` keys when building `response_text` / `response_body_preview`. Optional `content_block_types` is enough for operators if D6 codes are distinct (`judge_empty_text` vs `judge_json_invalid` vs HTTP message). Do not add a migration — candidate JSON is schemaless (`github_pipeline_trace.py:793–798`). |
| low | codebase | Metrics persistence % does not filter on parse_error string (`:48–50`). Renaming `"Anthropic response invalid"` off thinking-first will not break the 95% numerator. Recent-failure print will just show new codes. |

**Verified OK:** P2 no new tables; D6 additive codes (`judge_empty_text`) without renaming `judge_json_invalid`; RG-6 untouched.

---

## P3 — Staging validation & doc sync

| Severity | Area | Finding |
|:---|:---|:---|
| high | general plan | Baseline: 88/141 with `outcome`, 53 misses, 45 text-later + 8 thinking-only. Perfect extract of all 45 **and** all 45 parse as judge JSON → **133/141 = 94.3%**, below the stated ≥95% (needs ≥134). P3 Goal already says “or documented residual”, but the experiment table Fail column is only “stuck ~62% with thinking-first `parse_error`”. **Lock:** extract-only ceiling on this window is 94.3%; P3 **passes** at ≥95% **or** when residual is classified (thinking-only / invalid JSON / other) and JSON-contract is **not** reopened (D7). Do not add retries or a second model to chase 0.7 points. |
| medium | codebase | `judge_json_contract_staging_metrics.py` has no text-later vs thinking-only split. P3 “split in evidence” needs extra SQL (e.g. `raw_response_text` contains `"type": "text"` vs thinking-only) or a script flag — not a plain re-run of today’s printer. |

**Verified OK:** JTB-Q5 stays calibration-only; LT-2–LT-6 stay out; human gate belongs in P3 not P0–P2; next step `create-execution-plan` under `waves/` matches findings.

---

## Cross-cutting

| Severity | Area | Finding |
|:---|:---|:---|
| — | locked parity | JTB-D1–D8 and JTB-Q1–Q4 match findings registry ↔ general plan header. JTB-Q5 open in both. **Gap is D2 vs D4 raise-site**, not a missing ID. |
| — | prerequisite | JSON-contract P1 manifest fields + P4 one parse retry and transport logging are in this tree. JC program README still says `feat/judge-json-contract` / human gate pending; staging already measured 62.4% with those fields ([LIVE_TRAFFIC](../../../staging-validation/LIVE_TRAFFIC_FINDINGS.md) LT-1). Not blocking this program. |
| — | persistence | No new Alembic — P2 optional keys on existing JSON artifact. |
| — | i18n / FE | Backend-only — correct. |

### General-plan contract checklist

| Check | |
|:---|:---|
| Header links findings + program README / authority | pass |
| Locked decisions match findings registry | **fail until D2/D4 raise-site locked** |
| Each phase: Goal, Scope in/out, Deliverables, Depends on | pass |
| Prerequisites cited as on `main` / deployed | pass enough (observability in tree + live window used it) |
| New persistence → handwritten Alembic | n/a (none) |
| Named APIs exist or marked new | pass |
| Test paths exist or named new | pass (new fixtures in P0/P1) |
| Human/ops gate in P3 not mixed into code phases | pass |
| No TBD in decisions table | pass (Q5 calibration) |
| Next step matches findings (`create-execution-plan`) | pass |
| D6 codes vs metrics grouping | pass (no hardcoded old string) |
| RG-6 / D7 no extra retries / no JSON-contract rewrite | **at risk if P3 treats 94.3% as hard fail** |

## Open questions (max 3)

1. **D8 manifest:** strip `signature` from `JudgeParseError.response_text` / previews, or keep today’s truncated dump and rely on D6 codes + optional `content_block_types`?
2. **Empty `content: []`:** `judge_empty_text` (`JudgeParseError`) or leave `ServiceUnavailableError` (0 live)?

## Pre-execution checklist

- [ ] Findings + general plan: extract helper is taxonomy-neutral (D1–D3, D5); D4 wrap lives in `_post_judge_anthropic_messages` (or equivalent judge-only site), not `_extract_message_text`.
- [ ] P1: `_post_judge_anthropic_messages` records transport extra on empty-text (so P2 `content_block_types` has a home); do not remove gateway→direct fallback.
- [ ] P3: pass = ≥95% **or** documented residual; record extract-only ceiling 94.3% on the 2026-08-07 window; D7 holds.
- [ ] P2: D8 rule for signatures (INFO vs manifest); D6 code names (`judge_empty_text` vs keep `judge_json_invalid`).
- [ ] P3 evidence query: text-later vs thinking-only (script or SQL), not averaged.
- [ ] Then `create-execution-plan` for P0–P3 under `docs/review-pipeline/judge-thinking-blocks/waves/`.

**Next:** fix agent edits findings + general plan per this file → architecture-peer-review pass 2 (delta) → **`create-execution-plan`** when BLOCK is no.
