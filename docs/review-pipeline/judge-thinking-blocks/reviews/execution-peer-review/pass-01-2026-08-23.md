# Execution peer review — pass 1

**Plan folder:** `docs/review-pipeline/judge-thinking-blocks/`  
**Date:** 2026-08-23  
**Scope:** full (P0–P3)  
**Reviewer:** execution-peer-review skill  
**Baseline:** [FINDINGS](../../JUDGE_THINKING_BLOCKS_FINDINGS.md), [GENERAL_PLAN](../../JUDGE_THINKING_BLOCKS_GENERAL_PLAN.md), [EXECUTION index](../../waves/JUDGE_THINKING_BLOCKS_EXECUTION.md), codebase paths cited below

## Verdict

Review complete: **0 critical**, **0 high**, **2 medium**, **2 low**.  
**BLOCK phase-execution:** no — waves match locked D1–D9 and live symbols; P0+P1 same-PR coupling is explicit.

## Prior pass

None — first execution review after create-execution-plan. Architecture pass-02 D6 high (`judge_json_invalid` stays) is locked in findings, general plan, and all four wave files.

## Rollup (critical / high)

| Severity | Phase | Title |
|:---|:---|:---|
| *(none)* | | |

## Per-phase findings

## P0 — `docs/review-pipeline/judge-thinking-blocks/waves/JUDGE_THINKING_BLOCKS_P0_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | P1.1-style: P0.0 SSOT + Greptile `--write`/`--check` + `test_generate_greptile_files.py` present. No gap. |
| low | clarity | P0.3 `-k complete_review` depends on new test names matching `complete_review` (live symbol in `anthropic_review.py:527`). Fine if LOOP names tests after the function. |

**Verified OK:** `_extract_message_text` (`anthropic_review.py:208`) is first-block-only today; helper must stay taxonomy-neutral (D2). Empty `content: []` already SUE (D5). Review wrap `""`→SUE in P0 matches pass-02 half-wire lock. `run_review_run` except is `(httpx.HTTPError, ServiceUnavailableError)` at `github_review.py:1331`. P0.0 paths exist (`.revy/review-context.json`, `.greptile/files.json`, `.cursor/BUGBOT.md`). 5 subphases; phase gate + Next present. Ship-with-P1 locked.

## P1 — `docs/review-pipeline/judge-thinking-blocks/waves/JUDGE_THINKING_BLOCKS_P1_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | codebase | P1.1 **Files** lists `judge_llm_errors.py`. `JudgeParseError(code, *, response_text=)` already accepts any code string (`judge_llm_errors.py:16–22`). Production edit is **not** required there unless tests live in that module — wrap belongs in `_post_judge_anthropic_messages` only. Do not add a second JSON-invalid token while touching that file. |
| low | execution contract | P1.1 can drop `judge_llm_errors.py` from Files if unchanged; tests in `test_anthropic_review.py` already assert `code == "judge_json_invalid"`. |

**Verified OK:** Wrap site is `_post_judge_anthropic_messages` (`:282`); except at `:331` is HTTP+SUE only — extra must be set on the wrap (P1.2) before `JudgeParseError` escapes. `_post_judge_with_profile_fallback` already catches `JudgeParseError` (`:513`). Retry helper `call_judge_with_optional_retry` retries `JudgeParseError` / `judge_outcome_invalid` (`github_finding_judge.py:82–95`). Additive token `judge_empty_text` only; live `judge_json_invalid` unchanged (D6). `allow_judge_profile_fallback` is the real profile flag (`anthropic_review.py:135`). Review SUE regression is P1.4. 4 subphases; gates named.

## P2 — `docs/review-pipeline/judge-thinking-blocks/waves/JUDGE_THINKING_BLOCKS_P2_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| *(none blocking)* | | |

**Verified OK:** `_judge_failure_log_extra` / `_judge_failure_artifact` exist (`github_finding_judge.py:139`, `:160`). `judge_failure_trace_fields` maps `JudgeParseError.code` (`judge_llm_errors.py:64`). Metrics script `judge_json_contract_staging_metrics.py` groups by any `parse_error` (no hardcoded old-string filter — architecture pass-01). D8 INFO: current `logger.info` extras are transport fields only (`:293`, `:326`) — P2.3 is an audit + test, not a new dump. Split evidence correctly deferred to P3. Do not rename `judge_json_invalid`. 4 subphases; Next → P3.

## P3 — `docs/review-pipeline/judge-thinking-blocks/waves/JUDGE_THINKING_BLOCKS_P3_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | P3.2 allows script **or** memo SQL for the 45 vs 8 split. Locked enough (general plan allows either). LOOP must not skip the split. |
| low | docs | Validation memo is created in P3 (not now). Template on disk is `docs/review-pipeline/staging-validation/TEMPLATE_STAGING_VALIDATION.md`. |

**Verified OK:** D9 94.3% ceiling + classified residual; D7 no JSON-contract chase. Human gate stops LOOP. `changelog.json` skipped at `frontend/src/data/changelog.json`. Metrics command uses `DATABASE_SSL_INSECURE=1` (runbook). Doc-sync table lists README, findings, LT-1, staging index, judge README. Next: none. 4 subphases (3.1–3.4).

## Execution contract checklist

- [x] Header links general plan + findings
- [x] Goal + decisions locked per phase
- [x] Out of scope lists later phases
- [x] 3–6 subphases each (P0: 5 including P0.0; P1–P3: 4)
- [x] What / Files / Deliverable + verify command
- [x] Phase gate pytest (backend-only)
- [x] No migration
- [x] Human gate on P3
- [x] Next chain; P3 Next none
- [x] No TBD / no A-or-B except P3.2 evidence vehicle (script vs SQL) — both allowed
- [x] P0.0 SSOT + generated `files.json` + Bugbot
- [x] Final phase doc-sync; changelog skip justified
- [x] D6 live token `judge_json_invalid` in every wave file

**Next:** `phase-execution` from [JUDGE_THINKING_BLOCKS_P0_EXECUTION.md](../../waves/JUDGE_THINKING_BLOCKS_P0_EXECUTION.md). Mediums are implementation cautions, not blockers.
