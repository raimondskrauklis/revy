# Execution peer review — pass 2

**Plan folder:** `docs/review-pipeline/judge-thinking-blocks/`  
**Date:** 2026-08-23  
**Scope:** full (P0–P3)  
**Reviewer:** execution-peer-review skill  
**Baseline:** [FINDINGS](../../JUDGE_THINKING_BLOCKS_FINDINGS.md), [GENERAL_PLAN](../../JUDGE_THINKING_BLOCKS_GENERAL_PLAN.md), [EXECUTION index](../../waves/JUDGE_THINKING_BLOCKS_EXECUTION.md), [architecture pass-02](../architecture-peer-review/pass-02-2026-08-23-delta.md)

## Verdict

Review complete: **0 critical**, **0 high**, **0 medium**, **1 low**.  
**BLOCK phase-execution:** no — waves still match D1–D9 and live symbols; P0+P1 same-PR coupling is explicit; D6 live token `judge_json_invalid` is consistent across all four files.

## Prior pass

[pass-01-2026-08-23.md](./pass-01-2026-08-23.md) — BLOCK phase-execution: no. This pass re-reads all four waves independently (user invoked full execution-peer-review on the program README).

## Rollup (critical / high)

| Severity | Phase | Title |
|:---|:---|:---|
| *(none)* | | |

## Per-phase findings

## P0 — `docs/review-pipeline/judge-thinking-blocks/waves/JUDGE_THINKING_BLOCKS_P0_EXECUTION.md`

**P0 — no gaps found.**

**Verified OK:**
- Header links findings + general plan; D1–D5 raise-site split is explicit (helper returns `""`; no `JudgeParseError`; empty `[]` stays SUE; review `""`→SUE in P0; judge wrap deferred to P1). Same-PR ship with P1 matches general-plan half-wire lock.
- Paths exist: `_extract_message_text` / `_post_anthropic_messages` / `complete_review` in `backend/app/integrations/anthropic_review.py:208`, `:336`, `:527`; `run_review_run` except is `(httpx.HTTPError, ServiceUnavailableError)` at `github_review.py:1331`. P0.0 files (`.revy/review-context.json`, `.agent/review-context.json`, `.greptile/files.json`, `.cursor/BUGBOT.md`) and `backend/scripts/generate_greptile_files_from_review_context.py` (`--write`/`--check`) + `tests/unit/test_generate_greptile_files.py` exist.
- 5 subphases (P0.0–P0.4); What/Files/Deliverable + pytest; phase gate ruff + extract tests; Next → P1; D6 token not introduced here (`judge_json_invalid` only in out-of-scope). Concatenate with no extra separator matches D1.

## P1 — `docs/review-pipeline/judge-thinking-blocks/waves/JUDGE_THINKING_BLOCKS_P1_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| low | execution contract | P1.1 **Files** lists `judge_llm_errors.py`. `JudgeParseError(code, *, response_text=)` already accepts any code (`judge_llm_errors.py:19–22`). Production edit there is unnecessary; wrap belongs only in `_post_judge_anthropic_messages`. Same caution as pass-01 — do not invent a second JSON-invalid token while in that file. |

**Verified OK:** Wrap site `_post_judge_anthropic_messages` (`:282`); except `:331` is HTTP+SUE only — extra must be set on wrap (P1.2) via `_set_judge_transport_context` (`:69`) / `get_judge_transport_log_fields` (`:37`). Fallback already catches `JudgeParseError` (`:513`) with `allow_judge_profile_fallback` (`:135`). Retry helper retries `JudgeParseError` / `judge_outcome_invalid` (`github_finding_judge.py:92–95`); `judge_retry_count` exists (`:135`). Additive `judge_empty_text` only; live `judge_json_invalid` unchanged. Review SUE regression is P1.4. Same-PR as P0. 4 subphases; Next → P2. `content_block_types` matches findings D8 / general plan.

## P2 — `docs/review-pipeline/judge-thinking-blocks/waves/JUDGE_THINKING_BLOCKS_P2_EXECUTION.md`

**P2 — no gaps found.**

**Verified OK:** `_judge_failure_log_extra` / `_judge_failure_artifact` (`github_finding_judge.py:139`, `:160`); `judge_failure_trace_fields` maps `JudgeParseError.code` (`judge_llm_errors.py:64`). D6 additive `judge_empty_text` vs live `judge_json_invalid`. Metrics script groups by any `c->>'parse_error'` with no hardcoded token filter (`judge_json_contract_staging_metrics.py:50`, `:64`). D8: current `logger.info` extras are transport fields (`anthropic_review.py:293`, `:326`) — P2.3 is audit + test, not a new dump. Split evidence correctly deferred to P3. 4 subphases; Next → P3.

## P3 — `docs/review-pipeline/judge-thinking-blocks/waves/JUDGE_THINKING_BLOCKS_P3_EXECUTION.md`

**P3 — no gaps found.**

**Verified OK:** D9 94.3% ceiling + classified residual; D7 no JSON-contract chase. Human gate stops LOOP. Memo path + template `docs/review-pipeline/staging-validation/TEMPLATE_STAGING_VALIDATION.md` exist. Metrics command matches `--since` and `DATABASE_SSL_INSECURE` (`judge_json_contract_staging_metrics.py:356–383`). Split is required (script **or** memo SQL — general plan allows either; LOOP must still produce the 45 vs 8 split). Changelog skip `frontend/src/data/changelog.json` is real. Doc-sync table covers program README, findings Q5, LT-1, staging index, judge README. Next: none. 4 subphases (3.1–3.4).

## Execution contract checklist

- [x] Header links general plan + findings
- [x] Goal + decisions locked per phase
- [x] Out of scope lists later phases
- [x] 3–6 subphases each (P0: 5 including P0.0; P1–P3: 4)
- [x] What / Files / Deliverable + verify command
- [x] Phase gate pytest (backend-only)
- [x] No migration
- [x] Human gate on P3
- [x] Next chain; P3 Next none
- [x] No TBD / no A-or-B except P3.2 evidence vehicle (script vs SQL) — both allowed
- [x] P0.0 SSOT + generated `files.json` + Bugbot
- [x] Final phase doc-sync; changelog skip justified
- [x] D6 live token `judge_json_invalid` in every wave file

**Next:** `phase-execution` from [JUDGE_THINKING_BLOCKS_P0_EXECUTION.md](../../waves/JUDGE_THINKING_BLOCKS_P0_EXECUTION.md). Lows are naming/file-list cautions, not blockers.
