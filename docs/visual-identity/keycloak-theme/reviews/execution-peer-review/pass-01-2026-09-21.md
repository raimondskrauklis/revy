# Execution peer review — pass 1

**Plan folder:** `docs/visual-identity/keycloak-theme/`  
**Date:** 2026-09-21  
**Scope:** full (P0–P3, 4 files)  
**Reviewer:** execution-peer-review skill  
**Baseline:** [KEYCLOAK_THEME_FINDINGS.md](../KEYCLOAK_THEME_FINDINGS.md), [KEYCLOAK_THEME_GENERAL_PLAN.md](../KEYCLOAK_THEME_GENERAL_PLAN.md), [execution index](../README.md), codebase paths cited below  
**Architecture peer review:** [pass 2 delta](../architecture-peer-review/pass-02-2026-09-21-delta.md) — BLOCK: no

## Verdict

Review complete: 0 critical, 0 high, 8 medium/low.  
**BLOCK phase-execution:** no — all findings are clarity/wording/scoping items. No broken execution contract items.

## Rollup (critical / high)

None. No critical or high findings across all four execution files.

## Per-phase findings

### P0 — Inspector (read-only) — [`KEYCLOAK_THEME_P0_EXECUTION.md`](../KEYCLOAK_THEME_P0_EXECUTION.md)

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | **P0.1 delivers `python -m json.tool .revy/review-context.json > /dev/null` but doesn't say to also sync to `.agent/review-context.json`.** The "PR review context" section says to mirror, and "Files" lists both, but the **Deliverable** only checks `.revy/review-context.json`. The mirror copy to `.agent/review-context.json` has no verify command. Add `diff .revy/review-context.json .agent/review-context.json` to the deliverable. |
| medium | execution contract | **P0.1 `programs[].scope` is empty `[]` but the plan says "no backend/frontend code to scope."** The execution file says `scope: []` and the comment says scope is empty. Later phases (P1, P2, P3) produce `infra/keycloak/**` files. If P1 pushes code, scope should be `["infra/keycloak/**"]` (not empty) so the review tooling knows what to review. Either: (a) P1 should update the scope after it creates `infra/`, or (b) P0 should set `scope: ["infra/keycloak/**"]` now. Empty scope means Bugbot reviews nothing. |
| low | clarity | **P0.2 `docker exec revy-kc ls -la /opt/keycloak/themes /opt/keycloak/data/tmp` — two paths in one ls.** `ls -la dir1 dir2` works but could be clearer as two separate commands. Minor. |
| low | execution contract | **P0.4 `stylesCommon` test is documented as "scratched — not committed" but result goes into `P0_INSPECT.md`.** The P0.4 test itself is throwaway (correct for a smoke test), but the **result** needs to be committed in `P0_INSPECT.md` so P1 can read it. P0 gate includes "P0_INSPECT.md contains: … stylesCommon result" which covers this. No gap — just noting the distinction. |

**Verified OK:**
- Header links general plan + findings correctly (L5–6). ✓
- "Out of scope for P0" correctly defers CSS/messages/tree to P1, rsync/realm to P2, bake to P3. ✓
- 4 subphases — within 3–6 contract. ✓
- Each subphase has **What** / **Files** / **Deliverable**. ✓
- Phase gate is manual (correct — read-only, no code yet). ✓
- P0.2 `kc.sh --version` uses correct absolute path `/opt/keycloak/bin/kc.sh --version`. ✓
- P0.3 records all realm flags from the general plan. ✓
- P0.3 deliverable resolves Q12 `defaultLocale` — correct per general plan. ✓
- Deploy: "none — read-only. No container changes." Correct. ✓
- "Next" links to P1 execution file. ✓

### P1 — Theme package + local loop — [`KEYCLOAK_THEME_P1_EXECUTION.md`](../KEYCLOAK_THEME_P1_EXECUTION.md)

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | **P1.2 deliverable doesn't include contrast verification.** The general plan cross-cutting says "no pytest/Vitest" but the findings verification list includes visual QA passes. P1.2 is about writing CSS — the CSS itself should have the right hex. But `check_theme.sh` (P1.5) only checks "hex from token block + layout list", not contrast ratios. The general plan findings show WCAG 2.2 AA + APCA Lc ≥ 75 on key pairs — no automated check in P1 verifies this. Visual QA (P1.6) covers it manually, which is acceptable, but a note in P1.2 deliverable would help: "contrast verified visually in P1.6." Minor — not blocking. |
| low | execution contract | **P1.4 docker-compose says `ports: 8080:8080` — no `127.0.0.1` bind.** The production compose uses `127.0.0.1:8080:8080` for security. Local compose is for dev only, so publishing on `0.0.0.0:8080` is acceptable (access from host browser). But the `.env.example` with plaintext `KC_BOOTSTRAP_ADMIN_PASSWORD=admin` and public port could be a footgun if someone accidentally runs this on a shared network. No fix needed — local only — but worth noting. |
| low | clarity | **P1.2 "Cover all login-type pages (login, register, forgot, error, info, OTP, update password)."** — "update password" is not in the findings verification list (findings lists "login, register, forgot, error, info"). The general plan P1 scope says "login / forgot / error / info." "OTP" and "update password" are KC standard login-type pages — they'll inherit the same CSS, but the execution file should match the general plan's explicit QA scope. Not a gap (CSS covers them), just a slight scope mismatch. |
| low | clarity | **P1.6 "SPA callback simulation not needed (local only)."** — correct, but the P2 production QA **does** test OIDC callback. The P1→P2 handoff works, just noting for completeness. |

**Verified OK:**
- 6 subphases — within 3–6 contract. ✓
- P1.1 `theme.properties` matches findings B1 exactly. ✓
- P1.2 token block comment says "L63–106" — matches fixed general plan. ✓
- P1.2 CSS scope covers PF blue stripe kill, canvas, surface, ring, inputs 44px, CTA, links, danger, info, text — matches findings A1–A7. ✓
- P1.3 EN+LV messages with `check_theme.sh` key parity enforcement. ✓
- P1.4 local compose with theme cache flags off — matches findings C0. ✓
- P1.5 `check_theme.sh` checks for files, key parity, hex audit — matches findings B7. ✓
- Phase gate: `bash infra/keycloak/scripts/check_theme.sh` + visual screenshots. ✓
- Deploy: "first push" with commit message format — matches general plan push strategy. ✓

### P2 — Transport, apply, realm cutover — [`KEYCLOAK_THEME_P2_EXECUTION.md`](../KEYCLOAK_THEME_P2_EXECUTION.md)

| Severity | Area | Finding |
|:---|:---|:---|
| low | execution contract | **P2.2 `apply_theme.sh` lives in `infra/keycloak/scripts/` in the repo, but P2.4 runs it from `/mnt/revy_volume/keycloak-themes/apply_theme.sh`.** The script is committed in the repo at `infra/keycloak/scripts/apply_theme.sh`, but P2.4 says to run it from `/mnt/revy_volume/keycloak-themes/apply_theme.sh`. The P2.3 rsync only copies `infra/keycloak/themes/revy/`, **not** the `scripts/` directory. So `apply_theme.sh` won't exist at the target path. Fix: either (a) include `infra/keycloak/scripts/` in the P2.3 rsync, or (b) have P2.4 run the script from the repo path (e.g. scp it first or run from the operator's local checkout). |
| low | clarity | **P2.5 says "set `loginTheme=revy`. Record rollback command: `loginTheme` ← P0 value."** — the execution file doesn't say *where* the rollback command is recorded. The general plan says "documented in this folder." P2.5 deliverable says "Document rollback in `P0_INSPECT.md` appendix." Consistent with the general plan — but would be clearer in the **What** section of P2.5 rather than only in the deliverable. |
| low | execution contract | **P2.6 rollback test listed in "What" but not in "Deliverable."** P2.6 says "Rollback test: if needed, `loginTheme` ← P0 value (instant)." This is a test instruction, not a deliverable assertion. Unclear whether rollback testing is always required or only "if needed." The findings edge case says "Realm rollback: `loginTheme` is instant — record current value." Testing the rollback is prudent but should be explicit: either require it or mark it optional. |

**Verified OK:**
- 6 subphases — within 3–6 contract. ✓
- P2.1 uses `<deploy-user-from-P0>` and `<docker-gid-from-P0>` — correct per architecture review pass-02 resolution. ✓
- P2.2 backup step uses `date +%s` for timestamp — recoverable. ✓
- P2.2 restart waits for health check up to 60s. ✓
- P2.2 notes "Session impact per P0" — correct per architecture review resolution. ✓
- P2.5 sets i18n flags **before** `loginTheme=revy` — correct order (theme needs locale available). ✓
- P2.6 production QA covers login, forgot, error, register-if-enabled, EN+LV, 375px, password toggle, OIDC callback, admin stock — matches general plan. ✓
- Phase gate includes `curl` health check + visual + SPA callback + admin stock + rollback value known. ✓
- Human gate: "Confirm with ops before running, or run off-peak." Correct per session impact. ✓
- "Out of scope" correctly excludes P3 bake and policy changes. ✓

### P3 — Bake + docker-compose sync — [`KEYCLOAK_THEME_P3_EXECUTION.md`](../KEYCLOAK_THEME_P3_EXECUTION.md)

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | **P3.1 Dockerfile build requires `revy-keycloak:26.0` to exist locally. P3 decisions mention the prerequisite but P3.1 deliverable doesn't verify it.** The deliverable says `docker build -t revy-keycloak-themed:26.0 infra/keycloak/` succeeds. But if `revy-keycloak:26.0` doesn't exist on the build machine, the build fails. Add a pre-condition check: `docker inspect revy-keycloak:26.0 > /dev/null` or add a note that this must be built first from `deploy/keycloak/config/`. |
| low | execution contract | **P3.2 "do not execute on production — template only"** — the compose file edit changes the production compose template. If someone runs `docker compose up -d` from that directory later (e.g., for a KC version upgrade), the new bind-mount volume would be active. This is the **intent** (the entire point of P3), but the file notes "template only" and the ops docs (P3.3) cover it. The compose edit should have a comment that the bind-mount is **ready for the next deploy cycle** — not "don't execute." As written, it sounds like the mount should be commented out. Clarify: the volume line is active but the actual `docker compose up -d` is deferred. |
| low | clarity | **P3.3 "Add a section to `P0_INSPECT.md` (or a new `OPS_RECREATE.md`)."** — the **or** is ambiguous. Pick one and commit to it. The general plan says "document the ops-owned recreate option (C6)." A separate file is cleaner (P0_INSPECT is a redacted inspect output, not an ops runbook). Recommend `infra/keycloak/OPS_RECREATE.md`. |

**Verified OK:**
- 4 subphases — within 3–6 contract. ✓
- P3.1 Dockerfile explicitly separate from deploy Dockerfile — matches architecture review pass-02 resolution. ✓
- P3.1 `FROM revy-keycloak:26.0` with "No `kc.sh build`" — matches C5. ✓
- P3.2 bind-mount path is absolute `/mnt/revy_volume/keycloak-themes` — matches architecture review pass-02 resolution. ✓
- P3.2 comment notes "Bundled themes live in JARs — mounting the whole themes/ dir on top of JAR-provided themes is safe." Correct. ✓
- P3.4 doc sync updates README status rows with commit shas. ✓
- Phase gate: compose config validate + docker build + docker run verify. ✓
- Deploy: final push with PR title. ✓
- "Next: none — final phase. Program complete." Correct. ✓

## Execution contract checklist

| Check | P0 | P1 | P2 | P3 |
|:---|:---|:---|:---|:---|
| Header links general plan + findings | ✓ L5–6 | ✓ L5–6 | ✓ L5–6 | ✓ L5–6 |
| **Goal** + decisions locked for this phase | ✓ L8, L10–11 | ✓ L8, L10 | ✓ L8, L10–13 | ✓ L8, L10–13 |
| **Out of scope** lists later phases | ✓ L20–22 | ✓ L13–17 | ✓ L15–18 | ✓ L15–18 |
| **3–6 subphases** | ✓ (4) | ✓ (6) | ✓ (6) | ✓ (4) |
| Each subphase: **What** / **Files** / **Deliverable** | ✓ | ✓ | ✓ | ✓ |
| **Deliverable** includes verify command or concrete assertion | ✓ | ✓ | ✓ | ✓ |
| **Phase gate** — manual (no pytest) | ✓ (4 checks) | ✓ (2 checks) | ✓ (5 assertions) | ✓ (3 commands) |
| **Migration** subphase (no Alembic in this program) | N/A | N/A | N/A | N/A |
| **Human gate** if ops/sign-off | N/A (read-only) | N/A | ✓ (ops confirm) | N/A (template only) |
| **Next** → following execution file | ✓ L75 | ✓ L90 | ✓ L96 | ✓ L71 (final) |
| No TBD / no open options | ✓ (P0 closes Q12/Q14) | ✓ | ✓ | ✓ |
| **Final phase** doc sync | N/A | N/A | N/A | ✓ P3.4 |

## Open questions (max 3)

1. **P0.1 scope `[]` vs `["infra/keycloak/**"]`:** Should P0 set an empty scope (and P1 update it) or P0 set `infra/keycloak/**` upfront? Empty scope means Bugbot reviews nothing until P1 explicitly updates it.
2. **P2.4 `apply_theme.sh` runtime path mismatch:** The script lives at `infra/keycloak/scripts/apply_theme.sh` in the repo but P2.4 expects it at `/mnt/revy_volume/keycloak-themes/apply_theme.sh`. How should the script reach the droplet — extra rsync, scp, or inline paste?
3. **P0.4 `stylesCommon` test environment:** P0.4 tests `stylesCommon` locally with a minimal child theme. The general plan says KC 26.0 `start-dev`. Is the local compose from P1.4 used, or a standalone `docker run`? The execution file just says "mount into a local `start-dev` Keycloak 26.0" — could use a one-liner command example.

## Pre-execution checklist

- [ ] P0.1: add `.agent/review-context.json` mirror verification to deliverable (or fold into scope discussion)
- [ ] P0.1: resolve scope `[]` vs `["infra/keycloak/**"]` — set scope properly for Bugbot
- [ ] P1.2: consider adding a contrast verification note (visual QA in P1.6 covers it)
- [ ] P2.4: resolve `apply_theme.sh` path mismatch — ensure the script is on the droplet before invoking it
- [ ] P2.6: decide if rollback test is always required or optional
- [ ] P3.1: add `revy-keycloak:26.0` image existence check to deliverable
- [ ] P3.2: clarify compose bind-mount intent (active line, ops-ready, not executed until next cycle)
- [ ] P3.3: pick one output file (`P0_INSPECT.md` appendix or new `OPS_RECREATE.md`) — recommend separate `OPS_RECREATE.md`

**Next:** fix agent addresses the 8 findings; then re-review (execution-peer-review pass 2 delta) or `phase-execution` from P0 if none are blocking.