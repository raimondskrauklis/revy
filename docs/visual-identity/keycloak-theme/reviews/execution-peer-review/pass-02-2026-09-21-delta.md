# Execution peer review — pass 2 (delta)

**Plan folder:** `docs/visual-identity/keycloak-theme/`  
**Date:** 2026-09-21  
**Scope:** delta — pass-01 fix agent applied to all 4 execution files  
**Reviewer:** execution-peer-review skill  
**Baseline:** [KEYCLOAK_THEME_FINDINGS.md](../KEYCLOAK_THEME_FINDINGS.md), [KEYCLOAK_THEME_GENERAL_PLAN.md](../KEYCLOAK_THEME_GENERAL_PLAN.md), [execution index](../README.md), codebase paths cited below  

## Verdict

Review complete: 0 critical, 0 high, 2 medium/low.  
**BLOCK phase-execution: no** — all 8 pass-01 findings are resolved. Two new low items found: one rsync path mismatch on `scripts/`, one doc drift in P0 L14.

## Prior pass

[pass-01-2026-09-21.md](./pass-01-2026-09-21.md) — 0 critical, 0 high, 8 medium/low. Fix agent applied all 8 pre-execution checklist items across all 4 files.

## Verified — pass-01 findings resolved

| # | Pass-01 finding | Status | Evidence |
|:--|:---|:---|:---|
| 1 | P0.1 scope `[]` + no agent mirror verify | **Resolved ✓** | Scope `["infra/keycloak/**"]` in P0.1 What (L28); deliverable has `diff .revy/review-context.json .agent/review-context.json` exits 0 (L32) |
| 2 | P0.4 missing `stylesCommon` test command example | **Resolved ✓** | One-liner `docker run` command with volume mount + `KC_BOOTSTRAP_ADMIN_*` in P0.4 What (L58) |
| 3 | P1.2 missing contrast verification note | **Resolved ✓** | Deliverable ends with "WCAG 2.2 AA / APCA Lc ≥ 75 contrast verified visually in P1.6" (L36) |
| 4 | P1.2 CSS scope wording (OTP/update-password) | **Resolved ✓** | Lists "login, register, forgot-password, error, info" as explicit; "OTP, update-password, and other KC login-type pages inherit" (L32) |
| 5 | P2.3 rsync missing `scripts/` | **Resolved ✓** | Rsync includes `infra/keycloak/scripts/` (L43); deliverable checks both paths (L47) |
| 6 | P2.5 rollback location not in What | **Resolved ✓** | "Record rollback command in `P0_INSPECT.md` appendix: `loginTheme` ← P0 value" in What (L63) |
| 7 | P2.6 rollback test "if needed" → explicit | **Resolved ✓** | "**Rollback test (required):**" with full round-trip (revert → confirm stock → re-apply) (L75); deliverable says "rollback tested and reverted" (L77) |
| 8 | P3.1 missing `revy-keycloak:26.0` pre-condition | **Resolved ✓** | Deliverable starts with `docker inspect revy-keycloak:26.0 > /dev/null` (prerequisite image exists) (L28) |
| 9 | P3.2 bind-mount "template only" ambiguous | **Resolved ✓** | "The volume line is **active** — it will take effect on the next `docker compose up -d`" (L33) |
| 10 | P3.3 "or `P0_INSPECT.md`" ambiguity | **Resolved ✓** | Single file: `infra/keycloak/OPS_RECREATE.md`. "**Files:** New — `infra/keycloak/OPS_RECREATE.md`" (L43) |

## Per-phase findings (delta — only new or remaining issues)

### P0 — Inspector (read-only) — no new gaps from pass-01

**All 2 pass-01 findings resolved.** 

| Severity | Area | Finding |
|:---|:---|:---|
| low | clarity | **L14 PR review context still says `scope: []`.** P0.1 What (L28) correctly sets `scope: ["infra/keycloak/**"]`, but the PR review context summary at L14 still reads `scope: []`. The summary is informational (not actionable) but is stale. P0.1 already overrides it correctly — the summary just needs a sync. |
| — | pass-01 | P0.2 `ls -la dir1 dir2` — not changed (low-priority, noted "minor" in pass-01). Agent skipped this intentionally. Acceptable. |

**Pn — no new gaps** (all pass-01 findings resolved for P0).

### P1 — Theme package + local loop — no new gaps from pass-01

**All 3 pass-01 findings resolved.** 

**Pn — no new gaps** (all pass-01 findings resolved for P1).

### P2 — Transport, apply, realm cutover — one new path mismatch

**All 3 pass-01 findings resolved.** 

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | **P2.3 rsync copies `infra/keycloak/scripts/` as a directory — `apply_theme.sh` lands at `/mnt/revy_volume/keycloak-themes/scripts/apply_theme.sh`, but P2.4 expects `/mnt/revy_volume/keycloak-themes/apply_theme.sh`.** The rsync command `rsync -az --delete infra/keycloak/themes/revy/ infra/keycloak/scripts/ <ssh-target>:/mnt/revy_volume/keycloak-themes/` copies the `scripts/` **directory** into the target, producing `/mnt/revy_volume/keycloak-themes/scripts/apply_theme.sh`. P2.4 runs `bash /mnt/revy_volume/keycloak-themes/apply_theme.sh` — which won't exist. Fix: either change P2.4 to `/mnt/revy_volume/keycloak-themes/scripts/apply_theme.sh`, or rsync `infra/keycloak/scripts/apply_theme.sh` directly as a file (not the directory). Since the scripts dir is for the operator's reference, the simpler fix is P2.4 pointing at the scripts dir path. |

**Verified OK (P2 — unchanged from pass-01):**
- P2.1 uses from-P0 placeholders ✓
- P2.2 backup + restart + health check flow ✓
- P2.5 i18n before `loginTheme` ✓
- P2.6 rollback test now required ✓
- Phase gate + human gate ✓

### P3 — Bake + docker-compose sync — no new gaps from pass-01

**All 2 pass-01 findings resolved.** 

**Pn — no new gaps** (all pass-01 findings resolved for P3).

## Execution contract checklist (pass-02 delta — all pass-01 items still hold)

All contract items that passed in pass-01 still pass. No regressions introduced by the fixes.

## Open questions from pass-01 — status

| # | Question | Status |
|:---|:---|:---|
| 1 | P0.1 scope `[]` vs `["infra/keycloak/**"]` | **Resolved ✓** — scope is `["infra/keycloak/**"]` |
| 2 | P2.4 `apply_theme.sh` runtime path mismatch | **Partially resolved** — rsync now copies scripts/, but P2.4 path didn't update to match the new location (`/mnt/revy_volume/keycloak-themes/scripts/apply_theme.sh`) |
| 3 | P0.4 `stylesCommon` test environment | **Resolved ✓** — one-liner docker run command added |

## Pre-execution checklist

- [x] P0.1: scope `["infra/keycloak/**"]` + agent mirror verify — resolved
- [x] P0.4: `stylesCommon` test command example — resolved
- [x] P1.2: contrast verification note — resolved
- [ ] **P2.3/P2.4: rsync `scripts/` directory lands at `scripts/apply_theme.sh`, P2.4 expects `apply_theme.sh` — fix path mismatch**
- [x] P2.5: rollback location in What — resolved
- [x] P2.6: rollback test required — resolved
- [x] P3.1: `revy-keycloak:26.0` pre-condition — resolved
- [x] P3.2: bind-mount active wording — resolved
- [x] P3.3: single file `OPS_RECREATE.md` — resolved
- [ ] **P0 L14: `scope: []` in PR review context summary — sync to `["infra/keycloak/**"]`**

**Next:** fix the one medium (P2.3/P2.4 path mismatch) and one low (P0 L14 doc drift); then `phase-execution` from P0.