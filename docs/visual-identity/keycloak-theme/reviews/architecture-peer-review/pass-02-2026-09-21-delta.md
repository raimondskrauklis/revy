# Architecture peer review — pass 2 (delta)

**Plan folder:** `docs/visual-identity/keycloak-theme/`  
**Date:** 2026-09-21  
**Scope:** delta — sections changed per pass-01 fix agent  
**Reviewer:** architecture-peer-review skill  
**Baseline:** [KEYCLOAK_THEME_FINDINGS.md](../KEYCLOAK_THEME_FINDINGS.md), [KEYCLOAK_THEME_GENERAL_PLAN.md](../KEYCLOAK_THEME_GENERAL_PLAN.md), codebase paths cited below  

## Verdict

Review complete: 0 critical, 0 high, 2 medium/low.  
**BLOCK create-execution-plan: no** — all 5 pass-01 highs are resolved. Two low-level items remain: one stale line reference missed by the fix agent, one P3 scope detail.

## Prior pass

[pass-01-2026-09-21.md](./pass-01-2026-09-21.md) — 0 critical, 5 high, 5 medium/low. Fix agent was asked to resolve all 5 highs + 5 mediums.

## Verified (docs match codebase)

| Claim | Code evidence |
|:---|:---|
| L63–106 now used consistently | Findings L20, L141, L235, plan L17, L23, L58 — all `L63–106` ✓ |
| `--app-radius-md` 4px not dark-only | Findings A3, B6, Q8, plan Q8, P1 scope — noted "same in both modes" ✓ |
| P3 `infra/keycloak/Dockerfile` is separate | Findings C5 ("separate from deploy Dockerfile"), P3 scope ✓ |
| P2 `install` uses from-P0 variables | Plan P2 scope: `-o <deploy-user-from-P0> -g <docker-gid-from-P0>` ✓ |
| P3 bind-mount absolute path | Plan P3 scope: `/mnt/revy_volume/keycloak-themes:/opt/keycloak/themes:ro` ✓ |
| Session recorded in P0 deliverables | Plan P0 deliverables: "session persistence recorded"; findings parking lot L252 ✓ |
| `check_theme.sh` relaxed to hex+layout | Findings B7: "token block or documented layout hex list"; plan L37 cross-cutting ✓ |
| Register QA conditional | Plan P1 + P2: register QA only if `registrationAllowed=true` from P0 ✓ |
| P2 restart notes session impact | Plan P2 scope: "session impact per P0; brief blip accepted if Infinispan-only" ✓ |
| `kc.sh` command fixed | Plan P0 scope: `docker exec revy-kc /opt/keycloak/bin/kc.sh --version` ✓ |
| P0 deploy user + docker GID recorded | Plan P0 deliverables: "deploy user and docker GID resolved" ✓ |
| P0 `stylesCommon` test added | Findings B2 ("Verify during P0/P1 local loop"), plan P0 deliverables ✓ |

## Rollup (critical / high)

No critical or high findings remain from pass-01. All 5 resolved.

## Per-section findings (delta — only sections with changes or remaining items)

### Cross-cutting

**Pass-01 finding:** `check_theme.sh` hex policy too strict.  
**Status:** Resolved ✓. Plan L37 now says "allows hex from verbatim token block + documented layout hex list" — matches findings B7.

### P0 — Inspector (read-only)

**Pass-01 findings:** Stale `kc.sh` command, "local compose tag" in read-only phase, missing deploy user/session/`stylesCommon` tests.  
**Status:** Resolved ✓. P0 scope uses correct `kc.sh --version`, records deploy user + docker GID + session persistence + `stylesCommon` inheritance from local test, and doesn't claim to confirm local tag.

### P1 — Theme package + local loop

**Pass-01 findings:** Stale L65→L63, `radius-md` dark-only assumption, `stylesCommon` ambiguity, mute-text contrast, strict `check_theme.sh` hex, unconditional register QA.  
**Status:** All resolved ✓. L63–106 throughout. P1 scope says "no `stylesCommon` unless P0 test proves inherit is broken." Register QA conditional on P0 `registrationAllowed`.

### P2 — Transport, apply, realm cutover

**Pass-01 findings:** `<deploy-user>`/`<docker>` placeholders, unconditional register QA, session restart impact, `kc.sh` command, missing workflow file.  
**Status:** All resolved ✓ or delegated to P0. Placeholders use `<deploy-user-from-P0>` / `<docker-gid-from-P0>`. Register QA conditional. Restart notes "session impact per P0."

| Severity | Area | Finding |
|:---|:---|:---|
| low | general plan | **P2 scope references `<deploy-user-from-P0>` and `<docker-gid-from-P0>`** — these are templated names pointing at P0 values. During execution, the concrete values from the P0 artifact must be substituted. The plan correctly gates P2 on P0, but the names aren't runnable as-is. Acceptable — P0 is the fill-in step. |

### P3 — Bake + docker-compose sync (deferred)

**Pass-01 findings:** `FROM revy-keycloak:26.0` ambiguity, bind-mount path confusion.  
**Status:** Resolved ✓. P3 Dockerfile is explicitly separate, layers on built image with webhook JARs, no `kc.sh build`. Bind-mount is absolute path.

| Severity | Area | Finding |
|:---|:---|:---|
| low | general plan | **`revy-keycloak:26.0` must exist on the build machine.** P3 says "bakes locally and serves." Locally, the image is a tag from `docker compose build` in `deploy/keycloak/config/` — developer must build it first. Not blocking; execution plan can add a one-liner prerequisite note. |
| low | findings | **References L303 still says `L65–106`.** `frontend/src/styles/tokens.revy.css` — `html.dark` `--rv-*` scale (L65–106). This is the **only remaining stale line number** missed by the fix agent. Execution is unaffected (P1 scope says L63–106), but it's documentation drift. |

### Findings — stale reference

**Pass-01 finding:** Line references off by 2.  
**Status:** Partially resolved ✓ — all corrected to L63–106 except L303 in References section.

| Severity | Area | Finding |
|:---|:---|:---|
| low | findings | **L303: `L65–106` should be `L63–106`.** The References section listing `frontend/src/styles/tokens.revy.css` still cites `(L65–106)` — missed when the fix agent updated the other 6 references. |

## Open questions from pass-01 — status

| # | Question | Status |
|:---|:---|:---|
| 1 | `stylesCommon` inheritance behavior | **Delegated to P0** — findings B2 documents auto-inheritance hypothesis; plan P0 deliverables test with minimal child theme |
| 2 | Session persistence on production KC | **Delegated to P0** — P0 deliverables include "session persistence recorded"; P2 uses "session impact per P0" |
| 3 | P3 Dockerfile layering intent | **Resolved ✓** — P3 layers on `revy-keycloak:26.0` (built image with webhook JARs), not stock |

## Pre-execution checklist

- [ ] **Fix remaining stale line reference:** findings L303 `L65–106` → `L63–106` (one remaining)
- [x] All L65→L63 fixes applied (except the one above)
- [x] `--app-radius-md` 4px dark-only assumption corrected
- [x] P3 Dockerfile layering clarified
- [x] Deploy user/docker GID placeholders → from-P0
- [x] `stylesCommon` inheritance delegated to P0 test
- [x] `check_theme.sh` hex policy relaxed
- [x] Session persistence recorded in P0
- [x] Register QA conditional
- [x] `kc.sh --version` command fixed
- [x] P3 bind-mount path corrected to absolute

**Next:** `create-execution-plan` — all highs resolved, only one low (stale reference line) and one low (P3 build context note) remain. Neither blocks execution.