# docs/visual-identity/reviews/architecture-peer-review/pass-02-2026-09-19.md

# Architecture peer review — pass 2

**Plan folder:** `docs/visual-identity/`  
**Date:** 2026-09-19  
**Scope:** full (findings-only, after pass-01 highs locked; no general plan)  
**Reviewer:** architecture-peer-review skill  
**Baseline:** [VISUAL_IDENTITY_FINDINGS.md](../../VISUAL_IDENTITY_FINDINGS.md), [README.md](../../README.md), codebase paths cited below  

## Verdict

Review complete: 0 critical, 0 high, 4 medium/low.  
**BLOCK create-general-plan:** no — pass-01 highs are locked as VI-Q5 / VI-Q9 / VI-Q10; Greptile-look collision stays withdrawn. Remaining items are general-plan enumeration, not decision gaps.

Findings header still says “not baseline-ready / BLOCK until pass 2.” That status is stale after this pass; do not wait for another architecture pass before `create-general-plan`.

## Prior pass

[pass-01-2026-09-19-delta.md](./pass-01-2026-09-19-delta.md) (and [pass-01](./pass-01-2026-09-19.md)). Fix agent (human) locked the three remaining highs and kept VI-Q0 terminal console. Greptile: job rival; copy none of the UI.

## Verified (docs match codebase)

| Claim | Code evidence |
|:---|:---|
| `--app-info` aliases primary today | `tokens.revy.css:11` and `:19` — VI-Q10 is the overlay remap, not current paint |
| No workspace inbox API | `reviewer/api.ts` still per repository / PR; `dashboard/api.ts` audit only |
| ReviewerSummaryWidget is a CTA card | `ReviewerSummaryWidget.tsx:17–31` |
| Reviewer nav gated off until available | `ReviewerNavItem.tsx:15–17` — Track C must un-gate |
| Authenticated entry still `/dashboard` | `LandingPage.tsx:21`; `LoginPage.tsx:13`; `AuthCallbackPage.tsx:18` |
| Greptile captures (paths only) | `misc/image.png` (dark SaaS + mint bar); `misc/image copy.png` (light mint-pill hero) |
| Registry VI-Q0–Q10 all **locked** | findings `:270–284` — no TBD in the table |

## Rollup (critical / high)

| Severity | Section | Title | Status vs pass-01 / δ |
|:---|:---|:---|:---|
| — | VI-Q0 | Greptile occupies phosphor console | **resolved** (δ + findings `:50`, Q0) |
| — | VI-Q5 | Chevron as `>` glyph | **resolved** (Q5: custom wedge SVG; `>` layout only) |
| — | VI-Q9 | Operator home vs no inbox | **resolved** (Q9: no new list API; promote `/reviewer`; kill greeting; restyle CTA widget; no fake queue) |
| — | VI-Q10 | `--app-info` = primary | **resolved** (Q10: distinct info token) |

No new critical/high.

## Per-section findings

## VI-Q0 — concept + Greptile

| Severity | Area | Finding |
|:---|:---|:---|
| — | findings | **Resolved.** Job rival, not console. Captures cited by path. Steal approach, copy none of the UI. VI-Q0 phosphor console kept. |

**Verified OK:** cover-the-name vs Greptile is mint-pill light hero / mint billing banner — not “drop phosphor.”

## VI-Q5 — mark

| Severity | Area | Finding |
|:---|:---|:---|
| — | findings | **Resolved.** Wordmark + custom wedge SVG. Unicode `>` is prompt-line layout only. |
| low | general plan | SVG source is right; browsers still want a raster favicon (`favicon.ico` / PNG). Name that in Track I deliverables — not a findings re-lock. |

**Verified OK:** letter-R tile and ASCII art stay rejected.

## VI-Q9 — operator home

| Severity | Area | Finding |
|:---|:---|:---|
| — | findings | **Resolved.** No inbox API. No fake queue. Kill greeting. Restyle existing widget slot. Nav visible when empty. |
| medium | findings | “Promote `/reviewer`” is locked, but default authenticated landing is still `/dashboard` in several files (`LandingPage.tsx:21`, `LoginPage.tsx:13`, `AuthCallbackPage.tsx:18`). General plan must list those entrypoints (and tests). Do **not** retarget admin `/admin/dashboard`, impersonation-stop, or danger-zone fallbacks unless the plan says so. |

**Verified OK:** no client fan-out; github-onboarding behavior out; diff viewer out.

## VI-Q10 — info ≠ primary

| Severity | Area | Finding |
|:---|:---|:---|
| — | findings | **Resolved.** Overlay scale must uncouple info from primary/CTA so findings `info` is not the brand button. |
| low | findings | `--app-link` may still follow primary (console-correct). Do not uncouple links unless the general plan wants a dim olive link. |

**Verified OK:** four-level severity stays named; green = merge success, not info (`types.ts:22`).

## Tracks I / P / C / R / V — no new gaps

Pass-01 mediums that findings absorbed (contrast WCAG **and** APCA, latin-ext, both Sonner sites, QuietSelect, radius/hairline, VI-Q2 boot mechanism): **verified in file**, unchanged vs tree.

| Severity | Area | Finding |
|:---|:---|:---|
| medium | clarity | Named `--rv-*` hex trio (canvas / body / accent) is still parking-lot. That is correct for **general plan**, not a missing Q#. Avoid Greptile `#28E99F` and GitHub `#39d353` *recipes* without dropping phosphor. |
| low | plan-folder README | Duplicate “Does not replace” paragraph (`README.md:11–13`). Index row is the only README edit this skill may touch — flag for the next human/docs pass. |

## Cross-cutting

| Check | Result |
|:---|:---|
| Header links findings + authority | OK |
| Locked table complete, no TBD | OK — VI-Q0–Q10 locked |
| General plan phases | **None yet** — next skill is `create-general-plan` |
| New persistence | None — inbox API stays out |
| i18n / theme | EN+LV + latin-ext + force-dark mechanism named |
| Human gates | Contrast measure + cover-the-name in experiment table |
| Next step | Findings still say BLOCK until pass 2 — this pass **clears** that |

## Open questions (max 3)

None blocking. Hex trio and `/dashboard` entrypoint list belong in the general plan.

## Pre-execution checklist

- [x] VI-Q5 wedge (not `>`) locked
- [x] VI-Q9 no inbox / no fake queue locked
- [x] VI-Q10 info ≠ primary locked
- [x] Greptile look collision withdrawn; VI-Q0 kept
- [ ] `create-general-plan` (I → P+V → C → R): named `--rv-*` pairs; boot force-dark; latin-ext; both Sonner sites; `/reviewer` as operator home **including** landing/login/callback defaults; Track I raster favicon

**Next:** `create-general-plan` on this folder. Do not run another architecture pass unless the general plan contradicts these locks.
