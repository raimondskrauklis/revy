# docs/visual-identity/reviews/architecture-peer-review/pass-03-2026-09-19.md

# Architecture peer review — pass 3

**Plan folder:** `docs/visual-identity/`  
**Date:** 2026-09-19  
**Scope:** full (findings + general plan P0–P4)  
**Reviewer:** architecture-peer-review skill  
**Baseline:** [VISUAL_IDENTITY_FINDINGS.md](../../VISUAL_IDENTITY_FINDINGS.md), [VISUAL_IDENTITY_GENERAL_PLAN.md](../../VISUAL_IDENTITY_GENERAL_PLAN.md), codebase paths cited below  

## Verdict

Review complete: 0 critical, 0 high, 5 medium/low.  
**BLOCK create-execution-plan:** no — phases match VI-Q0–Q10; pass-02 entrypoints and raster favicon are in P0/P3; leftover items are execution enumeration (404 home link, repo→PR hop shape, font/radius wiring).

## Prior pass

[pass-02-2026-09-19.md](./pass-02-2026-09-19.md) — **BLOCK create-general-plan: no.** General plan now exists. Pass-02 asked the plan to name `/dashboard` entrypoints, raster favicon, and `--rv-*` as a P0 measure — all present.

## Verified (docs match codebase)

| Claim | Code evidence |
|:---|:---|
| Overlay info = primary today | `tokens.revy.css:11` / `:19` — P0 uncouples (VI-Q10) |
| Three named auth entrypoints still `/dashboard` | `LandingPage.tsx:21`; `LoginPage.tsx:13`; `AuthCallbackPage.tsx:18` — P3 in-scope |
| P3 correctly leaves admin / impersonation / danger-zone | `AdminLayout.tsx`; `ImpersonationBanner.tsx:29`; `DangerZonePage.tsx:80` — plan **out** |
| Reviewer nav gated | `ReviewerNavItem.tsx:15–17` — P3 un-gate |
| Reviewer routes are home → repo PRs → PR detail | `routerInstance.tsx:102–118` |
| Raw `<select>` on reviewer home | `ReviewerHomePage.tsx:75–86` — P4 |
| 404 / unauthorized still send people to `/dashboard` | `NotFoundPage.tsx:18`; `UnauthorizedPage.tsx:20` — **not** in P3 list |
| No `--radius` / `--font-*` tokens yet | grep empty — P0 new |
| Vitest exists beside landing + login + callback | `LandingPage.test.tsx`; `LoginPage.test.tsx`; `AuthCallbackPage.test.tsx` |
| Findings Q0–Q10 ↔ plan thesis | locked; hex calibration only after P0 measure |

## Rollup (critical / high)

None. Pass-02 highs stay **resolved** in the general plan (wedge + raster in P0; no inbox / named entrypoints in P3; info ≠ primary in P0).

## Per-section findings

## P0 — Foundations

| Severity | Area | Finding |
|:---|:---|:---|
| medium | general plan | `--font-sans` / `--font-mono` and radius 0–4px are named, but today there is no `@theme` font map and no radius token (`index.css` is imports only; components use Tailwind `rounded-lg` / `rounded-xl`). Execution must: (1) map Plex into Tailwind so `font-mono` is Plex, (2) **use** `--app-radius` on chrome in **P2**, not expect P0 tokens to restyle pages by themselves. |
| low | general plan | Contrast “trio” vs full scale: measure canvas/body/accent **and** CTA fg/bg + info-on-canvas so VI-Q10 does not ship an unreadably dim info lamp. |

**Verified OK:** info ≠ primary; force-dark mechanism; store/`theme.ts` kept; both Sonner sites; wedge SVG + raster favicon; latin-ext; no landing/reviewer IA in P0.

## P1 — Public + voice

| Severity | Area | Finding |
|:---|:---|:---|
| low | general plan | Login “split only if it earns a move” is still an option. Execution should pick keep-split-without-blob **or** single column in P1.1 — do not re-open VI-Q1. |
| — | pass-02 | Cover-the-name / 375 / reduced-motion / no mint-pill hero — **in scope**. |

**Verified OK:** not 3-card / trust-bar; `>` layout only; EN+LV voice; depends on P0 so tokens exist before composition (no green-landing-on-indigo-app).

## P2 — Product chrome

| Severity | Area | Finding |
|:---|:---|:---|
| — | findings | Hairline vs `ring-1` cards + Quiet* via tokens matches the reuse trap. Chrome restyle is this phase; greeting/entrypoints deferred to P3 — correct split. |

**Verified OK:** impersonation banner stays; github-onboarding HMAC out; admin readable; depends P0 only (LOOP still runs P1 before P2 by number).

## P3 — Operator home

| Severity | Area | Finding |
|:---|:---|:---|
| medium | general plan | Pass-02 medium **mostly resolved** (three entrypoints named). `NotFoundPage.tsx:18` and `UnauthorizedPage.tsx:20` still `to` / `navigate('/dashboard')`. P1 restyles 404; P3 does not retarget those homes. After operator home, “go home” to the greeting dashboard is leftover IA. Execution: either fold those two links into P3 **or** explicitly keep `/dashboard` as the 404/unauthorized fallback. |
| low | general plan | `CompleteProfilePage` also lands `/dashboard`. Closer to callback than to danger-zone; call it in or out in the execution file. |
| — | pass-02 | Kill greeting, restyle widget slot, nav always visible, no inbox / no fan-out — **locked in scope**. |

**Verified OK:** tests for the three entrypoints are named; admin/impersonation/danger-zone stay out.

## P4 — Reviewer UX

| Severity | Area | Finding |
|:---|:---|:---|
| medium | general plan | “Shorten **repo → PR**” has no chosen IA. Routes today: `/reviewer` (repo table) → `…/repositories/:repoId/pull-requests` (`routerInstance.tsx:102–118`). Allowed without a new API: skip the table when there is one repo; denser table; default first repo. Execution must pick one — do not invent a workspace PR list. |
| — | findings | Four severities; green = merge; QuietSelect; no diff viewer; no scanlines — **match**. |

**Verified OK:** depends P0+P3; FindingRow title+message dump is the scan target.

## Cross-cutting

| Check | Result |
|:---|:---|
| Header links findings + pass 2 | OK; scaffold Q4 / R7 live in findings, not repeated in plan header (low) |
| Locked VI-Q0–Q10 match plan | OK — no contradiction |
| Each phase: Goal, Scope in/out, Deliverables, Depends on | OK |
| New persistence | None — correct |
| Test paths | Cross-cutting + P3 entrypoints; P0 theme boot; Vitest-beside-screens |
| i18n / theme | EN+LV; force-dark; latin-ext |
| No TBD in decisions | Hex is **calibration after P0 measure**, not an open identity fork |
| Next step | `create-execution-plan` — matches findings |

## Open questions (max 3)

None blocking.

## Pre-execution checklist

- [x] Findings VI-Q0–Q10 locked; Greptile not a console look
- [x] General plan P0–P4; no inbox API; three auth entrypoints named
- [x] Raster favicon in P0
- [ ] `create-execution-plan`: P0 font `@theme` + radius token **consumed in P2**; P3 404/unauthorized home; P4 one-repo hop rule; CTA/info contrast in P0 measure
- [ ] `execution-peer-review` before `phase-execution`

**Next:** `create-execution-plan` on this folder.
