# docs/visual-identity/reviews/execution-peer-review/pass-01-2026-09-19.md

# Execution peer review — pass 1

**Plan folder:** `docs/visual-identity/`  
**Date:** 2026-09-19  
**Scope:** full (P0–P4 execution)  
**Reviewer:** execution-peer-review skill  
**Baseline:** [VISUAL_IDENTITY_FINDINGS.md](../../VISUAL_IDENTITY_FINDINGS.md), [VISUAL_IDENTITY_GENERAL_PLAN.md](../../VISUAL_IDENTITY_GENERAL_PLAN.md), [README.md](../../README.md), architecture [pass 3](../architecture-peer-review/pass-03-2026-09-19.md)  

## Verdict

Review complete: 0 critical, 2 high, 9 medium/low.  
**BLOCK phase-execution:** yes — P0 overlay list omits surface/text-strong/chip (slate leak); P4 one-repo hop bounces via ReviewerLayout back link.

## Prior pass

None — first execution review.

## Rollup (critical / high)

| Severity | Phase | Title |
|:---|:---|:---|
| high | P0 | Overlay list omits `--app-surface` / `--app-text-strong` / `--app-chip` (and related) |
| high | P4 | One-repo `Navigate` + `ReviewerLayout` back-to-`/reviewer` bounce |

## Per-phase findings

## P0 — `docs/visual-identity/VISUAL_IDENTITY_P0_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| high | execution contract | P0.1 `--rv-*` list is canvas / body / muted / ring / primary / info / warning / danger / success / cta. `tokens.css` `html.dark` also sets `--app-surface`, `--app-text-strong`, `--app-chip`, `--app-ring-strong`, `--app-input-border`, table rows. If those stay slate (`#0F172A` / `#F8FAFC`), P0’s own goal (“every screen that already uses `--app-*` can paint the console”) is green buttons on GitHub-dark — the recipe findings defer. Name those tokens in P0.1. |
| medium | execution contract | P0.2 / P0.5 **Deliverable** commands do not assert the claim: P0.2 re-runs `consoleContrast.test.ts` (no font check); P0.5 re-runs theme + RevyLogo (no `richColors` grep). Gate should include a font/`@theme` assertion and `rg -n richColors frontend/src/main.tsx frontend/src/lib/toast.ts`. |
| medium | execution contract | P0.0 `python -m json.tool ../.revy/review-context.json` is cwd-relative and wrong from repo root. Use `.revy/review-context.json` from the repo root (the pytest already is `cd backend`). |
| low | execution contract | Six subphases (at cap). P0.2 verify is weak as above. |

**Verified OK:**

- Header + locked decisions match VI-Q2/Q4/Q5/Q10; radius defined here, consumed P1/P2.
- Paths exist (`theme.ts`, `AppearanceSettingsPage.test.tsx`, `tokens.revy.css`, both Sonner sites). New files named.
- Out-of-scope lists P1–P4; frontend + backend gates; Next → P1.
- `active_program` today is `github-onboarding` (`.revy/review-context.json`) — P0.0 switch is the right first subphase.

## P1 — `docs/visual-identity/VISUAL_IDENTITY_P1_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| low | clarity | Header says “Track II”; findings catalog is **Track P**. |
| low | execution contract | Cover-the-name screenshot is explicitly **non-gate**. Acceptable (no browser in LOOP); keep the 375 / ~800 height note in P1.1 test or a comment so it is not dropped. |

**Verified OK:**

- Keep-split-without-blob locked; dest stays `/dashboard` until P3; 4 subphases; files exist (`StatusGatePage.tsx`, `UnauthorizedPage.test.tsx`; `NotFoundPage.test.tsx` named new).
- Landing test currently asserts “Ship with confidence” (`LandingPage.test.tsx:22`) — P1.1/P1.4 correctly retarget it.
- Out-of-scope and Next → P2.

## P2 — `docs/visual-identity/VISUAL_IDENTITY_P2_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | P2.1 **Deliverable** command is `npm test -- RevyLogo` — that does not prove sidebar source renders `RevyLogo` or uses `--app-radius-md`. Add an `AppShellLayout` test (new) or a source assertion in the gate. |
| medium | execution contract | `quiet-select` menu uses `popover.tsx` (`rounded-xl`). P2.3 Files omit it; radius consumption on Quiet* can still look like boxed SaaS in the dropdown. Include `popover.tsx` or explicitly leave it. |
| low | clarity | Header “Track III”; findings catalog is **Track C**. |

**Verified OK:**

- Radius consumption on shell + Quiet* matches general plan / pass-3 leftover. HMAC/start-connect out. Impersonation/admin `/dashboard` fallbacks stay.
- Cited files exist (`AppHeader.tsx`, `quiet-date.tsx`, `ConnectGitHubPanel.test.tsx`). 3 subphases. Next → P3.

## P3 — `docs/visual-identity/VISUAL_IDENTITY_P3_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| low | execution contract | P3.1 lists `LandingPage.test.tsx` but the deliverable sentence does not require an authenticated `replace('/reviewer')` assertion (today the landing test is unauthenticated only). Add that case so dest retarget cannot ship as copy-only. |

**Verified OK:**

- Six dest files match the tree (`CompleteProfilePage.tsx:24,45`; `UnauthorizedPage.tsx:20,65`; `AuthCallbackPage.tsx:18`). Admin/impersonation/danger-zone explicitly out. WelcomeWidget only used from DashboardPage.
- Nav un-gate + no fake counts match VI-Q9 / general plan. 3 subphases. Gate lists all six dest tests. Next → P4.

## P4 — `docs/visual-identity/VISUAL_IDENTITY_P4_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| high | codebase | One-repo `Navigate` to `/reviewer/repositories/:id/pull-requests` plus `ReviewerLayout.tsx:19–25` `Link to="/reviewer"` (`backToRepositories`) **bounces**: PR list → home → Navigate back to PR list. P4.2 Files omit `ReviewerLayout`. Hide or retarget that back link when the hop applies (do not invent a workspace PR list). |
| medium | execution contract | P4.3 touches `PullRequestListPage` (inline error is missing today — toast only, `:19–23`) but the deliverable command is `ReviewerHomePage FindingRow`. Add a list-page test (new) or include it in the gate. |
| medium | execution contract | P4.4 grep for stale `/dashboard` as default member home can “fix” impersonation / danger-zone / admin fallbacks that P3 locked **out**. Scope the grep to the six dest files + landing copy, not a repo-wide replace. |
| low | clarity | Header “Track III reviewer”; findings catalog is **Track R**. |

**Verified OK:**

- One-repo hop rule matches general plan; four severities; info ≠ merge green; no new API; FindingRow tests + changelog.json exist. Doc-sync is the final subphase. Next: none.

## Execution contract checklist

- [x] Each file links general plan + findings
- [x] Goal + decisions locked per phase
- [x] Out of scope lists later phases
- [x] 3–6 subphases each (P0 at 6; others 3–4)
- [x] What / Files / Deliverable present
- [ ] Deliverable commands always assert the claim (fail P0.2/P0.5/P2.1/P4.3)
- [x] Phase gates (frontend npm test; P0 also pytest manifest)
- [x] No Alembic (frontend-only)
- [x] No TBD / A-or-B in locked decisions
- [x] FE gates on every phase
- [x] P4 doc-sync + changelog
- [ ] Overlay token completeness (P0.1) and ReviewerLayout bounce (P4.2) before LOOP

**Next:** fix agent reads this pass file; then execution-peer-review pass 2. Do not start `phase-execution` until **BLOCK: no**.

