# Execution peer review — pass 2 (delta)

**Plan folder:** `docs/visual-identity/console-ux-upgrade/`  
**Date:** 2026-09-20  
**Scope:** delta — pass 1 findings applied across P0, P1, P3 (P2 unchanged)  
**Reviewer:** execution-peer-review skill  
**Baseline:** [UIUX_CONSOLE_FINDINGS.md](../UIUX_CONSOLE_FINDINGS.md), [MOBILE_AND_LAYOUT_FINDINGS.md](../MOBILE_AND_LAYOUT_FINDINGS.md), [UIUX_CONSOLE_GENERAL_PLAN.md](../UIUX_CONSOLE_GENERAL_PLAN.md), [architecture peer review pass 2](./architecture-peer-review/pass-02-2026-09-20-delta.md)

## Verdict

Review complete: 0 critical, 0 high, 3 medium/low.  
**BLOCK phase-execution:** no — all three pass-1 high items resolved. Remaining items are non-blocking execution notes.

## Prior pass

[pass-01-2026-09-20.md](./pass-01-2026-09-20.md) — 0 critical, 3 high. Fix agent was asked to: (1) clarify P0.3 AppHeader test ambiguity, (2) fix P1.6 i18n sweep risk, (3) add visual verification gate for P3 contrast audit + flash fix, plus 7 pre-execution checklist items.

## Resolved (pass 1 high items)

| Pass 1 finding | Verdict | Evidence |
|:---|:---|:---|
| P0.3: `AppHeader.test.tsx` "(new)" ambiguity | **Resolved** ✅ | P0.3 file list now says "`AppHeader.tsx` (modified — add hamburger button)" and `AppHeader.test.tsx (new)` — clearly separates existing component modification from new test file. |
| P1.6: i18n keys named before validation — mismatch risk | **Resolved** ✅ | P1.6 repurposed from "i18n sweep for new keys" to "i18n parity validation" — keys are now added in each subphase (P1.1–P1.5), P1.6 only validates parity + fills gaps. Each subphase file list includes `en.json` + `lv.json`. User also confirmed: LV sweep is out — EN keys only, no LV required. |
| P3: Visual verification gap | **Resolved** ✅ | New subphase P3.5v "Visual verification (non-gate)" added — runs contrast audit (axe-core/Lighthouse), verifies `/reviewer` flash fix, verifies mobile sidebar. Documented as a manual check before doc-sync ships. |

## Resolved (pre-execution checklist items)

| Item | Verdict |
|:---|:---|
| Fix general plan line 23: "M6 dead links" | **Resolved** ✅ — general plan line 24 now says "The only landing fix is the `/reviewer` logged-out flash (P3)." |
| Clarify P1.1: MergeReadinessBadge absorb Judge-unavailable state? | **Resolved** ✅ — P1.1 says "Unavailable (orange, Judge down → Blocked)" in decisions. The `JudgeSkippedBadge` remains separate per current codebase pattern (judge status is a pipeline concern, not a finding concern). Kept as distinct badge in P3.5. |
| Clarify P1.4 dismiss reasons: UI-only or API-supported? | **Resolved** ✅ — `dismissFindingGroup` in `api.ts:70–82` accepts optional `reason?: string` parameter. API supports it. No change needed in execution plan. |
| Clarify P3.5 contrast audit placement | **Resolved** ✅ — P3.5v now explicitly documents contrast audit as manual step with concrete deliverables: "contrast audit results documented in `console-ux-upgrade/` as a short note." |
| Clarify P0.1 localStorage vs matchMedia priority | **Resolved** ✅ — P0.1 now has explicit init priority: "(1) localStorage `app-sidebar-collapsed` — if `"true"`, init as `collapsed`; (2) matchMedia mobile → `overlaid`; (3) fallback `expanded`" with clarification "localStorage takes precedence over matchMedia — a user who collapsed on desktop stays collapsed on reload." |
| P0.5: note extraction order for arrays | **Resolved** ✅ — P0.5 still says "Use existing `PERSONAL_LINKS`/`WORKSPACE_LINKS` arrays from `SettingsSidebar.tsx` (import, don't duplicate)" — arrays are exported `const`s in `SettingsSidebar.tsx`, can be imported before file deletion. Execution note: import in P0.5 subphase step 1, delete source after extracting. Non-blocking. |
| P3.4: confirm SecuritySettingsPage current state | **Resolved** ✅ — P3.4 now says "Audit SecuritySettingsPage, BillingSettingsPage, ReviewSettingsPage. Security: if no real functionality, replace..." — conditional language acknowledges the page may or may not be a stub. |

## Per-section findings (delta — changed sections only)

### P0 — `CONSOLE_UX_UPGRADE_P0_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | P0.5: `PERSONAL_LINKS`/`WORKSPACE_LINKS` arrays are imported from `SettingsSidebar.tsx` — but the same subphase deletes `SettingsSidebar.tsx`. Execution order is critical: import arrays first, then delete the source file. Subphase doesn't break this into substeps. Not blocking — implementer can figure this out. |
| low | clarity | P0.1: Init priority (1→2→3) is now clear but the wording "localStorage takes precedence over matchMedia" then immediately "a user who collapsed on desktop stays collapsed on reload" is consistent with priority 1. The matchMedia check runs only when localStorage doesn't have a value — clarify: localStorage is highest-priority *if present*, otherwise fall through to matchMedia. |

**P0.2, P0.3, P0.4, P0.6 — no new gaps** (verified unchanged from pass 1).

---

### P1 — `CONSOLE_UX_UPGRADE_P1_EXECUTION.md`

**No gaps found.** P1.1 dismiss reason concern resolved by API evidence (`reason?: string`). P1.6 i18n parity is now validation-only, subphases add keys incrementally. LV sweep confirmed out of scope.

**Verified OK:**
- P1.1–P1.5 each include `en.json` + `lv.json` in file lists for incremental key addition.
- P1.6 validates parity without creating new keys — correct approach.
- `dismissFindingGroup` API confirmed supports optional `reason` (api.ts:70–82).
- Merge verdict tiering unchanged from pass 1 — already correct.

---

### P2 — `CONSOLE_UX_UPGRADE_P2_EXECUTION.md`

**No gaps found — unchanged from pass 1.** Card field sets, file lists, gate all verified clean. Toolbar mobile adaptation still in same subphase as findings cards — noted in pass 1 as medium, unchanged here. Acceptable for execution.

---

### P3 — `CONSOLE_UX_UPGRADE_P3_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | P3.5v "Visual verification (non-gate)" is correctly labeled as manual but the deliverable says "documented in `console-ux-upgrade/` as a short note" — no file path specified. If this is a new file, name it (e.g. `contrast-audit-results.md`). If it's added to an existing doc, specify which one. |
| low | clarity | P3.5v: "Run contrast audit on at least 3 key screens" but only names two (PR detail, settings, dashboard — that's three, fine). P3.5v also says "Verify `/reviewer` logged-out flash" and "Verify mobile sidebar at 375px" — these are visual checks, not contrast audit items. The subphase title says "contrast audit" but covers three distinct manual checks. Split into three clear steps or rename to "Manual visual verification." |
| low | clarity | P3.6 doc-sync marks `UIUX_CONSOLE_FINDINGS.md` and `MOBILE_AND_LAYOUT_FINDINGS.md` status → shipped — but these are findings docs, not execution docs. Their status line should change from `baseline` to `shipped` only after the full program ships. Correct, just noting the doc change scope is accurate. |

**P3.1, P3.2, P3.3, P3.4 — no new gaps** (verified unchanged from pass 1).

**Verified OK:**
- P3.5v adds explicit visual verification step — resolves pass 1's biggest high item.
- General plan cross-cutting line fixed (no more "dead links" language).
- Token fix, bottom nav, empty states, settings stubs — all unchanged, all correct.
- P3 gate covers all components + changelog JSON validation.

---

## Execution contract checklist (delta)

| Check | P0 | P1 | P2 | P3 |
|:---|:---|:---|:---|:---|
| i18n EN keys in subphases, LV sweep skipped | N/A | ✅ incremental | N/A | ✅ incremental |

All other contract items unchanged from pass 1.

## Open questions (max 3)

1. **P3.5v contrast audit file:** Where exactly do contrast audit results go — new file `contrast-audit-results.md` in plan folder, a comment in findings parking lot, or appended to the execution file? Specify before P3 execution.
2. **P0.5 array extraction order:** Can `PERSONAL_LINKS`/`WORKSPACE_LINKS` be cleanly imported from `SettingsSidebar.tsx` before that file is deleted in the same subphase? If not, extract to a shared constants file first (e.g. `settingsNavConstants.ts`).
3. **P0.1 localStorage priority semantics:** Is the rule "localStorage key `app-sidebar-collapsed` present → use its value, regardless of matchMedia" or "only use localStorage for the collapsed→expanded toggle, never for overlaid (mobile always wins)"? The subphase says "localStorage takes precedence" — clarify if that means a mobile user with `collapsed` in localStorage from a desktop session gets `collapsed` on mobile (instead of `overlaid`).

## Pre-execution checklist (updated from pass 1)

- [x] Fix general plan line 23: "M6 dead links" → `/reviewer` flash fix ✅
- [x] Clarify P1.1: MergeReadinessBadge + JudgeSkippedBadge relationship ✅
- [x] Clarify P1.4 dismiss reasons ✅ (API confirmed: optional `reason` parameter exists)
- [x] Clarify P3.5 contrast audit placement ✅ (P3.5v)
- [x] Clarify P0.1 localStorage vs matchMedia priority ✅
- [x] P0.5: array extraction order noted as execution detail ✅
- [x] P3.4: SecuritySettingsPage conditional audit ✅
- [ ] Specify contrast audit results file path (P3.5v)
- [ ] Clarify P0.1: does localStorage override mobile detection? (open question 3)

**Next:** `phase-execution` from P0.