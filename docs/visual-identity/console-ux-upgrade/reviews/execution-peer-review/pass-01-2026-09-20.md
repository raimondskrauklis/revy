# Execution peer review — pass 1

**Plan folder:** `docs/visual-identity/console-ux-upgrade/`  
**Date:** 2026-09-20  
**Scope:** full — P0, P1, P2, P3 execution  
**Reviewer:** execution-peer-review skill  
**Baseline:** [UIUX_CONSOLE_FINDINGS.md](../UIUX_CONSOLE_FINDINGS.md), [MOBILE_AND_LAYOUT_FINDINGS.md](../MOBILE_AND_LAYOUT_FINDINGS.md), [UIUX_CONSOLE_GENERAL_PLAN.md](../UIUX_CONSOLE_GENERAL_PLAN.md), [architecture peer review pass 2](./architecture-peer-review/pass-02-2026-09-20-delta.md), codebase paths cited below

## Verdict

Review complete: 0 critical, 3 high, 6 medium/low.  
**BLOCK phase-execution:** no — all three high items are fixable without restructuring. Fix before running phase-execution.

## Rollup (critical / high)

| Severity | Phase | Title |
|:---|:---|:---|
| high | P0.5 | `AppHeader.tsx` test file listed as "(new)" but AppHeader already exists in codebase — the test file is new; clarify deliverable scope |
| high | P1.6 | i18n sweep lists concrete keys but EN/LV parity is deferred to a single subphase — keys are named before they're validated; mismatch risk |
| high | P3 | Doc-sync (P3.6) is the final subphase — but contrast audit + flash fix (P3.5) and settings stub content (P3.4) rely on visual verification that isn't tested. P3 gate only runs unit tests, no visual checks. |

## Per-phase findings

### P0 — `CONSOLE_UX_UPGRADE_P0_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| high | codebase | P0.3: `AppHeader.test.tsx` listed as "(new)" alongside an existing `AppHeader.tsx`. `AppHeader` currently has no test file — the line is technically correct (test file is new) but the ambiguity could cause confusion: is a new `AppHeader.tsx` being written or only its test? Clarify the file list is "new test" only — the component itself is modified, not replaced. |
| medium | execution contract | P0.5: Route config change (`routerInstance.tsx` removing `SettingsLayout` wrapper) is a single-file change that touches every settings route — no subphase validates that all settings pages render correctly after flattening. Phase gate only runs `AppSidebar` tests. Consider adding a smoke test: `/settings/profile`, `/settings/team`, etc. render without error. |
| medium | general plan | P0.1: Three-state SidebarProvider is initialized from `localStorage` and `window.matchMedia` — but the init logic isn't specified: what happens when localStorage says `collapsed` but `matchMedia` says mobile? (mobile should win; state machine order matters). Execution can resolve this, but subphase should note the priority. |
| low | clarity | P0.5: "Use existing `PERSONAL_LINKS` / `WORKSPACE_LINKS` arrays from `SettingsSidebar.tsx` (import, don't duplicate)." Since `SettingsSidebar.tsx` is deleted in the same subphase, the arrays must be extracted to a shared location before deleting the source. Subphase doesn't specify extraction order. |

**Verified OK:**
- `SidebarProvider` as new file in `components/layout/` — confirmed no existing provider in codebase (grep: zero matches for `SidebarProvider`).
- `AppSidebar.tsx` extraction from `AppShellLayout.tsx:44–96` — confirmed existing sidebar content is in that range.
- `max-w-lg` at `DashboardPage.tsx:17` confirmed — target line matches.
- `SettingsLayout.tsx`, `SettingsSidebar.tsx`, and their test files confirmed exist at paths listed for deletion.
- All new file paths correctly placed under `frontend/src/`.
- 6 subphases — within the 3–6 range.

---

### P1 — `CONSOLE_UX_UPGRADE_P1_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | P1.1: `MergeReadinessBadge` "Unavailable" state (Judge down, orange badge) is listed in decisions but not included in P1.1 deliverables — only `'failure'` path is wired. The current codebase already renders `JudgeSkippedBadge` separately (line 126–128 of `PullRequestDetailPage.tsx`). Plan should clarify: does P1.1 integrate the Judge-unavailable state into `MergeReadinessBadge` (replacing the separate `JudgeSkippedBadge`), or keep it as a separate badge? General plan says Unavailable is a MergeReadinessBadge state. |
| medium | codebase | P1.4: Detail sheet "dismiss button with reason dropdown" lists 4 reasons but these aren't in the existing `dismissFindingGroup` API — `dismissFindingGroup` in `api.ts:70` takes only `groupId`, no reason. Either the dismiss reason is UI-only (not persisted) or the API needs extending. Execution plan doesn't note this gap. P1's Out says "backend API changes" are out — so dismiss reasons must be UI-only or the `dismissFindingGroup` endpoint already supports an optional reason field not shown in the API signature. Clarify. |
| medium | execution contract | P1.5: Loading skeletons replace generic loading text in `PullRequestDetailPage`, `PullRequestListPage`, and `ReviewerHomePage` — but only `PullRequestDetailPage` has the "pipeline badge placeholder" requirement. The other two pages also have loading states that flash generic text — they should get skeleton rows too, which is listed, but the "no 'none' claim" badge rule isn't applied to them. Minor; PR list/repo list don't have pipeline badges. |
| low | clarity | P1.5: `TableSkeleton` placed under `components/ui/` — but the plan says "4-row ghost table (pulsing `--app-chip` bars)". `TableSkeleton` should accept a `rows` count prop for reusability across pages. Not specified but implied by being a shared component. |
| low | clarity | P1.3: "Sortable column headers: click Severity/File/Date header → toggle asc/desc" — `ReconciledFinding` has `created_at` and `updated_at` but the plan just says "Date." Lock to `updated_at` (most recent change) or `created_at` (first seen). Minor — execution can decide. |

**Verified OK:**
- `deriveMergeConclusion` function confirmed at `mergeConclusion.ts:4–10` — matches plan.
- `useRevisionFindings` hook confirmed at `hooks.ts:143–159` — existing, zero callers, correct for P1.4 detail sheet.
- `dismissFindingGroup` API confirmed at `api.ts:70` — takes `groupId`, matches plan.
- `MergeReadinessBadge.test.tsx`, `mergeConclusion.test.ts`, `FindingRow.test.tsx` all confirmed existing — updates are valid.
- i18n keys listed with specific names — good practice.
- Detail data source locked: `useRevisionFindings` (resolved from architecture pass 2).
- 6 subphases — within 3–6 range.

---

### P2 — `CONSOLE_UX_UPGRADE_P2_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | execution contract | P2.1: Findings toolbar mobile adaptation is in the same subphase as the findings table card layout — but the toolbar was built in P1.3 and P2.1 now adds "collapsible Filters section with chevron toggle." This is a meaningful UI change to a component owned by P1. Consider splitting toolbar mobile adaptation to its own subphase or explicitly noting it's an extension of an existing component. |
| low | clarity | P2.2: Card field "Head SHA (first 7 chars, mono)" — `PullRequestListPage` currently shows `pullRequest.head_sha.slice(0, 7)` on line 79. Card layout should replicate this exactly. Noted for execution. |
| low | clarity | P2.1–P2.5: Each subphase describes card layout but doesn't share a `MobileCard` base component. Five different card implementations could lead to visual drift. Consider a `MobileTableCard` wrapper in `components/ui/` for consistency — not required, just a DRY note. |

**Verified OK:**
- All 5 table component files confirmed exist: `PullRequestDetailPage.tsx`, `PullRequestListPage.tsx`, `ReviewerHomePage.tsx`, `InstallationsTable.tsx`, `MembersTable.tsx`.
- All 5 corresponding test files confirmed exist.
- Card field sets match general plan locked fields exactly (P2.1–P2.5).
- `overflow-x-auto` verified on all table files.
- 5 subphases — within 3–6 range.

---

### P3 — `CONSOLE_UX_UPGRADE_P3_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| high | execution contract | P3.6 doc-sync is the final phase gate but P3.5 contrast audit + flash fix depend on visual/manual verification that unit tests can't cover. P3 gate only runs `npm test` — there's no visual verification step. The contrast audit in particular (P3.5 scope says "run axe-core or Lighthouse") produces output that should be documented or at minimum confirmed passing before doc-sync marks everything as shipped. Add a manual verification step or note the contrast audit results in the findings parking before closing. |
| high | general plan | P3 cross-cutting in general plan (line 23): "M6 dead links are removed (router only)" contradicts corrected findings — M6 now correctly states no dead links exist, only the `/reviewer` flash fix. P3.5 correctly implements the flash fix, but the general plan header is stale. Fix the general plan line before execution. |
| medium | execution contract | P3.4 settings audit: "Security: if no real functionality, replace page content with Coming soon" — execution plan should read `SecuritySettingsPage.tsx` first to determine current state before replacing content. Currently the subphase file list only has i18n + settings pages — no explicit audit step before replacement. |
| low | execution contract | P3.5: `/reviewer` flash fix — "in router or `ProtectedRoute`, redirect to `/` before render if not authenticated." The current `LandingPage` already checks `isAuthenticated` and renders `<Navigate to="/reviewer" replace />` — the flash may be from the router mounting `ProtectedRoute` children before auth check completes. The fix location (router vs `ProtectedRoute`) should be investigated before implementation; subphase could note "audit auth gate timing first." |
| low | clarity | P3.5: "Contrast audit" listed in the subphase title but no deliverables section for it — the subphase content only covers tooltips + flash fix. The contrast audit is mentioned in P3 scope but appears orphaned in P3.5. Either add it to P3.5 deliverables or note it as a manual post-phase check. |

**Verified OK:**
- `DevNoticeWidget.tsx` confirmed exists at `frontend/src/features/dashboard/widgets/` — line 19 matches the token fix location.
- `changelog.json` confirmed exists at `frontend/src/data/changelog.json`.
- All settings pages confirmed exist: `SecuritySettingsPage.tsx`, `BillingSettingsPage.tsx`, `ReviewSettingsPage.tsx`, `AppearanceSettingsPage.tsx` — with matching test files.
- `JudgeSkippedBadge.tsx` confirmed exists.
- `MobileBottomNav` correctly marked as new — no existing component (grep confirmed zero matches).
- 6 subphases — within 3–6 range.
- Doc-sync correctly lists all 5 docs + changelog — final phase closeout.

---

## Execution contract checklist

| Check | P0 | P1 | P2 | P3 |
|:---|:---|:---|:---|:---|
| Header links general plan + findings | ✅ | ✅ | ✅ | ✅ |
| Goal + decisions locked | ✅ | ✅ | ✅ | ✅ |
| Out of scope lists later phases | ✅ | ✅ | ✅ | ✅ |
| 3–6 subphases | ✅ (6) | ✅ (6) | ✅ (5) | ✅ (6) |
| Each subphase: What / Files / Deliverable | ✅ | ✅ | ✅ | ✅ |
| Deliverable includes verify command | ✅ | ✅ | ✅ | ✅ |
| Phase gate — copy-paste test block | ✅ | ✅ | ✅ | ✅ |
| Migration subphase flagged | N/A | N/A | N/A | N/A |
| Human gate if spike/ops/sign-off | N/A | N/A | N/A | ⚠️ contrast audit no gate |
| Next → following execution file | ✅ | ✅ | ✅ | ✅ final |
| No TBD / open options | ✅ | ✅ | ✅ | ✅ |
| FE phase has frontend gate | ✅ `npm test` | ✅ `npm test` | ✅ `npm test` | ✅ `npm test` + JSON |
| i18n EN + LV for new keys | ✅ | ✅ P1.6 | N/A | ✅ |

## Open questions (max 3)

1. **P1.4 dismiss reasons vs API:** `dismissFindingGroup` takes only `groupId` — does the existing API accept an optional reason/comment field not shown in the TS signature, or are the 4 dismiss reasons UI-only metadata? Execution needs to know before P1.4.
2. **P3.5 contrast audit:** Where should contrast audit results live — in the findings parking lot (`VISUAL_IDENTITY_FINDINGS.md`), in a P3.5 deliverable file, or as a manual post-gate check before doc-sync? Currently orphaned between tooltips + flash fix scope.
3. **P0.1 SidebarProvider init priority:** When localStorage says `collapsed` and `matchMedia` says mobile — which wins? Mobile (overlaid) should take priority over persisted desktop state. Lock this before P0.1 implementation.

## Pre-execution checklist

- [ ] Fix general plan line 23: "M6 dead links are removed" → match corrected findings (M6 is `/reviewer` flash fix only)
- [ ] Clarify P1.1: does `MergeReadinessBadge` absorb the Judge-unavailable state (replacing separate `JudgeSkippedBadge`), or keep both badges?
- [ ] Clarify P1.4 dismiss reasons: UI-only or API-supported? If UI-only, note that reason isn't persisted
- [ ] Clarify P3.5 contrast audit placement: add to deliverables or call it a manual post-phase check
- [ ] Clarify P0.1 localStorage vs matchMedia priority when both active
- [ ] P0.5: note extraction order for `PERSONAL_LINKS`/`WORKSPACE_LINKS` arrays before deleting `SettingsSidebar.tsx`
- [ ] P3.4: confirm `SecuritySettingsPage` current state before replacing content

**Next:** fix agent reads this pass file → resolves 3 high + pre-execution checklist items → re-run execution-peer-review (pass 2, delta) → phase-execution from P0