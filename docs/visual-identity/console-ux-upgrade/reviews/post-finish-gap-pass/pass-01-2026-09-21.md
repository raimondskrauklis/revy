# docs/visual-identity/console-ux-upgrade/reviews/post-finish-gap-pass/pass-01-2026-09-21.md

# Post-finish gap pass — Console UX + mobile/layout

**Date:** 2026-09-21 · **Phase:** all (P0–P3 shipped) · **BLOCK: no**

All P0–P3 deliverables are shipped. Remaining items are small polish.

## Findings

### F1 — Landing hero title should wrap into 2 lines ✅

**Fixed:** Split `en.json`/`lv.json` title with `\n`, added `whitespace-pre-line` + `leading-snug` to `<h1>`.

**Files:** `LandingPage.tsx`, `LandingPage.test.tsx`, `en.json`, `lv.json`

---

### F2 — MergeReadinessBadge missing tooltip ✅

**Fixed:** Added `title={t('reviewer.glossary.mergeReadiness')}`.

**Files:** `MergeReadinessBadge.tsx`, `en.json`, `lv.json`

---

### F3 — Pipeline status text missing glossary tooltips ✅

**Fixed:** Added `title` attributes on review run and publish status spans.

**Files:** `PullRequestDetailPage.tsx`, `en.json`, `lv.json`

---

### F4 — Review settings model selectors missing disabled-reason indicator ✅

**Fixed:** Added `dashboard.planSummary.modelsProRequired` text note below model section.

**Files:** `ReviewSettingsPage.tsx`, `en.json`, `lv.json`

---

### F5 — Doc-sync: README phase status ✅

**Fixed:** All phases set to "done", status line updated, post-finish gap pass linked.

**Files:** `README.md`

---

### F6 — Redundant ReviewerSummaryWidget on dashboard ✅

**Fixed:** Removed `reviewer-summary` extension registration from `registerRevy.ts`. The component stays available but no longer appears as a dashboard card. InstallationsSummaryWidget already covers the `/installations` path.

**Files:** `registerRevy.ts`

---

### F7 — PlanSummaryWidget missing free-plan description + feedback hint ✅

**Fixed:** Added `dashboard.planSummary.free` description line, usage counter below, and `feedbackHint` text when under limit. No duplicate feedback button — DevNoticeWidget is the single CTA.

**Files:** `PlanSummaryWidget.tsx`, `PlanSummaryWidget.test.tsx`, `en.json`, `lv.json`

---

### F8 — GitHub feedback link inaccessible (repo not public)

**Verified:** `https://github.com/raimondskrauklis/revy/issues/new` returns 404 for unauthenticated visitors. The link in `DevNoticeWidget.tsx` is correct — the repo needs to be made public (user plans to do this tomorrow).

**Files:** `DevNoticeWidget.tsx` (no change needed)