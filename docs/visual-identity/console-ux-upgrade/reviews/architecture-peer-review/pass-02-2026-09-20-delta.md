# Architecture peer review — pass 2 (delta)

**Plan folder:** `docs/visual-identity/console-ux-upgrade/`  
**Date:** 2026-09-20  
**Scope:** delta — P1 merge tiering, P2 card field sets, P3 ReviewerHomePage empty state, M6 landing dead links (sections changed since pass 1)  
**Reviewer:** architecture-peer-review skill  
**Baseline:** [UIUX_CONSOLE_FINDINGS.md](../UIUX_CONSOLE_FINDINGS.md), [MOBILE_AND_LAYOUT_FINDINGS.md](../MOBILE_AND_LAYOUT_FINDINGS.md), [UIUX_CONSOLE_GENERAL_PLAN.md](../UIUX_CONSOLE_GENERAL_PLAN.md)

## Verdict

Review complete: 0 critical, 0 high, 3 medium/low.  
**BLOCK create-execution-plan:** no — all four pass-1 high items resolved. Remaining medium items are non-blocking clarity notes for execution.

## Prior pass

[pass-01-2026-09-20.md](./pass-01-2026-09-20.md) — 0 critical, 4 high. Fix agent was asked to: (1) lock merge tiering in general plan, (2) define P2 card field sets for all 5 tables, (3) add ReviewerHomePage empty state to P3, (4) correct M6 dead-link finding.

## Verified (docs match codebase)

| Claim | Code evidence |
|:---|:---|
| M6 finding now correctly states no dead links exist in code | `UIUX_CONSOLE_FINDINGS.md:188–198` — "No dead links exist in the current landing code — `routerInstance.tsx` has no `/about`, `/features`, `/pricing`, or `/docs` routes. The earlier walkthrough 404s were from manually-typed paths, not linked from the UI." Matches `routerInstance.tsx` — only `'/' → LandingPage` and `'*' → NotFoundPage`. |
| M6 priority table updated — only `Fix /reviewer logged-out flash` remains | `UIUX_CONSOLE_FINDINGS.md:250` — P3 row is now "Fix `/reviewer` logged-out flash; glossary for Review/Judge/Publish" (was "Marketing/docs honesty"). `visual-identity/README.md:49` — "M6: `/reviewer` logged-out flash (no dead links — landing is clean, code verified)". |

## Resolved (pass 1 high items)

| Pass 1 finding | Verdict | Evidence |
|:---|:---|:---|
| P1 — `deriveMergeConclusion` tiered logic underspecified | **Resolved** ✅ | General plan P1 (lines 42–43): `'failure'` on Critical/Error → Blocked, `'neutral'` on Warning only → Needs review, `'success'` on zero active → Ready. Explicit three-way tier locked. |
| P2 — Card layout scope incomplete | **Resolved** ✅ | General plan P2 (lines 57–61): card field sets locked for all 5 tables — Findings (Severity+Title+File+State), PR list (Number+Title+Status+SHA+Revisions), Repo list (Name+Full name), Installations (Login+Status+Action), Team (Name+Role+Actions inline). |
| P3 — ReviewerHomePage empty state omitted | **Resolved** ✅ | General plan P3 (line 75): "`ReviewerHomePage` empty state: same pattern — refresh + help text + link to installations when no repos." |
| P3 — Landing dead-link finding contradicts codebase | **Resolved** ✅ | Findings M6 corrected — explains 404s were manually-typed, no dead links in code. Priority table updated. `visual-identity/README.md` updated. `UIUX_CONSOLE_GENERAL_PLAN.md` cross-cutting (line 26): "M6 dead links are removed (router only)" — now correctly refers to the `/reviewer` flash fix, not dead-page links. |

## Per-section findings (delta — changed sections only)

### P1 — Findings UX & merge verdict

| Severity | Area | Finding |
|:---|:---|:---|
| medium | general plan | Line 43: "`ReconciledFinding` type extended with or fetched alongside `ReviewFinding` fields on detail open" — still "or." The findings M1 explicitly recommends "Fetch `ReviewFinding` data when detail panel opens (on-demand, not preloaded)." The `useRevisionFindings` hook already exists and is never called. Lock this into the plan: use on-demand `ReviewFinding` fetch via `useRevisionFindings`. |
| low | general plan | Line 44: "sortable column headers (Severity, File, Date)" — `ReconciledFinding` has `created_at` and `updated_at`. No ambiguity anymore since tiering is locked, but note for execution: sort by `updated_at` (most useful for recency). |

**P0, P2, P3 — no new gaps** (verified unchanged from pass 1 for non-delta sections).

---

### Cross-cutting (delta updates only)

| Pass 1 open question | Status |
|:---|:---|
| 1. Merge verdict tiers: Critical only vs Error too? | **Resolved** ✅ — general plan explicitly says Critical/Error → `'failure'` |
| 2. Landing dead-link finding (M6) | **Resolved** ✅ — findings corrected, router verified |
| 3. `ReconciledFinding` extension vs `useRevisionFindings` | **Open** — still an "or" in general plan (see P1 medium above) |

## Pre-execution checklist (updated from pass 1)

- [x] Lock `deriveMergeConclusion` tiered logic ✅
- [x] Define card layout field sets for all 5 tables in P2 ✅
- [x] Add `ReviewerHomePage` empty state ✅
- [x] Resolve landing-page dead-link finding ✅
- [ ] Decide `ReconciledFinding` extension vs `useRevisionFindings` for detail sheet data (P1) — **still open, lock before P1 execution**
- [ ] Add test file expectations to phase deliverables (unchanged from pass 1)
- [ ] List settings pages to audit in P3 scope explicitly (unchanged from pass 1)
- [ ] Remove `max-w-[900px]` vs `max-w-3xl` ambiguity — plan now has `max-w-[900px]` only, findings M4 still mentions `max-w-3xl`/`max-w-4xl` as alternatives. Plan value is locked; findings reference is informational. Not blocking.

**Next:** `create-execution-plan` on this general plan.