# Architecture peer review — pass 1

**Plan folder:** `docs/visual-identity/console-ux-upgrade/`  
**Date:** 2026-09-20  
**Scope:** full — findings + general plan  
**Reviewer:** architecture-peer-review skill  
**Baseline:** [UIUX_CONSOLE_FINDINGS.md](../UIUX_CONSOLE_FINDINGS.md), [MOBILE_AND_LAYOUT_FINDINGS.md](../MOBILE_AND_LAYOUT_FINDINGS.md), [UIUX_CONSOLE_GENERAL_PLAN.md](../UIUX_CONSOLE_GENERAL_PLAN.md), codebase paths cited below

## Verdict

Review complete: 0 critical, 4 high, 4 medium/low.  
**BLOCK create-execution-plan:** no — findings are codebase-verified, plan phases are cohesive and correctly sequenced. High items are clarity gaps and missed deliverables, not architectural flaws. Fix before execution.

## Verified (docs match codebase)

| Claim | Code evidence |
|:---|:---|
| `deriveMergeConclusion` has dead `'failure'` return — severity threshold never wired | `frontend/src/features/reviewer/mergeConclusion.ts:4–10` — only `'success'` (zero active) and `'neutral'` (default), `'failure'` unreachable |
| `MergeReadinessBadge` has `'failure'` styling but that path is dead | `frontend/src/features/reviewer/components/MergeReadinessBadge.tsx:14` — `failure: 'bg-[color:var(--app-danger-subtle)]...'` stylized but unreachable |
| Sidebar hidden on mobile, no hamburger — `hidden md:flex` | `frontend/src/components/layout/AppShellLayout.tsx:44` — `aside className="hidden md:flex w-56 shrink-0 …"` |
| Mobile header has no hamburger button | `frontend/src/components/layout/AppHeader.tsx:7–10` — only `WorkspaceSwitcher` + `UserMenu`, no menu toggle |
| `max-w-lg` on dashboard (~512px) | `frontend/src/features/dashboard/pages/DashboardPage.tsx:17` — `className="mx-auto max-w-lg space-y-6"` |
| `max-w-lg` on settings content area | `frontend/src/features/settings/layout/SettingsLayout.tsx:9` — `className="mx-auto max-w-lg min-w-0 flex-1"` |
| All reviewer tables use `overflow-x-auto` with no mobile card layout | `PullRequestDetailPage.tsx:164`, `PullRequestListPage.tsx:50`, `ReviewerHomePage.tsx:100`, `InstallationsTable.tsx:23` — all have `overflow-x-auto` |
| Team table Actions column off-screen at narrow widths | `frontend/src/features/settings/components/MembersTable.tsx:65–70` — Actions column is the last `<th>`, no sticky positioning |
| `SettingsSidebar` as separate nav rail within settings | `frontend/src/features/settings/layout/SettingsLayout.tsx:7–9` — `<SettingsSidebar />` + `<Outlet />` in `flex-col md:flex-row` |
| `SettingsSidebar` is `md:w-52` second rail | `frontend/src/features/settings/layout/SettingsSidebar.tsx:52` — `className="w-full shrink-0 md:w-52"` |
| `DevNoticeWidget` uses `--app-primary` not `--app-cta-bg` | `frontend/src/features/dashboard/widgets/DevNoticeWidget.tsx:19` — `bg-[color:var(--app-primary)]` |
| No `SidebarProvider`, `useSidebar`, `SidebarTrigger`, `MobileBottomNav` exist in codebase | Grep across `frontend/src/` returns zero matches for all four symbols |
| `useRevisionFindings` exists but is never called from any component | `frontend/src/features/reviewer/hooks.ts:143–159` — exported but zero callers (grep confirmed) |
| `ReconciledFinding` has no `start_line`, `end_line`, or `suggestion` | `frontend/src/features/reviewer/types.ts:120–135` — no line-range or suggestion fields; those exist only on `ReviewFinding` (lines 108–117) |
| Landing page has no dead `/about`, `/features`, `/pricing`, `/docs` routes in router | `frontend/src/lib/routerInstance.tsx` — no such routes defined; only `'/' → LandingPage` |
| P0 → P1 → P2 dependency chain is correct | P0 creates `SidebarProvider` + Sheet (needed by P1 detail sheet); P1 redesigns findings table (needed by P2 to add responsive cards); P3 needs P0 bottom nav slot + P1 toolbar |
| CTA uses `--app-cta-bg` / `--app-cta-fg` in all other 17 buttons | Grep `--app-cta-bg` across `frontend/src/` — 17 matches (LandingPage, all settings pages, admin), only `DevNoticeWidget` uses `--app-primary` |
| Appearance settings is already disabled | `frontend/src/features/settings/pages/AppearanceSettingsPage.tsx` — confirmed disabled per VI-Q2 (checked but not reproduced for brevity; findings state this as locked) |

## Rollup (critical / high)

| Severity | Section | Title |
|:---|:---|:---|
| high | P1 | `'failure'` logic detail is underspecified — `deriveMergeConclusion` return condition not explicit in general plan |
| high | P2 | Responsive table card layout scope incomplete — only findings table has row-click sheet, other tables need card touch targets defined |
| high | P3 | Empty state for `PullRequestListPage` in scope but `ReviewerHomePage` (repo list empty) omitted |
| high | P3 | Landing dead links finding (C2/M6) contradicts codebase — no dead routes exist; router evidence conflicts with findings claim |

## Per-section findings

### P0 — Shell, sidebar & responsive layout

| Severity | Area | Finding |
|:---|:---|:---|
| medium | general plan | Container width target specified as `max-w-[900px]` but findings (M4) also suggest `max-w-3xl` (~768px) or `max-w-4xl` (~896px) as alternatives. Plan should lock a single value. |
| low | general plan | `AppSidebar` extraction is mentioned in deliverables (M2 recommendation) but not listed as a new file in the general plan — implied by "settings flattened into main sidebar" |
| low | general plan | Settings flattening removes `SettingsLayout` + `SettingsSidebar` (2 components) but general plan doesn't list the deleted files or note that `routerInstance.tsx` must be updated to remove `SettingsLayout` wrapper |

**Verified OK:**
- `SidebarProvider` context + three-state (expanded/collapsed/overlaid) correctly follows external reference patterns.
- `Cmd/Ctrl+B` shortcut, localStorage persistence, 200ms animation, `prefers-reduced-motion` — all explicit.
- 44px touch targets mandated in cross-cutting rules.
- Settings accordion group in main sidebar is well-specified.

---

### P1 — Findings UX & merge verdict

| Severity | Area | Finding |
|:---|:---|:---|
| high | general plan | `deriveMergeConclusion` must return `'failure'` when active Critical or Error findings exist — but the general plan says only "Critical/Error active" without specifying which severity triggers `'failure'` and which triggers `'neutral'` / `'needs review'`. The findings (C1) say: Critical/Error → Blocked, Warning only → Needs review, none → Ready. General plan should restate that explicit tiered logic. |
| medium | general plan | `ReconciledFinding` type extension or `ReviewFinding` fetch is mentioned ("extended with or fetched alongside ReviewFinding") but as an OR decision — the general plan should lock which approach (extend type on fetch vs on-demand detail fetch) since P1 deliverables depend on it. |
| medium | general plan | Detail sheet shows "file + line range" but `ReconciledFinding` has no `start_line`, `end_line` — only `ReviewFinding` has them. General plan should explicitly state that detail sheet fetches `ReviewFinding` data on open (as findings M1 recommends). |
| medium | findings | `useRevisionFindings` hook exists in codebase but is never called — P1 could reuse this instead of a new `useFindingDetail` hook listed in deliverables. Plan doesn't mention it. |
| low | general plan | "Sortable column headers (Severity, File, Date)" — current findings table has no date column. Is this "created_at" from `ReconciledFinding` or from `ReviewFinding`? Clarify. |
| low | general plan | Loading skeleton spec says "4-row ghost table" but infinite scroll means row count is unbounded — skeleton should be a fixed placeholder not tied to real count. Plan is fine, just note it's already clear. |

**Verified OK:**
- MergeReadinessBadge four states (Ready/Blocked/Needs review/Unavailable) correctly mapped to findings C1 recommendation.
- Sticky summary bar with severity counts + next-action line matches C2+ recommendation.
- Toolbar (search, filter, sort, result count) matches M1 recommendation.
- Row click → detail Sheet matches M1 detail drawer recommendation.
- Loading skeletons replace generic "Loading..." — matches M2 recommendation.

---

### P2 — Responsive tables

| Severity | Area | Finding |
|:---|:---|:---|
| high | general plan | Scope lists 5 tables (findings, PR list, repo list, installations, Team) but card layout spec only details findings table behavior (severity + title + file + state). The other 4 tables need explicit card layouts defined: PR list (number + title + state), repo list (name + full name), installations (account + status), Team (name + role + actions). Without this, P2 is underspecified. |
| medium | general plan | Findings toolbar "stacks vertically on mobile (search full-width, filters collapsible)" — described in findings M5 but not in P2 deliverables of the general plan. Toolbar adaptation belongs in P2 scope. |
| low | general plan | Team table "Actions column sticky on desktop" — this is a P2 deliverable that's really a desktop fix (sticky column via CSS), not a mobile card concern. Should it be in P1 (table redesign) or P3 (polish)? Plan puts it in P2 which is fine but its deliverable category is mixed. |

**Verified OK:**
- Card/stacked at `< md` breakpoint, `overflow-x-auto` hidden — correct approach.
- Label-value pair pattern for stacked cards — matches external reference.
- Depends correctly on P0 (shell) + P1 (findings table redesigned).

---

### P3 — Polish, empty states & bottom nav

| Severity | Area | Finding |
|:---|:---|:---|
| high | general plan | Findings M6 ("dead links on landing: `/about`, `/features`, `/pricing`, `/docs` return 404") is cited as baseline but the codebase router (`routerInstance.tsx`) has **no such routes** — only `'/' → LandingPage` and `'*' → NotFoundPage`. The dead-link finding appears to be false or refers to a past state. General plan P3 scope says nothing about fixing dead landing links, which is correct — but the findings doc should be corrected so P3 doesn't waste time chasing a non-issue. |
| high | findings | `PullRequestListPage` empty state (M5) is in scope with "refresh button + help text + link to installations" but `ReviewerHomePage` empty state (no repositories) is not mentioned — it has the same generic empty text pattern. Either merge into P3 scope or note it as out-of-scope by name. |
| medium | general plan | P3 scope says settings audit: "focus on Security, Billing, Review stubs" but doesn't name which are "Coming soon" vs functional. Findings M3 says "mark clearly as Coming soon if not implemented" but the general plan should list at minimum: Security (functional?), Billing (stub?), Review (disabled selectors — confirmed stub). |
| low | general plan | Contrast audit is listed as "Open (calibration only)" at plan bottom — if it's a P3 deliverable, it should be in-scope with a concrete tooling step (axe-core, Lighthouse CI, or manual). |
| low | general plan | Terminology tooltips (m1) — plan says "key terms have tooltips" but doesn't name which terms (Review, Judge, Publish, Reconciled?) — minor, can be locked in execution. |

**Verified OK:**
- `DevNoticeWidget` token fix (`--app-cta-bg`) correctly identified.
- `MobileBottomNav` component spec (4 tabs, 44px targets, active highlight) is clear.
- Bottom nav depends on P0 shell slot — correct.
- CTA button consistency — correct scope.

---

## Cross-cutting

| Check | Status |
|:---|:---|
| Header links findings + program README | ✅ General plan header links both findings docs |
| Locked decisions in general plan match findings registry | ✅ "Landing stays as-is", "`--app-*` only", "`deriveMergeConclusion` must return `'failure'`" — all match |
| Each phase: Goal, Scope, Deliverables, Depends on | ✅ All four phases have all four sections |
| Prerequisites cited as merged on target branch | N/A — no external program dependencies (P0–P4 is shipped baseline) |
| New persistence / migrations | N/A — no backend changes, no new DB tables |
| APIs/services/models named in plan exist or are explicitly new | ✅ No new APIs — plan is frontend-only |
| Test paths in deliverables named as new | ⚠️ Cross-cutting rule says "Vitest colocated with every new/changed component and hook" but no phase deliverables explicitly list test files (e.g. `SidebarProvider.test.tsx`, `FindingsToolbar.test.tsx`, `MobileBottomNav.test.tsx`) |
| Human/ops gates in correct phase | ✅ P3 implies staging verification for contrast audit; no deploy gates mixed into code phases |
| No TBD / open options | ⚠️ Three open points: (1) `ReconciledFinding` extension vs fetch approach (P1), (2) exact severity threshold for `deriveMergeConclusion` tiers (P1), (3) contrast numbers from audit (P3) — flagged as "calibration only" |
| **Next step** matches findings | ✅ `create-execution-plan` — correct |
| i18n — EN + LV for all new user-facing strings | ✅ Cross-cutting rule mandates `t()` for every new string |
| `--app-*` token compliance | ✅ Cross-cutting rule mandates no hex in feature UI |
| Migration policy: no `--autogenerate` | N/A — no backend changes |

## Open questions (max 3)

1. **P1 merge verdict tiers:** Should `'failure'` trigger on Critical only, or also Error? General plan says "Critical/Error active" → Blocked. Both findings (C1) and M1 recommendation use "Critical or Error" — confirm this is the intended gate. If Error also triggers Blocked, the existing `FindingSeverity` type supports it.
2. **P3 dead-link finding (M6):** The router has no `/about`, `/features`, `/pricing`, `/docs` routes. Was this finding from an earlier build or a different deploy? The general plan correctly does NOT include fixing dead links in P3, but the findings doc should be corrected to avoid confusion during execution.
3. **P1 detail sheet approach:** Lock on extending `ReconciledFinding` with `start_line`/`end_line`/`suggestion` on fetch (backend change needed?) vs on-demand `ReviewFinding` fetch via `useRevisionFindings` (already coded, never called). Recommend the latter — zero backend work, existing hook. Confirm before execution.

## Pre-execution checklist

- [ ] Lock `deriveMergeConclusion` tiered logic: Critical → `'failure'`, Error → `'failure'`, Warning only → `'neutral'` (or add `'needs_review'` to type?), none → `'success'`
- [ ] Define card layout field sets for all 5 tables in P2 (findings, PR list, repo list, installations, Team)
- [ ] Add `ReviewerHomePage` empty state or explicitly exclude it from P3 scope
- [ ] Resolve landing-page dead-link finding (remove from findings if inaccurate; do NOT add slop pages)
- [ ] Decide `ReconciledFinding` extension vs `useRevisionFindings` for detail sheet data (P1)
- [ ] Add test file expectations to phase deliverables (or note that tests are covered by the cross-cutting Vitest rule — currently implicit)
- [ ] List settings pages to audit in P3 scope explicitly (Security → ?, Billing → ?, Review → stub)
- [ ] Remove `max-w-[900px]` vs `max-w-3xl` ambiguity — lock one value in plan

**Next:** fix agent reads this pass file → updates general plan and/or findings → re-run architecture-peer-review (pass 2, delta on changed sections) → `create-execution-plan`