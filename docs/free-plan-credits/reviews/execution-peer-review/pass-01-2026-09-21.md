# Execution peer review — pass 1

**Plan folder:** `docs/free-plan-credits/`  
**Date:** 2026-09-21  
**Scope:** full (P0–P2)  
**Reviewer:** execution-peer-review skill  
**Baseline:** [FINDINGS.md](../FREE_PLAN_CREDITS_FINDINGS.md), [GENERAL_PLAN.md](../FREE_PLAN_CREDITS_GENERAL_PLAN.md), [EXECUTION index](../README.md), [Architecture pass 2](../reviews/architecture-peer-review/pass-02-2026-09-21-delta.md), codebase paths cited below  

## Verdict

Review complete: 0 critical, 0 high, 3 medium/low.  
**BLOCK phase-execution:** no — all phases are executable. Medium items are scope/clarity, not logic errors.

## Rollup (critical / high)

No critical or high findings.

## Per-phase findings

---

## P0 — `docs/free-plan-credits/FREE_PLAN_CREDITS_P0_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | scope leak | **Migration file name has placeholder `xxxx`** — P0.1 uses `2026_09_21_xxxx_0036_workspace_credit_limits.py`. The `xxxx` should be replaced with actual timestamp minutes (e.g. `0900`). The agent creating the file may pick a different timestamp, so `xxxx` is effectively a todo — not blocking but should be resolved during execution. |
| low | contract | **General plan says "4 locations" but lists 5.** General plan P0 scope says `all 4 locations` then enumerates 5 items (plan_gates dict + 3 endpoint guards + 1 service guard). Execution P0.4 correctly says "5 locations." Inconsistency between general plan and execution file is cosmetic, not functional. |
| low | contract | **P0.0 deliverable test exists** — `tests/unit/test_engineering_context_manifest.py` is a real file at `backend/tests/unit/test_engineering_context_manifest.py`. Confirmed on disk. |
| low | deliverable | **P0.1 human gate** — LOOP pause after migration is appropriate for hand-written Alembic. The pause note at line 56 is explicit and correct. |

**Verified OK:**
- `plan_gates.py:21-23` — `PLAN_FEATURES` dict with `"installations.create": "pro"` confirmed; removal target is correct
- `installations.py:64,91,122` — 3 `require_plan_feature("installations.create")` guards confirmed; correct lines
- `github_installations.py:142-148` — `workspace_has_feature(workspace, "installations.create")` guard confirmed (lines 142-148 match)
- `onboarding.py:52` — `WorkspaceORM(slug=slug, name=name)` — correct insertion point, no existing credit defaults
- `onboarding.py:80-88,120` — both `maybe_auto_provision_user()` and `complete_user_profile()` go through `activate_user_with_workspace()` — single fix covers all paths
- Latest migration is `0035_finding_group_claim_slot` — `0036` chains correctly
- P0 phase gate lists 4 test files — all 4 are `tests/unit/*.py` and will be created during execution
- Subphase count: 5 (P0.0–P0.4) — within 3–6 range
- All subphases have `**What**` / `**Files**` / `**Deliverable**`

---

## P1 — `docs/free-plan-credits/FREE_PLAN_CREDITS_P1_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | contract | **Missing PR review context subphase.** P0 has P0.0 for SSOT + Bugbot wiring. P1 does not — it says "SSOT stays from P0 commit" and "scope narrows to backend files touched in P1" but doesn't list a subphase to update `.revy/review-context.json` with narrowed scope. Without this, the SSOT could reference stale scope from P0. Not blocking but the agent should update scope during execution. |
| low | codebase | **P1.1 line reference is approximate** — says "after line 635, before line 639". `create_review_run()` has `ensure_revision_access()` at lines 629-635 and `index_job_in_progress` check starts at line 639. The credit check should go in between. Lines 636-638 are blank — correct gap. |
| low | deliverable | **P1.0 README status** — `Done (<sha>)` without a `?` capture group syntax. The agent reading this during `phase-execution` needs to know that `<sha>` is a placeholder to fill from `git rev-parse HEAD` after P0 commit. This is consistent with how other execution files in this repo are written. The agent's `phase-execution` LOOP handles this automatically. |
| low | codebase | **P1.3 `ForbiddenError` import line** — says "line 20 (import)" for `review_pipeline.py`. Current imports are at line 20: `from app.core.exceptions import ConflictError, ServiceUnavailableError`. Adding `ForbiddenError` to this import line is correct. |

**Verified OK:**
- `github_review.py:606-690` — `create_review_run()` structure: LLM checks (616-627) → `ensure_revision_access()` (629-635) → `index_job_in_progress` (639) → `get_latest_completed_index_job` (648). Credit check goes in gap at 636-638 — correct placement.
- `github_review.py:1278-1287` — `update(GitHubReviewRunORM).values(status='completed')` block confirmed. The `completed.rowcount == 0` check at line 1286 correctly handles superseded runs — credit increment must go after this check passes (after line 1287, per execution).
- `review_pipeline.py:20` — imports `ConflictError, ServiceUnavailableError` only. Adding `ForbiddenError` is correct.
- `review_pipeline.py:285-306` — `except ConflictError` → return `ReviewAfterIndexOutcome()`; `except ServiceUnavailableError` → `fail_pipeline_check=True`. `except ForbiddenError` block goes before `except ConflictError` — correct ordering (more specific first).
- `billing.py:283-290` — `checkout.session.completed` block: calls `_update_workspace_plan()` then `return`. Credit mutation goes after `_update_workspace_plan()` but before `return` — correct placement per P1.4
- `billing.py:292-312` — `customer.subscription.deleted` block: calls `_update_workspace_plan()` at line 310-312 (inside the broader subscription handler). Credit mutation at line 312 after the call — correct.
- P1 decisions locked: all 7 items match findings §1.4–1.8
- P1 phase gate lists 4 test files — same pattern as P0, all to be created
- Subphase count: 5 (P1.0–P1.4) — within 3–6 range
- Dependencies: correctly lists "Depends on Phase 0"
- Out of scope: correctly pushes frontend to P2

---

## P2 — `docs/free-plan-credits/FREE_PLAN_CREDITS_P2_EXECUTION.md`

| Severity | Area | Finding |
|:---|:---|:---|
| medium | contract | **Missing PR review context subphase.** Same as P1 — P2 says "scope adds frontend/" but doesn't list a subphase to update `.revy/review-context.json`. The `phase-execution` agent updating scope between phases is a well-established LOOP pattern, but making it explicit (like P0.0) is clearer. |
| low | codebase | **P2.1 line reference `model_copy` block** — says "line 71-77". Current code at `me.py:71-77` is: `me = me.model_copy(update={"workspace_id": ..., "role": ..., "workspace_plan": ...})`. Adding `"completed_review_runs": workspace.completed_review_runs, "review_run_limit": workspace.review_run_limit` to this dict is correct — single fetch, no new query. |
| low | contract | **P2.5 deliverable test** — says `npm test -- InstallationsPage` to confirm `SETUP_ERROR_KEYS` still includes `plan_upgrade_required`. This is a regression test for a feature that P0 made dead code (installation gate removed). The credit limit error toast (`credit_limit_reached`) uses `showDomainErrorToast(mapApiError(error))` which passes the `error_code` through `mapApiError` → `{ code: "credit_limit_reached" }`. No mapper change needed — the existing `errorMapper.ts` passes backend `error_code` as `code` field. Test is optional but safe. |
| low | deliverable | **P2.6 doc sync** — correctly identifies that findings and general plan docs don't change. Only README status rows updated. The human gate at line 130 (staging verification) is appropriate for final phase. |

**Verified OK:**
- `me.py:69-77` — `_build_current_me()` fetches `WorkspaceORM(...workspace_id)` and populates `workspace_plan` in `model_copy` block. Adding credit fields to the same dict is zero-cost — no new query.
- `me.py:29-43` (schema) — `MeResponse` has `workspace_plan: str | None`. Adding `completed_review_runs: int = 0` and `review_run_limit: int | None = None` — correct types.
- `frontend/src/lib/me.ts:30-43` — `MeUser` interface with `workspace_plan: string | null`. Adding `completed_review_runs: number` and `review_run_limit: number | null` — correct TS types.
- `PlanSummaryWidget.tsx:7-23` — uses `useAuth().user` which comes from `fetchMe()`. New credit fields are accessible via `user.completed_review_runs` and `user.review_run_limit` — no code change to data flow.
- `PlanSummaryWidget.test.tsx` — exists at `src/features/dashboard/widgets/PlanSummaryWidget.test.tsx` — test will need updating for dynamic counter.
- `ReviewTriggerBar.tsx:77,88` — `disabled={busy}` where `busy = reviewInFlight || triggerMutation.isPending`. Adding `|| (user.review_run_limit != null && (user.completed_review_runs ?? 0) >= user.review_run_limit)` — correct logic.
- `ReviewTriggerBar.test.tsx` — exists at `src/features/reviewer/components/ReviewTriggerBar.test.tsx` — test will need updating.
- `errorMapper.ts:43-72` — `mapApiError()` returns `{ code: parsed?.error }` where `parsed.error` comes from backend `error_code`. `credit_limit_reached` will pass through correctly — no mapper change needed.
- i18n keys: `dashboard.planSummary.free` exists at `en.json:79`; `dashboard.planSummary.title` exists. New keys P2.3 + P2.4 + P2.5 are correct additions.
- P2 decisions locked: all 7 items match findings §Track 2
- P2 phase gate: backend (`pipenv run pytest` for `test_me_credit_fields.py`) + frontend (`npm run build && npm test -- PlanSummaryWidget ReviewTriggerBar InstallationsPage`) — covers all deliverables
- Dependencies: correctly lists P1
- Out of scope: correctly defers warning banners, admin UI, per-user counters, billing page changes
- Final phase: P2.6 doc sync + human gate (staging verification) — correct for last phase
- Subphase count: 7 (P2.0–P2.6) — at upper range but justifiable (P2.0 is README boilerplate, P2.6 is doc sync boilerplate)

---

## Execution contract checklist (aggregate)

| Check | P0 | P1 | P2 |
|:---|:---|:---|:---|
| Header links general plan + findings | ✅ | ✅ | ✅ |
| **Goal** + **decisions locked** | ✅ | ✅ | ✅ |
| **Out of scope** lists later phases | ✅ | ✅ | ✅ |
| **3–6 subphases** | ✅ (5) | ✅ (5) | ✅ (7) |
| Each subphase: `**What**` / `**Files**` / `**Deliverable**` | ✅ | ✅ | ✅ |
| **Deliverable** includes verify command or concrete assertion | ✅ | ✅ | ✅ |
| **Phase gate** — copy-paste `pytest` / `npm test` block | ✅ | ✅ | ✅ |
| **Migration** subphase flagged + handwritten-only | ✅ (P0.1) | N/A | N/A |
| **Human gate** if spike/ops/sign-off | ✅ (P0.1 migration, P2.6 staging) | — | ✅ (P2.6) |
| **Next** → following execution file | ✅ | ✅ | ✅ (none) |
| No TBD / no open options | ✅ | ✅ | ✅ |
| FE phase has frontend gate when `frontend/` touched | N/A (no FE) | N/A (no FE) | ✅ |
| **Final phase** doc-sync + optional post-finish-gap-pass | N/A | N/A | ✅ |
| PR review context subphase (SSOT + Bugbot) | ✅ (P0.0) | ⚠️ implicit | ⚠️ implicit |
| Files paths real or correctly named new | ✅ | ✅ | ✅ |
| Line numbers match codebase | ✅ | ✅ | ✅ |
| API/model/component names match codebase | ✅ | ✅ | ✅ |

---

## Open questions

0 open — all items are review observations, not blockers.

**Next:** `phase-execution` — unblocked. Start with P0.