# Architecture peer review — pass 2 (delta)

**Plan folder:** `docs/free-plan-credits/`  
**Date:** 2026-09-21  
**Scope:** delta — P0 workspace creation default, P1 ForbiddenError catch, P1 Stripe downgrade placement, P2 credit fields on me endpoint  
**Reviewer:** architecture-peer-review skill  
**Baseline:** [FREE_PLAN_CREDITS_FINDINGS.md](../FREE_PLAN_CREDITS_FINDINGS.md), [FREE_PLAN_CREDITS_GENERAL_PLAN.md](../FREE_PLAN_CREDITS_GENERAL_PLAN.md)  
**Prior pass:** [pass-01-2026-09-21.md](pass-01-2026-09-21.md) — 3 high, 3 medium

## Prior pass

Pass 1 found 3 high items: (H1) P0 missing workspace creation default, (H2) P1 ForbiddenError escaping autostart pipeline, (H3) P2 billing endpoint admin-gated for credit display. All three were addressed by the fix agent. This delta pass re-reviews only the changed sections.

## Verdict

Review complete: 0 critical, 0 high, 2 medium/low.  
**BLOCK create-execution-plan:** no — all high items resolved, plan is executable.

---

## H1 — P0 workspace creation default → RESOLVED ✅

**Prior finding:** Plan P0 didn't include `review_run_limit = 25` on new workspace creation, despite findings §1.7 calling it out.

**Current state:**

| Severity | Area | Finding |
|:---|:---|:---|
| — | — | **Resolved.** P0 deliverables now include "`review_run_limit = 25` set on new workspace creation (`onboarding.py`)". Scope line says "workspace creation default". General plan line 18: `review_run_limit = 25 set on new workspace creation (onboarding.py)`. |

**Verified OK:**
- `backend/app/services/onboarding.py:52` — `activate_user_with_workspace()` creates `WorkspaceORM(slug=slug, name=name)` — no credit defaults yet (by design — this is implementation work)
- `backend/app/services/onboarding.py:80-88` — auto-provision path also calls `activate_user_with_workspace()`, so the single fix covers both registration flows
- `backend/app/services/onboarding.py:120` — `complete_user_profile()` → `activate_user_with_workspace()` also covered

---

## H2 — P1 ForbiddenError escaping autostart pipeline → RESOLVED ✅

**Prior finding:** `review_pipeline.py:285` catches only `ConflictError` and `ServiceUnavailableError`. `ForbiddenError(credit_limit_reached)` from `create_review_run()` would crash the pipeline.

**Current state:**

| Severity | Area | Finding |
|:---|:---|:---|
| — | — | **Resolved.** General plan P1 scope now reads: "`ForbiddenError` catch in autostart pipeline (`review_pipeline.py` — currently only catches `ConflictError` + `ServiceUnavailableError`)". Deliverables say: "autostart pipeline catches `ForbiddenError` → `fail_pipeline_check=True` with `pipeline_check_summary=\"credit_limit_reached\"`". |

**Verified OK:**
- `backend/app/services/review_pipeline.py:20` — imports `ConflictError, ServiceUnavailableError` only. Implementation must add `ForbiddenError` to the import and add `except ForbiddenError as exc:` block (this is correct — it's the work to be done, not a gap in the plan)
- `backend/app/services/review_pipeline.py:285-306` — two exception handlers confirmed; implementation is straightforward

---

## H3 — P2 credit fields on me endpoint → RESOLVED ✅

**Prior finding:** Plan said "extend BillingStatus schema" via billing endpoint, but `useBillingStatus()` requires `canManageBilling` (admin-only). Regular users couldn't see their credit counter.

**Current state:**

| Severity | Area | Finding |
|:---|:---|:---|
| — | — | **Resolved.** P2 scope now says "Extend `me` endpoint schema to include `completed_review_runs` + `review_run_limit`". Findings §2.1 rewritten: "Why not billing endpoint" section explains the admin gate. Q3 decision registry updated: "Extend `me` endpoint (not billing — billing is admin-gated)." |

**Verified OK:**
- Backend: `backend/app/api/v1/me.py:69-76` — `_build_current_me()` fetches `WorkspaceORM(workspace_id)` and sets `workspace_plan`. Credit fields (`completed_review_runs`, `review_run_limit`) can be added to the same `model_copy(update={...})` block — clean, single fetch, no new query needed
- Backend schema: `backend/app/schemas/me.py:29-43` — `MeResponse` has `workspace_plan: str | None`. Implementation: add `completed_review_runs: int` and `review_run_limit: int | None` to `MeResponse` and populate in `_build_current_me`
- Frontend: `frontend/src/lib/me.ts:30-43` — `MeUser` has `workspace_plan: string | null`. Implementation: add `completed_review_runs: number` and `review_run_limit: number | null` to `MeUser`
- Frontend dashboard: `PlanSummaryWidget.tsx` uses `useAuth().user` which comes from `fetchMe()` — no code change needed to access new fields
- `ReviewTriggerBar` can derive credit status from the same `useAuth().user` — no cross-widget data sharing needed

---

## Medium — P1 Stripe downgrade placement → RESOLVED ✅

**Prior finding:** Plan said "Stripe webhook sets limit on downgrade" but didn't clarify placement (in `_update_workspace_plan` vs `apply_subscription_event`). `_update_workspace_plan` is shared with upgrade path, so credit mutation there would be wrong.

**Current state:**

| Severity | Area | Finding |
|:---|:---|:---|
| — | — | **Resolved.** General plan P1 deliverables now specify: "Stripe webhook `customer.subscription.deleted` sets `review_run_limit = 25` (in `apply_subscription_event`, not `_update_workspace_plan`)". Findings §1.7 now explicitly says: "Do this **after** `_update_workspace_plan()` returns (not inside it — that function is shared with upgrade path)." Findings §1.8 also updated: "when checkout completes … set `review_run_limit = NULL`". |

**Verified OK:**
- `backend/app/services/billing.py:292-312` — `apply_subscription_event()` for `customer.subscription.deleted`: calls `_update_workspace_plan(session, workspace=workspace, new_plan="free")`. The credit mutation (`workspace.review_run_limit = 25`) can be added right after line 312 inside the `if event_type` block — clean and correct
- `backend/app/services/billing.py:200-230` — `_update_workspace_plan()` untouched, as designed
- Upgrade path in checkout handler (line 283-290) — calls `_update_workspace_plan(session, workspace=workspace, new_plan=plan)` where `plan = "pro"`. Credit mutation (`review_run_limit = NULL`) also placed in `apply_subscription_event` after the call — consistent with downgrade pattern

---

## Medium/low — remaining from pass 1

| Pass 1 finding | Status |
|:---|:---|
| P0 clarity: 4 locations not all enumerated | **Open** — still says "all 4 locations (API + OAuth callback + plan_gates dict)" but doesn't list them individually. Low severity, not blocking. |
| Migration number now `0036` | **Resolved** ✅ — findings §1.1 now says `2026_09_21_xxxx_0036_workspace_credit_limits.py` |
| P0 no explicit unit test deliverable | **Open** — cross-cutting line says "Every phase ships: backend unit tests" but P0 deliverables don't list specific test files. Consistent with how other P0 plans are written in this repo. Not blocking. |
| Dashboard loading state with real data | **Open** — `PlanSummaryWidget` currently has no loading state (it reads from `useAuth().user` which is always initialized). With dynamic credit data, the widget itself doesn't change — `completed_review_runs` and `review_run_limit` are always present in `MeResponse`. No loading state needed. **Resolved** ✅ — the me endpoint always returns the workspace, and credit fields have defaults (0 and None). |

---

## Cross-cutting re-check (changed sections only)

| Check | Status |
|:---|:---|
| Q3 decision updated to "me endpoint (not billing)" | ✅ — findings line 268 |
| General plan P2 no longer references billing endpoint | ✅ — scope says "me endpoint", scope out says "billing endpoint changes" |
| P1 ForbiddenError catch in deliverables matches scope description | ✅ |
| P0 workspace creation default consistent between findings (§1.7) and general plan (P0 deliverables) | ✅ |
| Stripe downgrade consistently positioned in `apply_subscription_event` across findings and plan | ✅ |
| Me endpoint placement consistent: findings §2.1 description matches `_build_current_me` code path | ✅ |

---

## Open questions

0 open — all pass-1 questions are answerable from the updated documents:

1. *(from pass 1)* Dashboard credit visibility: resolved — `me` endpoint serves all users.
2. *(from pass 1)* Downgrade credit behavior: findings edge cases section explicitly states "immediate consequences" — no warning needed.
3. *(from pass 1)* In-flight runs at limit: pass-1 question #3 remains a design decision, not a plan gap — credit check is creation-time gate, completion-time increment is counter-mechanical. The plan is correct as written; no rework needed.

---

## Pre-execution checklist

- [x] Workspace creation default in P0 deliverables — ✅
- [x] ForbiddenError catch in P1 deliverables — ✅
- [x] Credit fields on me endpoint — ✅
- [x] Stripe downgrade in `apply_subscription_event` — ✅
- [x] Migration number `0036` — ✅

**Next:** `create-execution-plan` — unblocked. All high items resolved, plan is ready for execution plan.