# deploy/nginx/auth.revy.createit.digital.conf
# Install: /etc/nginx/sites-available/auth.revy.createit.digital
# Enable:  ln -sf /etc/nginx/sites-available/auth.revy.createit.digital /etc/nginx/sites-enabled/
#
# Domain:        auth.revy.createit.digital
# Public URL:    https://auth.revy.createit.digital
# Issuer:        https://auth.revy.createit.digital/realms/revy
# JWKS:          https://auth.revy.createit.digital/realms/revy/protocol/openid-connect/certs
#
# TLS: same SAN cert as app (cert-name revy.createit.digital)
#
# Upstream: revy-kc (Keycloak 26) → 127.0.0.1:8080
# Health (localhost only, not proxied): curl http://127.0.0.1:9000/health/ready
#
# Keycloak .env alignment:
#   KC_HOSTNAME=https://auth.revy.createit.digital
#   KC_HOSTNAME_STRICT=true
#   KC_HTTP_ENABLED=true
#   KC_PROXY_HEADERS=xforwarded
#   KC_HEALTH_ENABLED=true
#   KC_METRICS_ENABLED=true
#
# GitHub / SPA build:
#   VITE_KEYCLOAK_URL=https://auth.revy.createit.digital
#   VITE_KEYCLOAK_REALM=revy
#   VITE_KEYCLOAK_CLIENT_ID=revy-web
#
# Backend (Docker revy-net):
#   KEYCLOAK_URL=http://keycloak:8080
#   KEYCLOAK_REALM=revy

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name auth.revy.createit.digital;

    ssl_certificate     /etc/letsencrypt/live/revy.createit.digital/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/revy.createit.digital/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    client_max_body_size 20m;

    location ~ ^/(health|metrics)(/|$) {
        return 404;
    }

    location / {
        proxy_pass http://127.0.0.1:8080;

        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host  $host;
        proxy_set_header X-Forwarded-Port  $server_port;

        proxy_http_version 1.1;
        proxy_set_header Upgrade           $http_upgrade;
        proxy_set_header Connection        "upgrade";

        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }
}
